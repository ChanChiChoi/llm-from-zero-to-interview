# 第 14 章 多租户隔离：资源隔离、权限隔离和数据隔离

前面我们讲了 Kubernetes、GPU 管理和训练任务调度。本章讲企业 AI Infra 里非常现实的问题：多个团队、多个项目、多个用户共用同一套 GPU 集群时，如何隔离？

如果没有多租户隔离，AI 平台很快会出现资源被抢占、数据被误读、权限失控、成本无法归因、日志混乱、镜像不可信、线上推理被实验任务干扰等问题。

先记住一句话：

> 多租户隔离不是只建几个 namespace，而是资源、身份、权限、数据、网络、镜像、密钥、审计和成本的系统性治理。

## 14.0 本讲资料边界与第二轮精修口径

本讲第二轮精修时，资料口径按“AI Infra 多租户隔离的稳定治理抽象”处理，而不是绑定某个云厂商账号体系、某个 Kubernetes 发行版、某个 service mesh 或某个内部平台实现。Kubernetes 侧重点参考官方 multi-tenancy、Namespace、ResourceQuota、LimitRange、RBAC、ServiceAccount、NetworkPolicy、Secret、Pod Security Standards 和静态数据加密等文档口径；容器和供应链侧沿用前文对镜像 digest、扫描、签名、registry 权限和运行时加固的边界；数据治理侧只抽象分类、授权、使用目的、血缘、产物继承和审计，不写成某个厂商的 IAM / DLP / KMS 私有配置。

需要注意三点：

1. Namespace 是多租户治理入口，不是完整隔离边界。只建 namespace 但不配 resource quota、RBAC、network policy、secret scope、data gateway、日志脱敏和审计，仍然不是可靠多租户。
2. AI Infra 的多租户隔离比普通 Web 服务更复杂，因为训练数据可能流入 checkpoint、模型权重、eval report、日志、trace、embedding、RAG 文档索引和下游微调产物。
3. 这里的 demo 是教学审计脚本，不调用真实 Kubernetes API。真实平台需要从 IdP、RBAC / ABAC policy engine、Kubernetes admission、scheduler、data access gateway、secret manager、registry、observability、audit log 和 cost ledger 采集证据。

## 14.1 什么是多租户

租户可以是：

1. 一个业务团队。
2. 一个项目组。
3. 一个部门。
4. 一个外部客户。
5. 一个环境，例如 dev、staging、prod。

多租户就是多个租户共享同一套平台能力。

共享的好处：

1. 提高 GPU 利用率。
2. 降低重复建设成本。
3. 统一平台能力。
4. 统一监控和治理。
5. 统一安全和审计。

共享的风险：

1. 资源互相抢。
2. 数据互相看见。
3. 网络互相访问。
4. 密钥泄露。
5. 镜像污染。
6. 成本无法归因。
7. 故障影响扩大。

多租户隔离的目标，就是在共享资源的同时控制这些风险。

## 14.2 多租户隔离的层次

AI Infra 多租户隔离至少包括八层：

1. 资源隔离。
2. 权限隔离。
3. 数据隔离。
4. 网络隔离。
5. 运行时隔离。
6. 镜像和供应链隔离。
7. 密钥隔离。
8. 审计和成本隔离。

其中任何一层缺失，都可能出问题。

例如资源隔离做得好，但数据权限没做好，仍然可能泄露训练数据。

又比如数据权限做得好，但密钥以环境变量泄露到日志里，也会出事。

## 14.3 资源隔离

资源隔离回答：每个租户能使用多少 GPU、CPU、内存、存储和网络？

常见资源：

1. GPU 数量。
2. GPU 型号。
3. GPU 小时。
4. CPU。
5. 内存。
6. 本地盘。
7. 共享存储容量。
8. 网络带宽。
9. 推理 QPS。
10. 队列并发。

资源隔离手段：

1. Namespace。
2. ResourceQuota。
3. Queue quota。
4. PriorityClass。
5. LimitRange。
6. 独立资源池。
7. 调度队列。
8. 成本预算。

资源隔离不是所有资源完全物理隔开，而是明确租户的保障、上限和超额使用规则。

## 14.4 硬隔离和软隔离

资源隔离可以分成硬隔离和软隔离。

硬隔离：

1. 专属节点。
2. 专属 GPU 资源池。
3. 独立网络。
4. 独立存储 bucket。
5. 独立 Kubernetes 集群。

优点是隔离强，缺点是利用率可能低、成本高。

软隔离：

1. 共享集群。
2. namespace 隔离。
3. 配额限制。
4. 优先级队列。
5. 抢占策略。
6. 逻辑权限控制。

优点是利用率高，缺点是治理复杂。

企业常见做法是混合：生产推理、高敏数据和关键任务硬隔离；普通训练和实验软隔离。

## 14.5 权限隔离

权限隔离回答：谁可以提交任务、访问数据、使用模型、查看日志、修改配置、发布服务？

常见权限对象：

1. 用户。
2. 用户组。
3. Service Account。
4. 训练任务。
5. 推理服务。
6. Agent。
7. 平台管理员。

常见权限动作：

1. submit_job。
2. cancel_job。
3. read_logs。
4. read_dataset。
5. write_checkpoint。
6. deploy_model。
7. access_secret。
8. change_quota。
9. approve_release。

权限模型通常需要 RBAC 和 ABAC 结合。

RBAC 按角色授权，例如 admin、developer、viewer。

ABAC 按属性授权，例如项目、数据等级、环境、任务类型、资源池。

## 14.6 数据隔离

数据隔离是 AI Infra 多租户里最重要的一层。

需要隔离的数据包括：

1. 原始训练数据。
2. 清洗数据。
3. 评估数据。
4. RAG 文档。
5. checkpoint。
6. 模型权重。
7. 日志。
8. trace。
9. prompt。
10. 用户请求。

数据隔离要解决：

1. 谁能读？
2. 谁能写？
3. 谁能复制？
4. 谁能导出？
5. 谁能用于训练？
6. 谁能用于评估？
7. 是否允许跨租户共享？

尤其要注意：训练任务一旦能读数据，就可能把数据记进模型参数、checkpoint 或日志里。

所以数据权限不仅是读文件权限，还要考虑数据使用目的和下游产物权限。

## 14.7 数据分类与使用策略

企业数据通常需要分类。

例如：

1. public。
2. internal。
3. confidential。
4. restricted。
5. regulated。

不同分类对应不同策略：

1. 是否允许训练。
2. 是否允许导出。
3. 是否允许跨区域。
4. 是否需要脱敏。
5. 是否需要审批。
6. 产出的模型是否继承数据等级。

例如使用 restricted 数据训练出的模型，可能也应该被标记为 restricted，不能随便发布到所有租户。

这叫数据等级传播。

## 14.8 网络隔离

网络隔离回答：不同租户的任务能不能互相访问？能不能访问外网？能不能访问生产数据库？

常见手段：

1. Kubernetes NetworkPolicy。
2. VPC / subnet。
3. Service mesh policy。
4. 防火墙。
5. egress 控制。
6. 私有 endpoint。
7. DNS 限制。

AI 任务网络风险包括：

1. 训练任务访问不该访问的数据服务。
2. 容器向外发送敏感数据。
3. 不同租户 Pod 互相访问。
4. 调试端口暴露。
5. 推理服务被非授权调用。

默认策略应该是最小开放，而不是默认互通。

## 14.9 运行时隔离

运行时隔离包括容器、Pod、节点和 GPU 层面的隔离。

需要关注：

1. 容器是否 privileged。
2. 是否挂载宿主机路径。
3. 是否允许 root。
4. 是否共享 IPC / PID namespace。
5. 是否共享 GPU。
6. 是否使用 MIG。
7. 是否隔离本地缓存。
8. 是否限制系统调用。

GPU 多租户尤其复杂。

整卡分配隔离更强，但利用率可能低。

MIG 可以提供硬件级切分，适合部分推理和开发负载。

时间片共享利用率高，但性能隔离和安全隔离较弱。

选择哪种方式要看任务风险和稳定性要求。

## 14.10 镜像和供应链隔离

租户提交的镜像可能不可信。

风险包括：

1. 镜像包含恶意代码。
2. 镜像包含漏洞包。
3. 镜像里写入密钥。
4. 镜像 tag 被覆盖。
5. 镜像来源不明。
6. 基础镜像过旧。

治理措施：

1. 私有镜像仓库。
2. 镜像扫描。
3. 镜像签名。
4. 只允许可信 registry。
5. 基础镜像白名单。
6. 记录 digest。
7. 禁止高风险镜像运行。
8. 构建流水线审计。

多租户平台不能让任何用户随便拉任意镜像在 GPU 节点上运行。

## 14.11 密钥隔离

AI 任务经常需要访问：

1. 数据库。
2. 对象存储。
3. 模型仓库。
4. 外部 API。
5. 评估服务。
6. 私有代码仓库。

这需要密钥。

密钥隔离要避免：

1. 把密钥写进镜像。
2. 把密钥写进代码。
3. 把密钥打印到日志。
4. 多租户共享同一个高权限密钥。
5. 任务结束后密钥仍然有效。

更好的做法：

1. 使用短期凭证。
2. 使用 Service Account。
3. 按任务注入最小权限密钥。
4. 密钥自动轮换。
5. 访问审计。
6. 日志脱敏。

## 14.12 日志、Trace 和可见性隔离

日志和 trace 也可能包含敏感信息。

例如：

1. 训练样本片段。
2. 用户 prompt。
3. 模型输出。
4. 数据路径。
5. 密钥片段。
6. 错误堆栈中的参数。
7. Agent 工具调用结果。

因此日志隔离要控制：

1. 谁能看任务日志。
2. 谁能看推理请求日志。
3. 谁能看 Agent trace。
4. 日志保留多久。
5. 是否需要脱敏。
6. 是否允许导出。

不要默认所有平台管理员都能随便看所有用户数据日志。至少要有审计和最小权限。

## 14.13 成本隔离和归因

多租户平台必须能回答：谁用了多少资源，花了多少钱？

需要归因的资源：

1. GPU 小时。
2. CPU 和内存。
3. 存储容量。
4. 网络流量。
5. 推理 token。
6. checkpoint 存储。
7. 评估任务。
8. Agent 工具调用。

成本归因维度：

1. tenant。
2. team。
3. project。
4. user。
5. job。
6. model。
7. environment。

没有成本隔离，就无法做预算、配额和优化。

## 14.14 生产环境和实验环境隔离

生产推理和实验训练最好隔离。

原因：

1. 生产有 SLO。
2. 实验任务不稳定。
3. 实验镜像可能不安全。
4. 训练任务会占用网络和存储。
5. 生产数据权限更敏感。
6. 变更流程不同。

隔离方式：

1. 独立 namespace。
2. 独立节点池。
3. 独立资源池。
4. 独立网络策略。
5. 独立权限审批。
6. 独立发布流程。

关键生产服务可以使用硬隔离资源池。

## 14.15 跨租户共享

完全隔离并不总是最佳。

有些资源需要共享：

1. 基础模型。
2. 公共数据集。
3. 公共 embedding 服务。
4. 公共评估 benchmark。
5. 基础镜像。
6. 工具平台。

共享要有规则：

1. 谁维护？
2. 谁能使用？
3. 是否只读？
4. 是否可复制？
5. 使用后产物如何定级？
6. 成本如何分摊？

共享不是把权限全打开，而是把共享资源产品化、版本化、权限化。

## 14.16 多租户隔离架构示例

一个企业 AI 平台可以这样设计：

```text
User / Team
  -> Identity Provider
  -> AI Platform Portal / CLI
  -> Policy Engine
  -> Queue / Quota / Scheduler
  -> Kubernetes Namespace / Resource Pool
  -> Data Access Gateway
  -> Secret Manager
  -> Image Registry
  -> Observability / Audit / Cost
```

关键点：

1. 身份统一从 IdP 来。
2. Policy Engine 统一判断权限。
3. Scheduler 管资源配额和队列。
4. Data Access Gateway 管数据访问。
5. Secret Manager 管短期凭证。
6. Image Registry 管可信镜像。
7. Observability 管日志、审计和成本。

## 14.17 多租户隔离审计指标与最小 demo

多租户隔离不能只靠“每个团队一个 namespace”。更工程化的做法是把资源、身份、权限、数据、网络、运行时、镜像、密钥、日志、成本和共享资源都变成可检查证据。

可以把一个租户或任务审计样本写成：

```math
m_i=(r_i,a_i,d_i,n_i,u_i,g_i,k_i,l_i,c_i,o_i,s_i,z_i)
```

其中，`r_i` 是资源与配额，`a_i` 是身份与权限，`d_i` 是数据与 artifact，`n_i` 是网络策略，`u_i` 是运行时安全，`g_i` 是镜像供应链，`k_i` 是密钥范围，`l_i` 是日志和 trace，`c_i` 是成本归因，`o_i` 是生产 / 实验环境边界，`s_i` 是跨租户共享规则，`z_i` 是最终门禁。

统一覆盖率可以写成：

```math
C_j=\frac{1}{N}\sum_{i=1}^{N}\mathbf{1}[g_j(m_i)=1]
```

资源配额占用率可以写成：

```math
Q_u=\frac{G_{u,\mathrm{used}}}{G_{u,\mathrm{quota}}}
```

其中，`G_u_used` 是租户 `u` 当前使用的 GPU，`G_u_quota` 是租户 GPU 配额。

未授权访问拦截率可以写成：

```math
B_{\mathrm{auth}}=\frac{N_{\mathrm{blocked}}}{N_{\mathrm{unauth}}}
```

其中，`N_blocked` 是被策略阻断的未授权访问次数，`N_unauth` 是未授权访问尝试总数。多租户系统里，这个值应接近 1。

数据等级传播可以写成：

```math
L_{\mathrm{artifact}}\ge \max_{d\in D_{\mathrm{train}}} L_d
```

其中，`L_d` 是训练数据等级，`L_artifact` 是 checkpoint、模型权重、embedding index 或 eval report 的等级。直觉是：使用高敏数据训练出的产物不能自动降级。

密钥暴露率可以写成：

```math
R_{\mathrm{secret}}=\frac{N_{\mathrm{secret\_exposed}}}{N_{\mathrm{secret}}}
```

成本归因覆盖率可以写成：

```math
C_{\mathrm{cost}}=\frac{K_{\mathrm{attributed}}}{K_{\mathrm{total}}}
```

最终，多租户隔离门禁可以写成：

```math
G_{\mathrm{tenant}}=\mathbf{1}\left[\min_j C_j\ge \tau_j \land Q_u\le 1 \land B_{\mathrm{auth}}=1 \land R_{\mathrm{secret}}=0 \land C_{\mathrm{cost}}\ge \gamma \land P_0=0\right]
```

其中，`\tau_j` 是每个隔离维度的最低覆盖率，`\gamma` 是成本归因最低覆盖率，`P_0` 是 P0 级隔离风险数量。

下面这个 0 依赖 demo 演示如何把多租户隔离拆成审计规则。它故意构造 1 个完整样本和 16 个坏样本，让每个关键维度各失败一次。

```python
import copy


METRICS = [
    "resource_quota_isolation",
    "identity_namespace_binding",
    "rbac_abac_permission_fit",
    "data_access_boundary",
    "data_classification_lineage",
    "network_policy_isolation",
    "runtime_security_boundary",
    "image_supply_chain_boundary",
    "secret_scope_rotation",
    "logs_trace_redaction",
    "cost_attribution_coverage",
    "prod_experiment_separation",
    "cross_tenant_sharing_governance",
    "audit_evidence_readiness",
    "blast_radius_control",
    "multi_tenant_isolation_gate",
]


DATA_LEVEL = {
    "public": 0,
    "internal": 1,
    "confidential": 2,
    "restricted": 3,
    "regulated": 4,
}


def quota_ratio(used_gpu, quota_gpu):
    return used_gpu / quota_gpu


def unauthorized_block_rate(blocked, attempts):
    return blocked / attempts


def secret_exposure_rate(exposed, total):
    return exposed / total


def cost_attribution_rate(attributed_cost, total_cost):
    return attributed_cost / total_cost


def blast_radius_ratio(affected_tenants, total_tenants):
    return affected_tenants / total_tenants


def max_data_level(levels):
    return max(DATA_LEVEL[level] for level in levels)


def build_multi_tenant_cases():
    complete = {
        "name": "complete",
        "resource": {
            "namespace": "team-a-prod",
            "resource_quota": True,
            "limit_range": True,
            "queue_quota": True,
            "gpu_used": 48,
            "gpu_quota": 64,
            "gpu_type_quota": True,
            "overage_policy": "preemptible-borrow",
        },
        "identity": {
            "idp_group": "team-a",
            "tenant_label": "team-a",
            "namespace_tenant": "team-a",
            "service_account_per_job": True,
            "workload_identity": True,
        },
        "permission": {
            "rbac": True,
            "abac": True,
            "least_privilege": True,
            "wildcard_actions": False,
            "admin_separation": True,
            "approval_for_quota_change": True,
        },
        "data": {
            "tenant_dataset_scope": True,
            "unauthorized_attempts": 5,
            "unauthorized_blocked": 5,
            "purpose_bound": True,
            "export_approval": True,
            "copy_policy": True,
        },
        "classification": {
            "input_levels": ["internal", "restricted"],
            "artifact_level": "restricted",
            "checkpoint_labeled": True,
            "model_labeled": True,
            "eval_report_labeled": True,
            "lineage_recorded": True,
        },
        "network": {
            "default_deny": True,
            "tenant_ingress_policy": True,
            "egress_allowlist": True,
            "prod_data_endpoint_private": True,
            "debug_port_blocked": True,
        },
        "runtime": {
            "privileged": False,
            "host_path": False,
            "run_as_root": False,
            "pod_security_enforced": True,
            "gpu_sharing_policy": "exclusive-or-mig",
            "local_cache_scoped": True,
        },
        "image": {
            "trusted_registry": True,
            "digest_pinned": True,
            "signed": True,
            "scan_passed": True,
            "base_allowlist": True,
            "critical_vulns": 0,
        },
        "secret": {
            "secret_manager": True,
            "tenant_scoped": True,
            "short_lived": True,
            "rotation": True,
            "secrets_exposed": 0,
            "secret_total": 10,
            "log_masking": True,
        },
        "logs": {
            "tenant_log_scope": True,
            "pii_redaction": True,
            "prompt_redaction": True,
            "trace_permission": True,
            "retention_policy": True,
            "export_audit": True,
        },
        "cost": {
            "tenant": "team-a",
            "project": "policy-llm",
            "cost_center": "cc-123",
            "attributed_cost": 9800,
            "total_cost": 10000,
            "gpu_hours_by_job": True,
            "storage_cost_by_artifact": True,
        },
        "environment": {
            "prod_namespace": "team-a-prod",
            "experiment_namespace": "team-a-dev",
            "separate_node_pool": True,
            "separate_release_flow": True,
            "prod_slo_guard": True,
            "experiment_image_review": True,
        },
        "sharing": {
            "shared_resource_owner": True,
            "versioned": True,
            "read_only_by_default": True,
            "copy_requires_approval": True,
            "usage_logged": True,
            "cost_split_rule": True,
        },
        "audit": {
            "policy_decision_log": True,
            "data_access_log": True,
            "secret_access_log": True,
            "image_admission_log": True,
            "cost_ledger": True,
            "reviewable_trace": True,
        },
        "blast_radius": {
            "affected_tenants": 1,
            "total_tenants": 20,
            "max_ratio": 0.1,
            "fault_domain_labeled": True,
            "kill_switch": True,
        },
        "gate": {"enabled": True, "thresholds_ok": True, "p0_risks": 0},
    }

    def bad_case(name, mutator):
        case = copy.deepcopy(complete)
        case["name"] = name
        mutator(case)
        return case

    bad_cases = [
        bad_case("resource_quota_missing_bad", lambda c: c["resource"].update({"resource_quota": False})),
        bad_case("identity_namespace_mismatch_bad", lambda c: c["identity"].update({"namespace_tenant": "team-b"})),
        bad_case("permission_overbroad_bad", lambda c: c["permission"].update({"wildcard_actions": True})),
        bad_case("data_cross_access_bad", lambda c: c["data"].update({"unauthorized_blocked": 3})),
        bad_case("classification_not_propagated_bad", lambda c: c["classification"].update({"artifact_level": "internal"})),
        bad_case("network_default_allow_bad", lambda c: c["network"].update({"default_deny": False})),
        bad_case("runtime_privileged_bad", lambda c: c["runtime"].update({"privileged": True})),
        bad_case("image_untrusted_bad", lambda c: c["image"].update({"signed": False})),
        bad_case("secret_shared_long_lived_bad", lambda c: c["secret"].update({"tenant_scoped": False, "short_lived": False})),
        bad_case("logs_unredacted_bad", lambda c: c["logs"].update({"pii_redaction": False})),
        bad_case("cost_unattributed_bad", lambda c: c["cost"].update({"attributed_cost": 7000})),
        bad_case("prod_experiment_mixed_bad", lambda c: c["environment"].update({"separate_node_pool": False})),
        bad_case("sharing_unversioned_bad", lambda c: c["sharing"].update({"versioned": False})),
        bad_case("audit_missing_bad", lambda c: c["audit"].update({"data_access_log": False})),
        bad_case("blast_radius_large_bad", lambda c: c["blast_radius"].update({"affected_tenants": 6})),
        bad_case("tenant_gate_missing_bad", lambda c: c["gate"].update({"enabled": False})),
    ]
    return [complete] + bad_cases


def check_resource(case):
    resource = case["resource"]
    return (
        resource["resource_quota"]
        and resource["limit_range"]
        and resource["queue_quota"]
        and quota_ratio(resource["gpu_used"], resource["gpu_quota"]) <= 1
        and resource["gpu_type_quota"]
        and resource["overage_policy"] in {"deny", "preemptible-borrow"}
    )


def check_identity(case):
    identity = case["identity"]
    return (
        identity["idp_group"] == identity["tenant_label"]
        and identity["namespace_tenant"] == identity["tenant_label"]
        and identity["service_account_per_job"]
        and identity["workload_identity"]
    )


def check_permission(case):
    permission = case["permission"]
    return (
        permission["rbac"]
        and permission["abac"]
        and permission["least_privilege"]
        and not permission["wildcard_actions"]
        and permission["admin_separation"]
        and permission["approval_for_quota_change"]
    )


def check_data(case):
    data = case["data"]
    return (
        data["tenant_dataset_scope"]
        and unauthorized_block_rate(data["unauthorized_blocked"], data["unauthorized_attempts"]) == 1
        and data["purpose_bound"]
        and data["export_approval"]
        and data["copy_policy"]
    )


def check_classification(case):
    cls = case["classification"]
    return (
        DATA_LEVEL[cls["artifact_level"]] >= max_data_level(cls["input_levels"])
        and cls["checkpoint_labeled"]
        and cls["model_labeled"]
        and cls["eval_report_labeled"]
        and cls["lineage_recorded"]
    )


def check_network(case):
    return all(case["network"].values())


def check_runtime(case):
    runtime = case["runtime"]
    return (
        not runtime["privileged"]
        and not runtime["host_path"]
        and not runtime["run_as_root"]
        and runtime["pod_security_enforced"]
        and runtime["gpu_sharing_policy"] in {"exclusive", "exclusive-or-mig"}
        and runtime["local_cache_scoped"]
    )


def check_image(case):
    image = case["image"]
    return (
        image["trusted_registry"]
        and image["digest_pinned"]
        and image["signed"]
        and image["scan_passed"]
        and image["base_allowlist"]
        and image["critical_vulns"] == 0
    )


def check_secret(case):
    secret = case["secret"]
    return (
        secret["secret_manager"]
        and secret["tenant_scoped"]
        and secret["short_lived"]
        and secret["rotation"]
        and secret_exposure_rate(secret["secrets_exposed"], secret["secret_total"]) == 0
        and secret["log_masking"]
    )


def check_logs(case):
    return all(case["logs"].values())


def check_cost(case):
    cost = case["cost"]
    return (
        bool(cost["tenant"])
        and bool(cost["project"])
        and bool(cost["cost_center"])
        and cost_attribution_rate(cost["attributed_cost"], cost["total_cost"]) >= 0.95
        and cost["gpu_hours_by_job"]
        and cost["storage_cost_by_artifact"]
    )


def check_environment(case):
    env = case["environment"]
    return (
        env["prod_namespace"] != env["experiment_namespace"]
        and env["separate_node_pool"]
        and env["separate_release_flow"]
        and env["prod_slo_guard"]
        and env["experiment_image_review"]
    )


def check_sharing(case):
    return all(case["sharing"].values())


def check_audit(case):
    return all(case["audit"].values())


def check_blast_radius(case):
    blast = case["blast_radius"]
    return (
        blast_radius_ratio(blast["affected_tenants"], blast["total_tenants"]) <= blast["max_ratio"]
        and blast["fault_domain_labeled"]
        and blast["kill_switch"]
    )


def check_gate(case):
    gate = case["gate"]
    return gate["enabled"] and gate["thresholds_ok"] and gate["p0_risks"] == 0


CHECKS = {
    "resource_quota_isolation": check_resource,
    "identity_namespace_binding": check_identity,
    "rbac_abac_permission_fit": check_permission,
    "data_access_boundary": check_data,
    "data_classification_lineage": check_classification,
    "network_policy_isolation": check_network,
    "runtime_security_boundary": check_runtime,
    "image_supply_chain_boundary": check_image,
    "secret_scope_rotation": check_secret,
    "logs_trace_redaction": check_logs,
    "cost_attribution_coverage": check_cost,
    "prod_experiment_separation": check_environment,
    "cross_tenant_sharing_governance": check_sharing,
    "audit_evidence_readiness": check_audit,
    "blast_radius_control": check_blast_radius,
    "multi_tenant_isolation_gate": check_gate,
}


def audit_multi_tenant_isolation(cases):
    case_failures = {}
    for case in cases:
        failures = [name for name, check in CHECKS.items() if not check(case)]
        case_failures[case["name"]] = failures

    metrics = {}
    for name, check in CHECKS.items():
        metrics[name] = round(sum(int(check(case)) for case in cases) / len(cases), 3)

    failed_cases = [name for name, failures in case_failures.items() if failures]
    failed_gates = [name for name, value in metrics.items() if value < 1.0]
    return {
        "metrics": metrics,
        "hard_blocker_count": len(failed_cases),
        "failed_cases": failed_cases,
        "failed_gates": failed_gates,
        "multi_tenant_gate_pass": not failed_cases and min(metrics.values()) >= 0.95,
    }


cases = build_multi_tenant_cases()
case_by_name = {case["name"]: case for case in cases}
complete = case_by_name["complete"]
tenant_examples = {
    "quota_ratio": round(quota_ratio(48, 64), 3),
    "unauthorized_block_rate": round(unauthorized_block_rate(5, 5), 3),
    "artifact_level_ok": DATA_LEVEL["restricted"] >= max_data_level(["internal", "restricted"]),
    "secret_exposure_rate": round(secret_exposure_rate(0, 10), 3),
    "cost_attribution_rate": round(cost_attribution_rate(9800, 10000), 3),
    "blast_radius_ratio": round(blast_radius_ratio(1, 20), 3),
}

smoke = {
    "complete_case_passes": all(check(complete) for check in CHECKS.values()),
    "caught_cross_data_access": not check_data(case_by_name["data_cross_access_bad"]),
    "caught_classification_gap": not check_classification(case_by_name["classification_not_propagated_bad"]),
    "caught_network_default_allow": not check_network(case_by_name["network_default_allow_bad"]),
    "caught_secret_gap": not check_secret(case_by_name["secret_shared_long_lived_bad"]),
    "caught_blast_radius": not check_blast_radius(case_by_name["blast_radius_large_bad"]),
}

audit = audit_multi_tenant_isolation(cases)
print(f"tenant_examples={tenant_examples}")
print(f"smoke={smoke}")
print(f"metrics={audit['metrics']}")
print(f"hard_blocker_count={audit['hard_blocker_count']}")
print(f"failed_cases={audit['failed_cases']}")
print(f"failed_gates={audit['failed_gates']}")
print(f"multi_tenant_gate_pass={audit['multi_tenant_gate_pass']}")
```

一组典型输出是：

```text
tenant_examples={'quota_ratio': 0.75, 'unauthorized_block_rate': 1.0, 'artifact_level_ok': True, 'secret_exposure_rate': 0.0, 'cost_attribution_rate': 0.98, 'blast_radius_ratio': 0.05}
smoke={'complete_case_passes': True, 'caught_cross_data_access': True, 'caught_classification_gap': True, 'caught_network_default_allow': True, 'caught_secret_gap': True, 'caught_blast_radius': True}
metrics={'resource_quota_isolation': 0.941, 'identity_namespace_binding': 0.941, 'rbac_abac_permission_fit': 0.941, 'data_access_boundary': 0.941, 'data_classification_lineage': 0.941, 'network_policy_isolation': 0.941, 'runtime_security_boundary': 0.941, 'image_supply_chain_boundary': 0.941, 'secret_scope_rotation': 0.941, 'logs_trace_redaction': 0.941, 'cost_attribution_coverage': 0.941, 'prod_experiment_separation': 0.941, 'cross_tenant_sharing_governance': 0.941, 'audit_evidence_readiness': 0.941, 'blast_radius_control': 0.941, 'multi_tenant_isolation_gate': 0.941}
hard_blocker_count=16
failed_cases=['resource_quota_missing_bad', 'identity_namespace_mismatch_bad', 'permission_overbroad_bad', 'data_cross_access_bad', 'classification_not_propagated_bad', 'network_default_allow_bad', 'runtime_privileged_bad', 'image_untrusted_bad', 'secret_shared_long_lived_bad', 'logs_unredacted_bad', 'cost_unattributed_bad', 'prod_experiment_mixed_bad', 'sharing_unversioned_bad', 'audit_missing_bad', 'blast_radius_large_bad', 'tenant_gate_missing_bad']
failed_gates=['resource_quota_isolation', 'identity_namespace_binding', 'rbac_abac_permission_fit', 'data_access_boundary', 'data_classification_lineage', 'network_policy_isolation', 'runtime_security_boundary', 'image_supply_chain_boundary', 'secret_scope_rotation', 'logs_trace_redaction', 'cost_attribution_coverage', 'prod_experiment_separation', 'cross_tenant_sharing_governance', 'audit_evidence_readiness', 'blast_radius_control', 'multi_tenant_isolation_gate']
multi_tenant_gate_pass=False
```

这个 demo 的重点是把多租户隔离从“namespace 分组”拆成可验证证据链：资源配额要生效，身份和 namespace 要绑定，RBAC / ABAC 要最小权限，数据访问要按租户和用途阻断，数据等级要传播到 checkpoint / 模型 / eval report，网络默认拒绝，运行时不能 privileged / hostPath / root，镜像要可信，密钥要短期和租户化，日志和 trace 要脱敏，成本要归因，生产和实验要隔离，跨租户共享要有 owner、版本和审计，最终还要能控制故障影响范围。

## 14.18 面试中如何回答多租户隔离

如果面试官问：

```text
如何设计一个多租户 GPU 训练平台？
```

可以这样回答：

```text
我会从资源、权限、数据、网络、运行时、镜像、密钥、审计和成本几个层面设计隔离。

资源层用 namespace、queue、quota、priority 和资源池隔离不同团队，关键生产推理和高敏任务使用独立节点池或硬隔离资源池。调度层支持公平分享、抢占和成本归因。

权限层接入统一身份系统，用 RBAC + ABAC 控制谁能提交任务、访问数据、读取日志、部署模型和修改配额。

数据层按数据分类和项目授权控制访问，训练数据、checkpoint、模型权重、RAG 文档、日志和 trace 都要有权限和审计。使用敏感数据训练出的模型也要继承相应数据等级。

网络层通过 NetworkPolicy、VPC、egress 控制和私有 endpoint 限制租户间互访和外部访问。运行时层限制 privileged、hostPath、root、GPU 共享等高风险配置。

镜像和密钥层使用私有 registry、镜像扫描、签名、digest、Secret Manager 和短期凭证，避免密钥写入镜像或日志。

最后通过审计和成本系统记录谁在什么时候用了什么资源、访问了什么数据、产生了什么模型和成本。
```

## 14.19 常见误区

误区一：namespace 就是多租户隔离。

Namespace 只是逻辑分组，还需要资源、权限、网络、数据、密钥和审计隔离。

误区二：容器就是强安全边界。

容器隔离有限，privileged、hostPath、共享 GPU 和高权限密钥都可能破坏隔离。

误区三：只隔离训练数据就够了。

Checkpoint、模型权重、日志、trace、prompt 和评估结果都可能包含敏感信息。

误区四：共享资源就是所有人可读。

共享需要 owner、版本、权限、审计、成本分摊和使用策略。

误区五：成本归因是财务问题。

成本归因直接影响配额、公平性、资源优化和平台治理，是 AI Infra 的核心能力。

## 14.20 面试题

### 题 1：AI Infra 多租户隔离包括哪些层？

答：包括资源隔离、权限隔离、数据隔离、网络隔离、运行时隔离、镜像和供应链隔离、密钥隔离、日志审计隔离和成本隔离。

### 题 2：为什么数据隔离不只是文件权限？

答：训练任务读取数据后，数据可能进入 checkpoint、模型权重、日志、trace 或评估结果。使用敏感数据训练出的模型也可能继承敏感等级。所以要控制数据使用目的和下游产物权限。

### 题 3：生产推理为什么要和实验训练隔离？

答：生产推理有 SLO 和稳定性要求，实验训练可能不稳定、占用网络和存储、使用未审核镜像或访问不同数据。混用会造成性能干扰和安全风险。

### 题 4：如何避免密钥泄露？

答：不要把密钥写进镜像、代码或日志。使用 Secret Manager、短期凭证、Service Account、最小权限、自动轮换、日志脱敏和访问审计。

### 题 5：硬隔离和软隔离如何选择？

答：高敏数据、生产推理、关键业务和强合规场景适合硬隔离，例如专属资源池或独立集群。普通训练、评估和研究任务可用软隔离，通过 namespace、quota、queue、policy 和审计提高资源利用率。

## 14.21 小练习

练习一：设计一个团队级租户模型。

要求：包含用户、项目、namespace、queue、quota、dataset permission、model permission 和 cost center。

练习二：设计一个数据等级传播规则。

要求：说明 restricted 数据训练出的 checkpoint、模型权重、eval report 应该如何定级和授权。

练习三：排查跨租户数据泄露风险。

假设某用户在日志里看到了其他团队的数据片段，请从日志权限、数据挂载、任务身份、共享目录、trace 和脱敏策略排查。

练习四：设计一个多租户成本报表。

要求：按团队、项目、用户、任务、GPU 类型、存储和推理 token 展示成本。

## 14.22 本章小结

本章讲了 AI Infra 多租户隔离。

你需要掌握：

1. 多租户隔离不是 namespace，而是系统性治理。
2. 资源隔离要覆盖 GPU、CPU、内存、存储、网络、队列、推理 QPS 和成本。
3. 权限隔离需要 RBAC + ABAC。
4. 数据隔离要覆盖训练数据、checkpoint、模型权重、日志、trace、RAG 文档和评估结果。
5. 敏感数据训练出的模型和 artifact 可能需要继承数据等级。
6. 网络隔离要控制租户互访、外部访问和生产数据访问。
7. 运行时隔离要限制 privileged、hostPath、root、共享 GPU 等风险。
8. 镜像、密钥、日志和审计都是多租户安全的重要部分。
9. 成本隔离和归因是配额、公平性和资源优化的基础。

下一章我们会讲集群容量规划：GPU 数量、网络带宽、存储吞吐和利用率。
