# 第 57 章 设计一个模型仓库与发布系统

上一章设计了大模型评估与实验平台。本章继续系统设计题：设计一个模型仓库与发布系统。

模型仓库与发布系统是训练平台、评估平台和推理平台之间的桥梁。训练产出 checkpoint，评估产出报告，发布系统决定哪些模型可以进入线上、如何灰度、如何回滚、如何审计。

先记住一句话：

> 模型仓库与发布系统不是存权重文件的目录，而是模型资产从训练产物到线上服务的版本化、评估、审批、发布和回滚控制系统。

## 57.1 题目理解

面试题可能这样问：

1. 请设计一个企业级模型仓库。
2. 请设计一个支持模型版本管理、评估门禁和发布回滚的系统。
3. 请设计一个从 checkpoint 到 production model 的发布流水线。
4. 如何管理模型权重、tokenizer、adapter、量化版本和发布状态？

核心是模型资产生命周期管理。

## 57.2 需求澄清

先问：

1. 支持哪些模型类型？LLM、embedding、rerank、多模态？
2. 模型 artifact 有哪些？权重、tokenizer、config、adapter、量化版本？
3. 是否需要关联训练任务和数据血缘？
4. 是否需要评估门禁？
5. 是否支持灰度发布和 A/B 测试？
6. 是否支持一键回滚？
7. 是否多租户和权限隔离？
8. 是否需要跨地域分发？
9. 是否需要 artifact 安全校验？
10. 生命周期和存储成本如何管理？

模型仓库和发布系统既是资产系统，也是生产变更系统。

## 57.3 核心目标

系统目标：

1. 管理模型和模型版本。
2. 管理权重、tokenizer、config、adapter、量化版本。
3. 记录模型血缘。
4. 关联 eval report。
5. 支持发布状态机。
6. 支持质量门禁和审批。
7. 支持灰度、A/B 和回滚。
8. 支持权限、安全和审计。
9. 支持 artifact 校验和分发。
10. 支持生命周期和成本治理。

## 57.4 总体架构

可以设计如下：

```text
Training Platform
  -> Model Registry API
  -> Artifact Store
  -> Metadata DB
  -> Lineage Graph
  -> Eval Report Store
  -> Quality Gate
  -> Release Controller
  -> Deployment Controller
  -> Inference Platform
  -> Audit / Cost / Security
```

训练平台注册候选模型，评估平台写入评估报告，发布系统控制 staged、canary、production 和 rollback。

## 57.5 核心对象

核心对象包括：

1. Model。
2. ModelVersion。
3. ModelArtifact。
4. TokenizerVersion。
5. AdapterVersion。
6. QuantizedVersion。
7. EvalReport。
8. ReleaseManifest。
9. Deployment。
10. ApprovalRecord。
11. AuditEvent。

这些对象要有版本、状态、权限和血缘。

## 57.6 Model 和 ModelVersion

Model 是逻辑模型系列。

例如：

```text
company-chat
company-code
company-embedding
```

ModelVersion 是具体版本。

例如：

```text
company-chat:v1.2.0
company-chat:v1.3.0-sft
company-chat:v1.4.0-dpo
```

线上部署必须指向具体 ModelVersion。

不要用 latest 作为生产发布依据。

## 57.7 ModelVersion 元数据

ModelVersion 应记录：

1. model name。
2. version。
3. model type。
4. architecture。
5. parameter size。
6. context length。
7. weight artifact。
8. tokenizer version。
9. model config。
10. training job ID。
11. dataset versions。
12. code version。
13. eval reports。
14. safety level。
15. release state。
16. owner。
17. created time。

这些元数据让模型可追溯、可部署、可审计。

## 57.8 Artifact Store

Artifact Store 保存大文件：

1. 权重分片。
2. tokenizer 文件。
3. config。
4. adapter。
5. 量化权重。
6. deployment package。
7. eval report artifact。

要求：

1. 支持大文件上传下载。
2. 支持 checksum。
3. 支持权限控制。
4. 支持跨地域复制。
5. 支持冷热分层。
6. 支持生命周期管理。

Artifact Store 和 Metadata DB 要分离。

## 57.9 Artifact 校验

模型 artifact 必须校验：

1. 文件是否完整。
2. checksum 是否匹配。
3. 权重格式是否合法。
4. tokenizer 是否存在。
5. config 是否匹配。
6. runtime 是否兼容。
7. 是否通过安全扫描。
8. 是否有许可证风险。

校验失败的模型不能进入发布流程。

不要等推理部署时才发现模型缺文件。

## 57.10 模型血缘

模型仓库要记录血缘：

```text
dataset version + training config + code version
  -> training job
  -> checkpoint
  -> model version
  -> eval report
  -> release manifest
  -> deployment
```

血缘用于：

1. 复现训练。
2. 追踪数据问题影响。
3. 审计模型来源。
4. 分析质量退化。
5. 判断 artifact 是否可删除。

## 57.11 Adapter 和 Merge 管理

LoRA adapter 依赖 base model。

系统要记录：

1. adapter version。
2. base model version。
3. training job。
4. dataset version。
5. merge status。
6. eval report。
7. compatibility constraints。

Merge 后模型要记录：

```text
base model + adapter -> merged model
```

否则后续无法判断某个线上模型如何产生。

## 57.12 量化版本管理

量化版本依赖原始模型。

要记录：

1. source model version。
2. quantization method。
3. calibration dataset。
4. quantization tool version。
5. target runtime。
6. quality eval。
7. latency benchmark。
8. cost improvement。

量化版本不能只看性能，也要看质量和兼容性。

## 57.13 发布状态机

ModelVersion 状态机可以是：

```text
registered -> validated -> evaluated -> approved -> staged -> canary -> production -> deprecated -> archived
```

状态含义：

1. registered：已注册。
2. validated：artifact 校验通过。
3. evaluated：评估完成。
4. approved：审批通过。
5. staged：部署到预发。
6. canary：小流量灰度。
7. production：正式生产。
8. deprecated：不推荐使用。
9. archived：归档。

状态变更必须记录审计。

## 57.14 Quality Gate

Quality Gate 决定模型是否能发布。

门禁规则：

1. eval suite 通过。
2. safety eval 通过。
3. latency benchmark 达标。
4. cost benchmark 达标。
5. tool call success rate 达标。
6. output format 兼容。
7. 关键 slice 不退化。
8. artifact 校验通过。

没有通过门禁的模型不能进入 canary。

## 57.15 审批流程

企业发布通常需要审批。

审批人可能包括：

1. 模型负责人。
2. 业务负责人。
3. 安全负责人。
4. 平台负责人。
5. 合规负责人。

审批记录包括：

1. 审批人。
2. 时间。
3. 结论。
4. 风险说明。
5. 关联 eval report。
6. 回滚方案。

高风险模型发布不能绕过审批。

## 57.16 Release Manifest

ReleaseManifest 描述一次发布用到的完整资产。

包括：

1. model version。
2. tokenizer version。
3. weight artifact。
4. runtime image。
5. runtime config。
6. generation config。
7. safety policy。
8. route policy。
9. eval report。
10. release owner。

回滚时回滚的是 release manifest，而不只是模型权重。

## 57.17 灰度发布

灰度发布支持：

1. 按流量比例。
2. 按租户。
3. 按用户分桶。
4. 按地域。
5. 按业务场景。

灰度过程：

```text
1% -> 5% -> 10% -> 25% -> 50% -> 100%
```

每一步观察质量、安全、延迟、错误率和成本。

异常则暂停或回滚。

## 57.18 A/B 测试

A/B 测试用于比较新旧模型效果。

要支持：

1. 稳定分桶。
2. 实验配置。
3. 指标采集。
4. guardrail。
5. 统计分析。
6. 结果报告。

灰度关注风险控制，A/B 关注效果对比。

两者可以同时使用。

## 57.19 回滚设计

回滚必须快速可靠。

回滚对象包括：

1. model version。
2. tokenizer。
3. runtime config。
4. prompt。
5. route policy。
6. safety policy。
7. generation config。

如果只回滚权重，其他配置不回滚，行为可能仍然异常。

回滚前要确保旧 artifact 仍然可用。

## 57.20 Deployment Controller

Deployment Controller 负责把 release manifest 部署到推理平台。

能力包括：

1. 创建 endpoint。
2. 加载模型。
3. health check。
4. warmup。
5. 注册到 router。
6. 灰度流量调整。
7. 回滚。
8. 摘除异常实例。

部署不是文件复制，而是生产流量接入过程。

## 57.21 权限控制

模型仓库权限包括：

1. 谁能注册模型。
2. 谁能下载权重。
3. 谁能查看 eval report。
4. 谁能发布模型。
5. 谁能回滚。
6. 谁能删除 artifact。
7. 哪些租户能调用模型。

模型权重是高价值资产，必须控制下载和导出。

## 57.22 安全和审计

审计操作包括：

1. 模型注册。
2. artifact 上传。
3. 权重下载。
4. eval report 修改。
5. 审批。
6. 发布。
7. 灰度调整。
8. 回滚。
9. 删除。

安全检查包括：

1. artifact checksum。
2. 权重格式安全。
3. 镜像扫描。
4. secret 检查。
5. 许可证信息。
6. 数据敏感等级继承。

## 57.23 生命周期管理

模型 artifact 很占存储。

生命周期策略：

1. production 版本长期保留。
2. 可回滚版本保留。
3. failed candidate 定期清理。
4. 中间 checkpoint 按策略清理。
5. archived 版本转冷存储。
6. 删除前检查 deployment 和 lineage。

删除模型前必须确认没有线上依赖和合规保留要求。

## 57.24 可观测性

系统要观察：

1. model registration count。
2. artifact validation failure。
3. eval gate pass rate。
4. deployment success rate。
5. canary error rate。
6. rollback count。
7. model load latency。
8. storage growth。
9. download audit events。

发布系统的问题往往会直接影响线上推理。

## 57.25 核心 trade-off

Trade-off 包括：

1. 发布速度 vs 审批安全。
2. 模型版本保留 vs 存储成本。
3. 评估门禁严格 vs 迭代效率。
4. 权重下载便利 vs 模型资产安全。
5. 灰度慢 vs 新模型收益延迟。
6. 自动回滚 vs 指标波动误判。

企业平台要根据风险分层管理。

## 57.26 面试回答模板

可以这样回答：

```text
我会把模型仓库与发布系统设计成模型资产生命周期管理系统。训练平台完成任务后，将 checkpoint 和 metadata 注册到 Model Registry，生成 ModelVersion candidate。系统保存权重、tokenizer、config、adapter、量化版本等 artifact，并做 checksum、格式、安全和 runtime 兼容性校验。

模型仓库记录模型血缘，包括训练任务、数据版本、代码版本、checkpoint、eval report 和 deployment。评估平台完成 eval 后写回 EvalReport，Quality Gate 根据质量、安全、延迟、成本、工具调用和关键 slice 判断是否允许发布。

发布系统通过状态机管理 registered、validated、evaluated、approved、staged、canary、production、deprecated 等状态。每次发布生成 ReleaseManifest，包含模型权重、tokenizer、runtime config、generation config、route policy 和 safety policy。

Deployment Controller 将 release 部署到推理平台，支持灰度、A/B、health check、warmup 和一键回滚。系统还提供权限控制、审计、artifact 生命周期管理、跨地域分发和成本监控。
```

## 57.27 常见扣分点

扣分点一：模型仓库就是对象存储。

问题：忽略元数据、版本、血缘、评估、权限和发布状态。

扣分点二：只管理权重，不管理 tokenizer。

问题：tokenizer 不匹配会导致严重线上问题。

扣分点三：不提评估门禁。

问题：模型发布必须基于质量、安全、延迟和成本判断。

扣分点四：回滚只回滚模型文件。

问题：还要回滚 runtime、prompt、route 和 safety policy。

扣分点五：不提生命周期。

问题：模型和 checkpoint 存储成本非常高。

## 57.28 小练习

1. Model 和 ModelVersion 有什么区别？
2. ReleaseManifest 应包含哪些内容？
3. 为什么 tokenizer 必须和模型版本绑定？
4. 量化版本上线前要评估什么？
5. 模型发布状态机如何设计？
6. Quality Gate 应包含哪些规则？
7. 回滚为什么不能只回滚权重？
8. 删除模型 artifact 前要检查哪些依赖？

## 57.29 本章小结

本章系统设计了模型仓库与发布系统。

你需要记住：

1. 模型仓库管理的不只是权重，还有 tokenizer、config、adapter、量化版本、eval report、权限、血缘和发布状态。
2. 发布系统要通过 Quality Gate、审批、灰度、A/B 和回滚控制线上风险。
3. ReleaseManifest 是可复现发布和可靠回滚的关键。
4. 模型 artifact 要做 checksum、安全校验、runtime 兼容性检查和生命周期管理。
5. 企业级模型发布必须可审计、可追溯、可回滚、可治理。

下一章我们会设计一个 AI Infra 可观测性平台。
