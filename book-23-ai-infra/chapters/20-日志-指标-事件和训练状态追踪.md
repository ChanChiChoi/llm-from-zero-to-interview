# 第 20 章 日志、指标、事件和训练状态追踪

上一章讲了训练配置管理。本章讲训练平台的可观测性：日志、指标、事件和训练状态追踪。

大模型训练经常跑很久、资源很贵、链路很长。没有可观测性，任务失败时你只能看到“挂了”；任务变慢时你不知道是 GPU、网络、数据、checkpoint 还是代码问题；模型效果变化时你也不知道训练过程中发生过什么。

先记住一句话：

> 训练可观测性的目标不是收集更多日志，而是让平台能回答任务现在在哪、是否健康、为什么慢、为什么失败、是否可恢复、成本花在哪里。

## 20.0 本讲资料边界与第二轮精修口径

本讲第二轮精修时，资料口径主要对齐几类公开工程资料：OpenTelemetry 对 traces、metrics、logs、events 和 profiles 等观测信号的抽象；Prometheus 对 counter、gauge、histogram 等指标类型的定义；Kubernetes Job / Pod 生命周期、失败重试和完成状态的控制面语义；MLflow Tracking 对 run、params、metrics、artifacts 和 experiment 的实验追踪口径；PyTorch profiler 对训练算子耗时、显存和长任务 profiling 的性能诊断口径。

这里不绑定某个商业监控系统、日志系统、Dashboard 产品、告警平台或云厂商实现。正文只抽象大模型训练平台里稳定的能力：结构化日志、rank 维度日志、指标类型和维度、生命周期事件、训练状态机、attempt / retry、trace span、实验追踪链接、权限脱敏、留存成本、告警和自动异常检测。换成 Prometheus + Grafana、OpenTelemetry + Jaeger、ELK / Loki、MLflow / TensorBoard、Kubernetes event 或自研控制面时，底层采集方式可以不同，但审计问题是相同的：每个训练任务能否被定位、下钻、关联、恢复、复盘和治理。

第二轮精修重点放在三个方面：

1. 把 logs、metrics、events、traces、profiles 和 experiment tracking 的边界讲清楚，避免把“有 stdout”误认为“可观测”。
2. 给出可审计公式，能量化 rank 日志覆盖、指标维度覆盖、状态迁移合法性、trace 阶段覆盖和 checkpoint 新鲜度。
3. 增加一个 0 依赖 Python demo，把训练可观测性从概念清单变成可以运行的门禁检查。

## 20.1 为什么训练可观测性重要

训练任务不同于普通短任务。

它有几个特点：

1. 运行时间长。
2. 消耗 GPU 成本高。
3. 多节点多进程。
4. 故障原因复杂。
5. 中间状态重要。
6. 结果需要复现。
7. 失败恢复依赖 checkpoint。

没有可观测性，会出现：

1. 任务卡住没人知道。
2. GPU 空转没人发现。
3. loss 异常但继续烧钱。
4. checkpoint 保存失败很久才发现。
5. 某个 rank 慢拖住全部任务。
6. 训练失败后无法定位原因。
7. 实验对比缺少过程证据。

训练平台必须把可观测性作为核心能力，而不是上线后补日志。

## 20.2 日志、指标、事件、Trace 的区别

这几个概念经常混用，需要区分。

日志 logs：描述发生了什么，通常是文本或结构化记录。

指标 metrics：可聚合的数值时间序列，例如 loss、GPU utilization、tokens/s。

事件 events：任务生命周期中的关键状态变化，例如 job scheduled、checkpoint saved、worker failed。

Trace：跨组件、跨阶段的链路追踪，用来串起一次任务从提交到完成的全过程。

简单理解：

```text
Logs: 具体细节
Metrics: 数值趋势
Events: 状态变化
Traces: 因果链路
```

训练平台四者都需要。

## 20.3 训练日志

训练日志包括：

1. stdout / stderr。
2. rank 日志。
3. worker 日志。
4. launcher 日志。
5. dataloader 日志。
6. checkpoint 日志。
7. framework 日志。
8. 错误堆栈。

日志应该包含基本上下文：

1. job_id。
2. attempt_id。
3. node_id。
4. pod_id。
5. rank。
6. timestamp。
7. log_level。
8. component。

如果日志里没有 job_id 和 rank，排查分布式训练会非常痛苦。

## 20.4 Rank 维度日志

分布式训练有很多 rank。

常见问题：rank 0 日志正常，但 rank 17 已经报错。

所以日志系统要支持：

1. 按 job 查看所有日志。
2. 按 rank 过滤日志。
3. 按节点过滤日志。
4. 聚合错误堆栈。
5. 高亮第一个失败 rank。
6. 对 rank 0 主日志特殊展示。

一个实用设计是：默认展示 rank 0 关键日志，但错误排查时自动提示其他 rank 的异常。

## 20.5 日志脱敏和权限

训练日志可能包含敏感信息。

例如：

1. 数据样本片段。
2. 文件路径。
3. 用户输入。
4. API token。
5. 数据库连接串。
6. 模型输出。
7. 业务字段。

平台需要：

1. 日志访问权限。
2. 日志脱敏。
3. 日志保留策略。
4. 日志导出审计。
5. 敏感字段检测。

不要把所有训练日志默认暴露给所有平台用户。

## 20.6 训练指标

训练指标可以分成四类。

### 20.6.1 模型指标

包括：

1. loss。
2. eval loss。
3. perplexity。
4. accuracy。
5. reward。
6. safety score。
7. task-specific metric。

这些指标告诉我们模型是否在学习。

### 20.6.2 性能指标

包括：

1. tokens/s。
2. samples/s。
3. step time。
4. MFU。
5. HFU。
6. dataloader time。
7. forward time。
8. backward time。
9. optimizer time。
10. communication time。

这些指标告诉我们训练是否高效。

### 20.6.3 资源指标

包括：

1. GPU utilization。
2. GPU memory used。
3. GPU memory bandwidth。
4. CPU utilization。
5. host memory。
6. network throughput。
7. storage throughput。
8. disk usage。
9. power。
10. temperature。

这些指标告诉我们资源是否健康。

### 20.6.4 平台指标

包括：

1. queue wait time。
2. scheduling time。
3. image pull time。
4. startup time。
5. checkpoint save time。
6. checkpoint load time。
7. retry count。
8. preemption count。
9. cost。

这些指标告诉我们平台是否高效。

## 20.7 指标采集方式

指标来源包括：

1. 训练脚本主动上报。
2. Framework callback。
3. 日志解析。
4. GPU exporter。
5. Node exporter。
6. DCGM exporter。
7. Kubernetes metrics。
8. 存储和网络监控。
9. 平台控制面事件。

模型指标通常由训练脚本上报。

资源指标通常由 exporter 采集。

平台指标通常由训练平台控制面记录。

成熟平台要把这些指标用 job_id、rank、node、project 关联起来。

## 20.8 指标粒度

指标粒度要平衡成本和可诊断性。

粒度太粗：看不出问题。

粒度太细：存储成本高，查询慢。

常见维度：

1. job_id。
2. attempt_id。
3. rank。
4. node。
5. GPU id。
6. step。
7. timestamp。
8. component。

例如 GPU utilization 只按集群平均看没用，必须能下钻到 job、node、GPU。

loss 曲线要按 step 对齐，而不是只按时间。

## 20.9 事件 Events

事件记录任务生命周期关键变化。

常见事件：

1. JobSubmitted。
2. JobValidated。
3. JobQueued。
4. JobScheduled。
5. ImagePullStarted。
6. ContainerStarted。
7. DistributedInitStarted。
8. TrainingStarted。
9. CheckpointSaved。
10. EvalStarted。
11. WorkerFailed。
12. RetryStarted。
13. JobCompleted。
14. JobFailed。
15. JobCancelled。

事件应该是结构化的。

示例：

```json
{
  "event_type": "CheckpointSaved",
  "job_id": "job_123",
  "attempt_id": "attempt_2",
  "step": 12000,
  "checkpoint_uri": "s3://ckpts/job_123/step_12000",
  "duration_ms": 83000,
  "timestamp": "2026-05-30T12:00:00Z"
}
```

事件比日志更适合做状态追踪和自动化处理。

## 20.10 训练状态机

训练平台应该有状态机。

常见状态：

```text
submitted -> validating -> queued -> scheduled -> starting -> initializing -> running
                                                                  -> checkpointing
                                                                  -> evaluating
                                                                  -> retrying
                                                                  -> completed
                                                                  -> failed
                                                                  -> cancelled
```

状态机价值：

1. 用户知道任务在哪个阶段。
2. 平台知道是否可以重试。
3. 调度系统知道资源是否释放。
4. 运维可以定位卡点。
5. 审计可以复盘任务过程。

没有状态机，训练平台只是日志堆积。

## 20.11 Attempt 和重试追踪

一个 job 可能有多个 attempt。

例如第一次运行节点故障，第二次从 checkpoint 恢复。

需要区分：

1. job_id：逻辑任务。
2. attempt_id：某次运行尝试。
3. run_id：具体执行实例。

为什么重要？

因为指标和日志需要知道属于哪次尝试。

例如 loss 曲线应该能标注：step 8000 后任务失败，attempt 2 从 checkpoint 恢复。

## 20.12 Trace 设计

训练 trace 可以串起整个任务生命周期。

一个 trace 包含：

1. submit span。
2. validate span。
3. queue span。
4. schedule span。
5. image pull span。
6. startup span。
7. distributed init span。
8. training span。
9. checkpoint span。
10. eval span。
11. artifact registration span。

Trace 可以回答：

1. 任务为什么启动慢？
2. 排队花了多久？
3. 镜像拉取花了多久？
4. 分布式初始化卡了多久？
5. checkpoint 保存是否拖慢训练？

训练 trace 不一定要像微服务 trace 那样记录每个函数调用，但要记录关键阶段耗时。

## 20.13 Dashboard 设计

训练 dashboard 应该分层。

任务概览：

1. 当前状态。
2. 运行时长。
3. 队列等待时间。
4. 当前 step。
5. 最近 checkpoint。
6. 当前 loss。
7. GPU 数量。
8. 成本估计。

训练曲线：

1. loss。
2. learning rate。
3. tokens/s。
4. step time。
5. eval metrics。

资源面板：

1. GPU utilization。
2. 显存。
3. CPU。
4. 网络。
5. 存储。

诊断面板：

1. rank 差异。
2. 通信时间。
3. dataloader time。
4. checkpoint time。
5. 错误事件。

如果一个 dashboard 只有 loss 曲线，它不是训练平台 dashboard。

## 20.14 告警设计

训练平台需要告警。

常见告警：

1. loss NaN。
2. loss 长时间不下降。
3. GPU utilization 长时间过低。
4. step time 突然上升。
5. dataloader time 过高。
6. communication time 过高。
7. checkpoint 长时间未成功。
8. worker failed。
9. retry count 过多。
10. 存储空间不足。
11. 成本超过预算。

告警要避免过度打扰。

可以按严重程度分级：

1. info。
2. warning。
3. critical。

例如 loss NaN 是 critical，GPU 利用率略低可能只是 warning。

## 20.15 异常检测

平台可以做一些自动异常检测。

例如：

1. loss spike。
2. step time spike。
3. tokens/s 下降。
4. GPU utilization 异常低。
5. 某个 rank 明显慢。
6. checkpoint 保存时间异常长。
7. 数据读取吞吐下降。

异常检测可以基于：

1. 固定阈值。
2. 同任务历史。
3. 同类任务基线。
4. 滑动窗口统计。
5. 简单规则模型。

不需要一开始就复杂，很多问题用规则就能发现。

## 20.16 日志、指标和实验追踪的关系

实验追踪关注实验结果和配置。

可观测性关注运行过程。

二者要打通。

例如实验追踪中应该能跳转到：

1. 训练日志。
2. loss 曲线。
3. GPU 利用率。
4. checkpoint 事件。
5. 失败原因。
6. 评估报告。

这样分析模型效果时，不只看最终指标，还能看到训练过程是否健康。

## 20.17 数据留存和成本

日志和指标也有成本。

需要设计留存策略：

1. 高频指标保留多久？
2. 聚合指标保留多久？
3. 失败任务日志保留多久？
4. 成功任务日志保留多久？
5. 高敏日志是否更短留存？
6. 是否归档到低成本存储？

常见策略：

1. 原始高频指标保留 7 到 30 天。
2. 降采样指标保留更久。
3. 关键事件长期保留。
4. 发布模型关联日志长期保留。
5. 临时实验日志定期清理。

可观测性也需要生命周期管理。

## 20.18 训练可观测性审计指标与最小 demo

训练可观测性不能只靠“看起来有 Dashboard”。平台至少要能证明：日志有上下文、所有 rank 都可下钻、指标能按 job / attempt / rank / node / step 关联、事件和状态机合法、trace 能覆盖关键阶段、告警能发现高风险异常、日志权限和留存成本可治理。

可以把一次训练可观测性审计样本写成：

```math
o_i=(l_i,r_i,m_i,d_i,e_i,s_i,a_i,t_i,b_i,w_i,x_i,u_i,p_i,k_i,z_i)
```

其中，`l_i` 是结构化日志字段，`r_i` 是 rank 日志覆盖，`m_i` 是指标类型覆盖，`d_i` 是指标维度，`e_i` 是生命周期事件，`s_i` 是状态机路径，`a_i` 是 attempt / retry 记录，`t_i` 是 trace span，`b_i` 是 dashboard 分层，`w_i` 是告警规则，`x_i` 是异常检测规则，`u_i` 是实验追踪链接，`p_i` 是权限脱敏，`k_i` 是成本归因，`z_i` 是最终可观测性门禁。

统一覆盖率仍然可以写成：

```math
C_j=\frac{1}{N}\sum_{i=1}^{N}\mathbf{1}[g_j(o_i)=1]
```

rank 日志覆盖率可以写成：

```math
R_{\mathrm{rank}}=\frac{N_{\mathrm{logged}}}{N_{\mathrm{rank}}}
```

其中，`N_logged` 表示可查询日志的 rank 数，`N_rank` 表示本次任务总 rank 数。对分布式训练来说，`R_rank=1` 还不够，还要能定位第一个失败 rank、慢 rank 和非 rank 0 的异常堆栈。

训练 step time 分解可以写成：

```math
T_{\mathrm{step}}=T_{\mathrm{data}}+T_{\mathrm{fwd}}+T_{\mathrm{bwd}}+T_{\mathrm{comm}}+T_{\mathrm{optim}}+T_{\mathrm{ckpt}}+T_{\mathrm{log}}
```

如果 Dashboard 只有 loss 曲线，没有 `T_data`、`T_comm`、`T_ckpt`、rank skew 和 GPU / network / storage 指标，就很难解释“为什么慢”。

状态机迁移合法性可以写成：

```math
V_{\mathrm{state}}=\frac{1}{K}\sum_{k=1}^{K}\mathbf{1}[(s_{k-1},s_k)\in E_{\mathrm{allowed}}]
```

其中，`E_allowed` 是平台允许的状态迁移集合。例如 `queued -> scheduled -> starting -> initializing -> running` 合法，而 `completed -> running`、`failed -> completed` 或 `running -> queued` 通常说明状态机或事件处理有 bug。

trace 阶段覆盖率可以写成：

```math
R_{\mathrm{span}}=\frac{K_{\mathrm{observed}}}{K_{\mathrm{required}}}
```

其中，`K_required` 是 submit、validate、queue、schedule、image pull、startup、distributed init、training、checkpoint、eval、artifact registration 等必需阶段数量，`K_observed` 是实际 trace 中能看到的阶段数量。

checkpoint 新鲜度可以写成：

```math
A_{\mathrm{ckpt}}=t_{\mathrm{now}}-t_{\mathrm{last}}
```

如果 `A_ckpt` 超过阈值，且任务仍在长时间运行，就要触发告警：这可能意味着 checkpoint 保存失败、存储不可写、rank 卡住或控制面事件丢失。

训练可观测性综合门禁可以写成：

```math
G_{\mathrm{obs}}=\mathbf{1}\left[\min_j C_j\ge \tau_j \land R_{\mathrm{rank}}\ge \gamma \land V_{\mathrm{state}}=1 \land R_{\mathrm{span}}\ge \eta \land A_{\mathrm{ckpt}}\le \tau_{\mathrm{ckpt}} \land P_0=0\right]
```

其中，`P_0` 是 P0 级观测硬阻断数量，例如日志缺 job / attempt / rank、只有 rank 0 日志、状态机非法、trace 断链、日志泄露敏感信息、checkpoint 长时间无成功事件但没有告警。

下面这个 demo 不依赖第三方库。它用一条完整训练任务和 16 条坏样本，模拟训练可观测性门禁如何发现常见问题。

```python
from collections import OrderedDict


REQUIRED_LOG_FIELDS = {
    "job_id",
    "attempt_id",
    "node_id",
    "pod_id",
    "rank",
    "timestamp",
    "level",
    "component",
}

REQUIRED_METRIC_GROUPS = {"model", "performance", "resource", "platform"}
REQUIRED_METRIC_DIMS = {"job_id", "attempt_id", "rank", "node", "step", "timestamp"}
REQUIRED_EVENT_TYPES = {
    "JobSubmitted",
    "JobValidated",
    "JobQueued",
    "JobScheduled",
    "TrainingStarted",
    "CheckpointSaved",
    "WorkerFailed",
    "RetryStarted",
    "JobCompleted",
}
REQUIRED_EVENT_FIELDS = {"event_type", "job_id", "attempt_id", "timestamp"}
ALLOWED_TRANSITIONS = {
    ("submitted", "validating"),
    ("validating", "queued"),
    ("queued", "scheduled"),
    ("scheduled", "starting"),
    ("starting", "initializing"),
    ("initializing", "running"),
    ("running", "checkpointing"),
    ("checkpointing", "running"),
    ("running", "evaluating"),
    ("evaluating", "running"),
    ("running", "retrying"),
    ("retrying", "queued"),
    ("running", "completed"),
    ("running", "failed"),
    ("running", "cancelled"),
}
REQUIRED_TRACE_SPANS = {
    "submit",
    "validate",
    "queue",
    "schedule",
    "image_pull",
    "startup",
    "distributed_init",
    "training",
    "checkpoint",
    "eval",
    "artifact_registration",
}
REQUIRED_DASHBOARD_SECTIONS = {"overview", "training_curve", "resource", "diagnostic", "cost"}
REQUIRED_ALERT_RULES = {
    "loss_nan",
    "gpu_low",
    "step_time_spike",
    "checkpoint_stale",
    "worker_failed",
}
REQUIRED_ANOMALY_RULES = {
    "loss_spike",
    "step_time_spike",
    "rank_straggler",
    "checkpoint_slow",
}
REQUIRED_EXPERIMENT_LINKS = {
    "run_id",
    "params",
    "metrics",
    "artifacts",
    "config_hash",
    "checkpoint_uri",
}
REQUIRED_RETENTION_POLICY = {
    "raw_metrics",
    "downsampled_metrics",
    "failed_logs",
    "release_logs",
    "archive",
}
REQUIRED_PRIVACY_CONTROLS = {"rbac", "redaction", "export_audit", "sensitive_detector"}
REQUIRED_COST_FIELDS = {"gpu_hours", "storage_gib_day", "network_gib", "owner", "project"}


GATE_ORDER = [
    "structured_log_context",
    "rank_log_coverage",
    "metric_taxonomy_coverage",
    "metric_dimension_scope",
    "event_schema_lifecycle",
    "state_machine_validity",
    "attempt_retry_traceability",
    "trace_phase_coverage",
    "dashboard_diagnostic_readiness",
    "alert_rule_readiness",
    "anomaly_detection_rules",
    "experiment_tracking_linkage",
    "retention_cost_governance",
    "privacy_redaction_access",
    "cost_attribution_signal",
    "training_observability_gate",
]


def has_all(values, required):
    return set(values) >= set(required)


def ratio(numerator, denominator):
    if denominator == 0:
        return 0.0
    return numerator / denominator


def rounded(value):
    return round(value, 3)


def transitions_are_valid(states):
    if len(states) < 2:
        return False
    return all((src, dst) in ALLOWED_TRANSITIONS for src, dst in zip(states, states[1:]))


def structured_events_are_valid(events):
    event_types = {event.get("event_type") for event in events}
    fields_ok = all(set(event) >= REQUIRED_EVENT_FIELDS for event in events)
    return fields_ok and event_types >= REQUIRED_EVENT_TYPES


def rank_log_coverage(case):
    ranks = set(case.get("ranks", []))
    logged_ranks = set(case.get("logged_ranks", []))
    if not ranks:
        return 0.0
    return ratio(len(ranks & logged_ranks), len(ranks))


def step_time_breakdown_ms(case):
    return sum(case["step_breakdown_ms"].values())


def checkpoint_age_min(case):
    return case["now_min"] - case["last_checkpoint_min"]


def audit_one(case):
    gates = OrderedDict()
    gates["structured_log_context"] = has_all(case["log_fields"], REQUIRED_LOG_FIELDS)
    gates["rank_log_coverage"] = (
        rank_log_coverage(case) >= 0.99
        and set(case["error_ranks_detected"]) >= set(case["error_ranks"])
    )
    gates["metric_taxonomy_coverage"] = has_all(case["metric_groups"], REQUIRED_METRIC_GROUPS)
    gates["metric_dimension_scope"] = has_all(case["metric_dimensions"], REQUIRED_METRIC_DIMS)
    gates["event_schema_lifecycle"] = structured_events_are_valid(case["events"])
    gates["state_machine_validity"] = transitions_are_valid(case["state_events"])
    gates["attempt_retry_traceability"] = bool(case["attempt_id"]) and case["retry_policy_recorded"]
    gates["trace_phase_coverage"] = has_all(case["trace_spans"], REQUIRED_TRACE_SPANS)
    gates["dashboard_diagnostic_readiness"] = has_all(
        case["dashboard_sections"], REQUIRED_DASHBOARD_SECTIONS
    )
    gates["alert_rule_readiness"] = has_all(case["alert_rules"], REQUIRED_ALERT_RULES)
    gates["anomaly_detection_rules"] = has_all(case["anomaly_rules"], REQUIRED_ANOMALY_RULES)
    gates["experiment_tracking_linkage"] = has_all(
        case["experiment_links"], REQUIRED_EXPERIMENT_LINKS
    )
    gates["retention_cost_governance"] = has_all(
        case["retention_policy"], REQUIRED_RETENTION_POLICY
    )
    gates["privacy_redaction_access"] = has_all(case["privacy_controls"], REQUIRED_PRIVACY_CONTROLS)
    gates["cost_attribution_signal"] = has_all(case["cost_fields"], REQUIRED_COST_FIELDS)
    gates["training_observability_gate"] = bool(case["observability_gate"])

    return {
        "name": case["name"],
        "gates": gates,
        "rank_log_coverage": rounded(rank_log_coverage(case)),
        "trace_phase_coverage": rounded(ratio(len(set(case["trace_spans"])), len(REQUIRED_TRACE_SPANS))),
        "state_transitions_valid": transitions_are_valid(case["state_events"]),
        "step_time_ms": rounded(step_time_breakdown_ms(case)),
        "checkpoint_age_min": checkpoint_age_min(case),
    }


def clone(base, name, **changes):
    copied = {}
    for key, value in base.items():
        if isinstance(value, list):
            copied[key] = list(value)
        elif isinstance(value, dict):
            copied[key] = dict(value)
        elif isinstance(value, set):
            copied[key] = set(value)
        else:
            copied[key] = value
    copied["name"] = name
    copied.update(changes)
    return copied


complete_case = {
    "name": "complete_case",
    "job_id": "job_train_obs_001",
    "attempt_id": "attempt_2",
    "log_fields": REQUIRED_LOG_FIELDS,
    "ranks": list(range(8)),
    "logged_ranks": list(range(8)),
    "error_ranks": [6],
    "error_ranks_detected": [6],
    "metric_groups": REQUIRED_METRIC_GROUPS,
    "metric_dimensions": REQUIRED_METRIC_DIMS,
    "events": [
        {
            "event_type": event_type,
            "job_id": "job_train_obs_001",
            "attempt_id": "attempt_2",
            "timestamp": "2026-06-12T12:00:00Z",
            "step": 1200,
        }
        for event_type in REQUIRED_EVENT_TYPES
    ],
    "state_events": [
        "submitted",
        "validating",
        "queued",
        "scheduled",
        "starting",
        "initializing",
        "running",
        "checkpointing",
        "running",
        "evaluating",
        "running",
        "completed",
    ],
    "retry_policy_recorded": True,
    "trace_spans": REQUIRED_TRACE_SPANS,
    "dashboard_sections": REQUIRED_DASHBOARD_SECTIONS,
    "alert_rules": REQUIRED_ALERT_RULES,
    "anomaly_rules": REQUIRED_ANOMALY_RULES,
    "experiment_links": REQUIRED_EXPERIMENT_LINKS,
    "retention_policy": REQUIRED_RETENTION_POLICY,
    "privacy_controls": REQUIRED_PRIVACY_CONTROLS,
    "cost_fields": REQUIRED_COST_FIELDS,
    "observability_gate": True,
    "now_min": 240,
    "last_checkpoint_min": 228,
    "step_breakdown_ms": {
        "data": 180.0,
        "fwd": 260.0,
        "bwd": 520.0,
        "comm": 310.0,
        "optim": 90.0,
        "ckpt": 30.0,
        "log": 10.0,
    },
}

cases = [
    complete_case,
    clone(complete_case, "log_context_missing_bad", log_fields={"job_id", "timestamp", "level"}),
    clone(
        complete_case,
        "rank0_only_bad",
        logged_ranks=[0],
        error_ranks=[3],
        error_ranks_detected=[],
    ),
    clone(complete_case, "metric_taxonomy_missing_bad", metric_groups={"model"}),
    clone(
        complete_case,
        "metric_dimension_unscoped_bad",
        metric_dimensions={"job_id", "timestamp"},
    ),
    clone(
        complete_case,
        "event_schema_unstructured_bad",
        events=[{"event_type": "WorkerFailed", "message": "rank 3 exited"}],
    ),
    clone(complete_case, "state_machine_invalid_bad", state_events=["submitted", "running", "queued"]),
    clone(complete_case, "attempt_id_missing_bad", attempt_id="", retry_policy_recorded=False),
    clone(complete_case, "trace_phase_missing_bad", trace_spans={"submit", "training", "eval"}),
    clone(complete_case, "dashboard_loss_only_bad", dashboard_sections={"training_curve"}),
    clone(complete_case, "alert_rule_missing_bad", alert_rules={"loss_nan"}),
    clone(complete_case, "anomaly_rule_missing_bad", anomaly_rules=set()),
    clone(complete_case, "experiment_tracking_unlinked_bad", experiment_links={"run_id"}),
    clone(complete_case, "retention_policy_missing_bad", retention_policy=set()),
    clone(complete_case, "privacy_redaction_missing_bad", privacy_controls={"rbac"}),
    clone(complete_case, "cost_attribution_missing_bad", cost_fields={"owner"}),
    clone(complete_case, "observability_gate_missing_bad", observability_gate=False),
]

audits = [audit_one(case) for case in cases]
metrics = OrderedDict()
for gate_name in GATE_ORDER:
    passed = sum(1 for audit in audits if audit["gates"][gate_name])
    metrics[gate_name] = rounded(ratio(passed, len(audits)))

failed_cases = [
    audit["name"]
    for audit in audits
    if not all(audit["gates"].values())
]
failed_gates = [name for name, value in metrics.items() if value < 1.0]

complete_audit = audits[0]
examples = {
    "rank_log_coverage": complete_audit["rank_log_coverage"],
    "trace_phase_coverage": complete_audit["trace_phase_coverage"],
    "state_transitions_valid": complete_audit["state_transitions_valid"],
    "step_time_ms": complete_audit["step_time_ms"],
    "checkpoint_age_min": complete_audit["checkpoint_age_min"],
    "linked_run_id": "run_train_obs_001",
}
smoke = {
    "complete_case_passes": all(complete_audit["gates"].values()),
    "caught_rank0_only": "rank0_only_bad" in failed_cases,
    "caught_invalid_state": "state_machine_invalid_bad" in failed_cases,
    "caught_missing_trace": "trace_phase_missing_bad" in failed_cases,
    "caught_privacy_gap": "privacy_redaction_missing_bad" in failed_cases,
    "caught_gate_gap": "observability_gate_missing_bad" in failed_cases,
}

print("training_observability_examples=", examples, sep="")
print("smoke=", smoke, sep="")
print("metrics=", dict(metrics), sep="")
print("hard_blocker_count=", len(failed_cases), sep="")
print("failed_cases=", failed_cases, sep="")
print("failed_gates=", failed_gates, sep="")
print("training_observability_gate_pass=", all(value >= 0.99 for value in metrics.values()), sep="")
```

一组期望输出类似：

```text
training_observability_examples={'rank_log_coverage': 1.0, 'trace_phase_coverage': 1.0, 'state_transitions_valid': True, 'step_time_ms': 1400.0, 'checkpoint_age_min': 12, 'linked_run_id': 'run_train_obs_001'}
smoke={'complete_case_passes': True, 'caught_rank0_only': True, 'caught_invalid_state': True, 'caught_missing_trace': True, 'caught_privacy_gap': True, 'caught_gate_gap': True}
metrics={'structured_log_context': 0.941, 'rank_log_coverage': 0.941, 'metric_taxonomy_coverage': 0.941, 'metric_dimension_scope': 0.941, 'event_schema_lifecycle': 0.941, 'state_machine_validity': 0.941, 'attempt_retry_traceability': 0.941, 'trace_phase_coverage': 0.941, 'dashboard_diagnostic_readiness': 0.941, 'alert_rule_readiness': 0.941, 'anomaly_detection_rules': 0.941, 'experiment_tracking_linkage': 0.941, 'retention_cost_governance': 0.941, 'privacy_redaction_access': 0.941, 'cost_attribution_signal': 0.941, 'training_observability_gate': 0.941}
hard_blocker_count=16
failed_cases=['log_context_missing_bad', 'rank0_only_bad', 'metric_taxonomy_missing_bad', 'metric_dimension_unscoped_bad', 'event_schema_unstructured_bad', 'state_machine_invalid_bad', 'attempt_id_missing_bad', 'trace_phase_missing_bad', 'dashboard_loss_only_bad', 'alert_rule_missing_bad', 'anomaly_rule_missing_bad', 'experiment_tracking_unlinked_bad', 'retention_policy_missing_bad', 'privacy_redaction_missing_bad', 'cost_attribution_missing_bad', 'observability_gate_missing_bad']
failed_gates=['structured_log_context', 'rank_log_coverage', 'metric_taxonomy_coverage', 'metric_dimension_scope', 'event_schema_lifecycle', 'state_machine_validity', 'attempt_retry_traceability', 'trace_phase_coverage', 'dashboard_diagnostic_readiness', 'alert_rule_readiness', 'anomaly_detection_rules', 'experiment_tracking_linkage', 'retention_cost_governance', 'privacy_redaction_access', 'cost_attribution_signal', 'training_observability_gate']
training_observability_gate_pass=False
```

这个 demo 想说明：训练可观测性不是日志平台、指标平台、trace 平台或实验平台中的某一个，而是把它们通过 `job_id`、`attempt_id`、rank、step、状态机、checkpoint、run 和 artifact 串成同一条证据链。面试时如果能把“慢、挂、贵、不可恢复、不可复现”都落到这组证据链上，回答就会比只列工具名扎实很多。

## 20.19 面试中如何回答训练可观测性

如果面试官问：

```text
如何设计大模型训练平台的日志、指标和状态追踪？
```

可以这样回答：

```text
我会把训练可观测性分成 logs、metrics、events 和 traces。

Logs 用于排查具体错误，需要按 job、attempt、node、pod、rank、component 结构化采集，并支持 rank 维度查询和日志脱敏。

Metrics 分为模型指标、性能指标、资源指标和平台指标。模型指标包括 loss、eval score；性能指标包括 tokens/s、step time、MFU、communication time、dataloader time；资源指标包括 GPU utilization、显存、网络和存储；平台指标包括排队时间、启动时间、checkpoint 保存时间和重试次数。

Events 记录任务生命周期，比如 submitted、validated、queued、scheduled、started、checkpoint_saved、worker_failed、retry_started、completed。Trace 串起提交、校验、排队、调度、启动、初始化、训练、checkpoint 和 artifact 注册等阶段耗时。

平台需要训练状态机和 attempt 概念，区分逻辑 job 和多次运行尝试。Dashboard 要展示任务状态、训练曲线、资源使用、rank 差异、通信、数据加载和 checkpoint。告警覆盖 loss NaN、GPU 低利用、step time 上升、checkpoint 长时间失败和 worker failure。最后还要做日志权限、脱敏和留存成本管理。
```

## 20.20 常见误区

误区一：可观测性就是收集 stdout。

stdout 只是日志的一部分，还需要指标、事件、trace 和状态机。

误区二：只看 rank 0 日志就够。

分布式训练中错误可能发生在任意 rank。

误区三：有 loss 曲线就能判断训练健康。

还要看吞吐、step time、GPU、通信、数据加载和 checkpoint。

误区四：日志越多越好。

日志太多会增加成本和噪声，还可能泄露敏感信息。

误区五：成功任务不需要保留事件。

事件是审计、复现和性能分析的重要依据。

## 20.21 面试题

### 题 1：logs、metrics、events、traces 有什么区别？

答：logs 记录具体细节，metrics 是可聚合的数值时间序列，events 记录任务生命周期状态变化，traces 串起跨组件和阶段的因果链路。训练平台四者都需要。

### 题 2：训练平台应该采集哪些关键指标？

答：包括模型指标 loss、eval score；性能指标 tokens/s、step time、MFU、communication time、dataloader time；资源指标 GPU utilization、显存、CPU、网络、存储；平台指标 queue time、startup time、checkpoint time、retry count 和 cost。

### 题 3：为什么需要 attempt_id？

答：同一个 job 可能失败后重试或从 checkpoint 恢复。attempt_id 区分不同运行尝试，帮助关联日志、指标、事件和恢复过程，避免把多次运行混在一起。

### 题 4：训练 dashboard 应该包含什么？

答：应包含任务状态、运行时间、当前 step、最近 checkpoint、loss 和 learning rate 曲线、tokens/s、step time、GPU 和显存、rank 差异、通信时间、dataloader time、checkpoint time、错误事件和成本估计。

### 题 5：为什么日志需要权限和脱敏？

答：训练日志可能包含样本内容、用户输入、模型输出、路径、密钥片段和业务字段。如果不控制权限和脱敏，可能造成数据泄露。

## 20.22 小练习

练习一：设计一个训练任务事件 schema。

要求：包含 event_type、job_id、attempt_id、rank、step、timestamp、message 和 metadata。

练习二：设计一个训练 dashboard。

要求：分为任务概览、训练曲线、资源面板和诊断面板。

练习三：分析一个训练变慢问题。

假设 step time 从 1.2s 增加到 2.5s，请从 dataloader、通信、checkpoint、GPU、网络和存储指标排查。

练习四：设计日志留存策略。

要求：区分高频原始指标、降采样指标、失败任务日志、发布模型关联日志和临时实验日志。

## 20.23 本章小结

本章讲了日志、指标、事件和训练状态追踪。

你需要掌握：

1. 训练可观测性要回答任务在哪、是否健康、为什么慢、为什么失败、是否可恢复和成本花在哪里。
2. Logs、metrics、events、traces 各自解决不同问题。
3. 分布式训练日志要支持 rank、node、pod、attempt 维度查询。
4. 指标应覆盖模型、性能、资源和平台四类。
5. 事件和状态机用于追踪任务生命周期。
6. Attempt 用于区分同一 job 的多次运行尝试。
7. Trace 可以分析提交、排队、调度、启动、训练、checkpoint 等阶段耗时。
8. Dashboard 要同时展示训练曲线、资源、诊断和成本。
9. 告警和异常检测能减少 GPU 空烧和训练事故。
10. 日志、指标和事件也需要权限、脱敏和留存策略。

下一章我们会讲 Checkpoint 策略：频率、保留、异步保存和断点恢复。
