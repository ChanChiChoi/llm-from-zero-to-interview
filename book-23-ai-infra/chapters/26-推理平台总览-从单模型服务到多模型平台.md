# 第 26 章 推理平台总览：从单模型服务到多模型平台

前面第三部分讲完了大模型训练平台。从本章开始进入第四部分：大模型推理平台工程。

训练平台解决“模型怎么训出来”，推理平台解决“模型怎么稳定、低延迟、低成本地服务线上请求”。推理平台和训练平台同样重要，因为模型效果再好，如果推理延迟高、吞吐低、成本失控、版本不可回滚，也无法真正落地。

先记住一句话：

> 大模型推理平台不是把模型包成一个 HTTP 服务，而是围绕延迟、吞吐、并发、成本、版本、安全和可用性构建的在线模型服务系统。

## 26.0 本讲资料边界与第二轮精修口径

本讲按 `WRITING_PLAN.md` 做第二轮精修时，重点参考 vLLM 对 PagedAttention、KV cache block 和 continuous batching 的公开口径，Hugging Face TGI 对 streaming、Prometheus / OpenTelemetry 指标、continuous batching 和生产推理服务的口径，NVIDIA Triton Inference Server 对 model repository、dynamic batching、metrics 和多模型推理服务的口径，MLflow Model Registry 对模型版本、stage / alias、artifact 管理的口径，以及 OpenTelemetry 对 traces、metrics、logs 等 signals 的通用可观测性边界。

这些资料说明：推理平台不是单一 runtime，也不是 Kubernetes 上的一个 Deployment。runtime 解决高性能执行，平台解决从请求进入到 token 返回的完整生命周期、模型版本治理、SLO、灰度、限流、缓存、观测、安全和成本归因。

本章只讲稳定抽象，不绑定某个框架版本或厂商实现。你可以用 vLLM、TGI、Triton、TensorRT-LLM、SGLang 或自研 runtime，但面试回答必须能解释以下边界：

1. 请求生命周期：认证、限流、路由、排队、prefill、decode、streaming、计费和审计。
2. 性能指标：TTFT、TPOT、P95 / P99、tokens/s、queue time、KV cache pressure 和错误率。
3. 模型治理：Model Registry、ModelVersion、Endpoint、Deployment、RoutePolicy、ScalingPolicy 和 RolloutPolicy。
4. 运行时职责：模型加载、显存管理、KV cache、continuous batching、streaming 和 runtime metrics。
5. 平台职责：SLO、灰度、回滚、限流、降级、安全策略、成本归因和开发者自助。

## 26.1 从单模型服务开始

最简单的推理服务长这样：

```text
Client -> HTTP API -> Model Server -> GPU -> Response
```

一个模型，一个服务，一个 GPU 或一组 GPU。

这种方式适合：

1. Demo。
2. 小规模内部服务。
3. 单一模型场景。
4. 流量很低的实验。

但生产环境很快会遇到问题：

1. 多个模型如何部署？
2. 请求如何路由到合适模型？
3. 高峰流量如何扩容？
4. 延迟如何控制？
5. GPU 利用率如何提高？
6. 模型版本如何灰度和回滚？
7. 成本如何统计？
8. 某个模型故障如何降级？

这就需要推理平台。

## 26.2 推理平台要解决什么问题

推理平台要解决十个核心问题：

1. 模型部署。
2. 请求接入。
3. 模型路由。
4. 推理运行时。
5. batching 和队列调度。
6. KV cache 管理。
7. 自动扩缩容。
8. 限流、熔断、超时和降级。
9. 灰度、A/B 测试和回滚。
10. 监控、安全和成本治理。

这些问题组合起来，才是完整推理平台。

## 26.3 推理和训练的差异

训练关注：

1. 长时间运行。
2. 吞吐。
3. checkpoint。
4. 分布式通信。
5. 收敛稳定性。
6. 实验可复现。

推理关注：

1. 在线延迟。
2. QPS。
3. 并发。
4. 可用性。
5. 弹性扩缩容。
6. 成本。
7. 版本发布。
8. 用户体验。

训练任务可以排队几小时，线上推理请求不行。

训练失败可以从 checkpoint 恢复，推理服务故障要快速切流或降级。

所以推理平台是在线服务系统，不是训练平台的附属品。

## 26.4 推理平台总体架构

一个典型推理平台架构：

```text
Client / App / Agent
  -> API Gateway
  -> Auth / Rate Limit / Safety
  -> Model Router
  -> Request Queue / Scheduler
  -> Inference Runtime
      -> Model Worker Pods
      -> GPU / KV Cache / Batching
  -> Model Registry
  -> Cache Layer
  -> Observability: Metrics / Logs / Traces
  -> Autoscaler
  -> Deployment Controller
  -> Cost / Audit
```

模块说明：

1. API Gateway：统一入口、认证、限流。
2. Safety：输入输出安全策略。
3. Model Router：选择模型、版本和实例。
4. Request Queue：请求排队和 batching。
5. Inference Runtime：执行 prefill、decode 和生成。
6. Model Registry：管理模型版本、权重、tokenizer 和配置。
7. Cache Layer：prompt cache、KV cache、语义缓存和结果缓存。
8. Observability：TTFT、TPOT、QPS、错误率、GPU、成本。
9. Autoscaler：自动扩缩容。
10. Deployment Controller：灰度、回滚、A/B 测试。
11. Cost / Audit：成本和访问审计。

## 26.5 推理平台的核心指标

推理平台常见指标：

1. QPS。
2. 并发数。
3. TTFT。
4. TPOT。
5. end-to-end latency。
6. p50 / p95 / p99 latency。
7. input tokens/s。
8. output tokens/s。
9. GPU utilization。
10. KV cache utilization。
11. queue length。
12. error rate。
13. timeout rate。
14. cost per 1k tokens。

TTFT 是 Time To First Token。

TPOT 是 Time Per Output Token。

这两个指标非常重要。用户体感通常受 TTFT 和后续 token 生成速度共同影响。

## 26.6 Prefill 和 Decode

大模型推理可以粗略分为两个阶段。

Prefill：处理输入 prompt，计算所有输入 token 的上下文表示，生成 KV cache。

Decode：逐 token 生成输出，每一步使用已有 KV cache 生成下一个 token。

两阶段资源画像不同：

1. Prefill 更偏计算密集。
2. Decode 更偏显存带宽和 KV cache 访问。
3. 长 prompt 会拉高 prefill 成本。
4. 长输出会拉高 decode 成本。

推理平台必须理解 prefill 和 decode，否则无法解释延迟和吞吐。

## 26.7 KV Cache 为什么关键

KV cache 保存历史 token 的 Key 和 Value。它能避免每次生成 token 都重新计算全部上下文。

但 KV cache 会占用大量显存，尤其在：

1. 长上下文。
2. 高并发。
3. 大 batch。
4. 多层大模型。
5. 多用户长对话。

KV cache 管不好，会导致：

1. 显存不足。
2. 请求被驱逐。
3. 延迟抖动。
4. GPU 利用率下降。
5. p99 延迟变差。

因此现代推理 runtime 会重点优化 KV cache，例如分页管理、复用、驱逐、前缀缓存和分离式 prefill/decode。

## 26.8 从单模型服务到多模型平台

单模型服务只需要回答：这个模型能不能跑起来？

多模型平台要回答更多问题：

1. 哪个业务能调用哪些模型？
2. 哪个模型版本是默认版本？
3. 哪些请求走大模型，哪些请求走小模型？
4. 哪些请求需要更强模型，哪些请求可以降级？
5. 模型服务异常时如何切换？
6. 不同模型的成本如何归因？
7. 模型更新是否影响线上质量？

多模型平台的核心抽象通常包括：

1. Model。
2. ModelVersion。
3. Endpoint。
4. Deployment。
5. Runtime。
6. RoutePolicy。
7. ScalingPolicy。
8. QuotaPolicy。
9. SafetyPolicy。
10. CostPolicy。

这些抽象让平台从“部署一个服务”升级为“管理一组模型能力”。

## 26.9 Model Registry 的作用

Model Registry 管理模型资产。

它通常记录：

1. 模型名称。
2. 模型版本。
3. 权重路径。
4. tokenizer。
5. 推理配置。
6. 上下文长度。
7. 支持的任务类型。
8. 量化方式。
9. 依赖 runtime。
10. 安全等级。
11. 评估结果。
12. 发布状态。

没有 Model Registry，推理平台很容易变成一堆没人说得清的服务。

## 26.10 Model Router 的作用

Model Router 负责决定请求去哪。

常见路由依据：

1. 模型名称。
2. 模型版本。
3. 用户或租户。
4. 业务线。
5. 请求类型。
6. prompt 长度。
7. 成本预算。
8. 延迟 SLO。
9. 安全策略。
10. 当前实例负载。

路由不是简单的负载均衡。大模型请求之间差异巨大，一个 200 token 请求和一个 100k token 请求不是同一种负载。

## 26.11 Runtime 的作用

Inference Runtime 是真正执行推理的组件。

常见 runtime 包括：

1. Triton。
2. vLLM。
3. TGI。
4. SGLang。
5. TensorRT-LLM。
6. 自研 runtime。

Runtime 负责：

1. 加载模型权重。
2. 管理显存。
3. 处理 tokenizer。
4. 执行 prefill。
5. 执行 decode。
6. 管理 KV cache。
7. 支持 batching。
8. 支持 streaming response。
9. 暴露指标。

平台不要把所有逻辑塞进 runtime。更合理的方式是：平台负责治理和编排，runtime 负责高性能执行。

## 26.12 Batching 为什么重要

大模型推理如果一个请求一个请求跑，GPU 利用率通常很低。

Batching 把多个请求合并执行，可以提高吞吐。

但 batching 会带来延迟权衡：

1. batch 越大，吞吐越高。
2. 等待 batch 的时间越长，TTFT 可能越差。
3. 请求长度差异越大，padding 或调度浪费越明显。
4. 长请求可能拖慢短请求。

所以推理平台常常需要 continuous batching，而不是静态 batch。

Continuous batching 允许新请求在 decode 过程中动态加入，也允许完成的请求动态退出，更适合生成式模型。

## 26.13 自动扩缩容不是只看 GPU 利用率

传统服务扩缩容经常看 CPU 或 QPS。

大模型推理不够简单。

常见扩缩容信号包括：

1. QPS。
2. 并发请求数。
3. 队列长度。
4. TTFT。
5. p95 / p99 延迟。
6. output tokens/s。
7. GPU 利用率。
8. 显存占用。
9. KV cache 占用。
10. 失败率和超时率。

只看 GPU 利用率可能误判。比如 decode 阶段可能 GPU 利用率不高，但显存带宽或 KV cache 已经成为瓶颈。

## 26.14 冷启动和热启动

大模型服务冷启动很慢，因为需要：

1. 拉取镜像。
2. 加载模型权重。
3. 初始化 runtime。
4. 分配显存。
5. 加载 tokenizer。
6. 预热 kernel。

冷启动可能耗时几十秒到数分钟。

常见优化方法：

1. 权重本地缓存。
2. 镜像预拉取。
3. 常用模型保留 warm pool。
4. runtime 预热。
5. 按业务峰谷提前扩容。
6. 权重格式优化。

推理平台的扩缩容不能只考虑“要不要扩”，还要考虑“扩出来多久能服务”。

## 26.15 灰度、A/B 测试和回滚

模型上线比普通服务上线更复杂。

因为模型版本变化会影响：

1. 输出质量。
2. 风格。
3. 安全性。
4. 延迟。
5. 成本。
6. 工具调用行为。
7. 下游业务指标。

推理平台需要支持：

1. 按流量比例灰度。
2. 按租户灰度。
3. 按用户分桶。
4. 按场景灰度。
5. 指标对比。
6. 一键回滚。

没有灰度和回滚，模型发布风险会非常高。

## 26.16 限流、熔断、超时和降级

推理平台必须保护自己。

常见保护机制：

1. 租户级限流。
2. 模型级限流。
3. endpoint 级限流。
4. 最大上下文长度限制。
5. 最大输出长度限制。
6. 排队超时。
7. 请求执行超时。
8. 熔断异常模型。
9. 降级到小模型。
10. 降级到缓存结果。

大模型推理成本高，一个异常客户端可能迅速打满 GPU 资源。所以限流和配额不是附加功能，而是平台基本能力。

## 26.17 安全和治理

推理平台安全至少包括：

1. 身份认证。
2. 租户鉴权。
3. 模型访问控制。
4. 输入内容策略。
5. 输出内容策略。
6. 敏感信息保护。
7. 日志脱敏。
8. 密钥保护。
9. 审计追踪。
10. 数据留存策略。

推理请求可能包含用户隐私、企业知识、代码、合同、财务数据和业务决策信息。平台必须明确哪些数据可以记录，哪些数据不能记录，哪些数据需要脱敏。

## 26.18 成本治理

推理成本通常由这些因素决定：

1. 模型大小。
2. 输入 token 数。
3. 输出 token 数。
4. 并发量。
5. GPU 单价。
6. GPU 利用率。
7. cache 命中率。
8. batch 效率。
9. 量化方式。
10. SLO 要求。

成本治理常见手段：

1. 小模型优先。
2. 分层模型路由。
3. 语义缓存。
4. prompt 压缩。
5. 输出长度控制。
6. 低优先级请求降级。
7. 离峰批处理。
8. endpoint 成本归因。

一个成熟推理平台必须能回答：哪个业务、哪个模型、哪个版本、哪类请求消耗了多少成本。

## 26.19 一个完整请求流程

一个线上请求的完整流程可能是：

1. Client 发起请求。
2. API Gateway 做认证和基础限流。
3. Safety 模块检查输入策略。
4. Router 根据模型、租户、成本和负载选择 endpoint。
5. Scheduler 将请求放入队列。
6. Runtime 将请求加入 continuous batching。
7. Worker 执行 prefill。
8. Worker 生成 KV cache。
9. Worker 执行 decode。
10. 服务端以 streaming 方式返回 token。
11. Safety 模块检查输出策略。
12. Metrics 记录 TTFT、TPOT、tokens 和错误码。
13. Trace 记录路由、排队、runtime 和返回阶段耗时。
14. Cost 模块做成本归因。
15. Audit 模块记录访问事件。

这才是线上推理平台的一次完整服务链路。

## 26.20 推理平台总览审计指标与最小 demo

系统设计里最危险的回答，是把推理平台说成“API Gateway + Model Server + GPU”。真正的推理平台要证明请求生命周期、SLO、runtime、缓存、发布、安全和成本都能被观测和治理。

推理平台审计样本可以抽象成：

```math
u_i=(a_i,r_i,s_i,m_i,p_i,e_i,b_i,k_i,h_i,w_i,g_i,c_i,o_i,z_i)
```

其中，`a_i` 是接入和鉴权，`r_i` 是路由，`s_i` 是 SLO 指标，`m_i` 是 Model Registry，`p_i` 是 prefill / decode runtime，`e_i` 是 endpoint 和扩缩容，`b_i` 是 batching，`k_i` 是 KV cache，`h_i` 是灰度和回滚，`w_i` 是保护机制，`g_i` 是安全治理，`c_i` 是成本归因，`o_i` 是可观测性，`z_i` 是最终推理平台门禁。

第 `j` 个维度的覆盖率可以写成：

```math
C_j=\frac{1}{N}\sum_{i=1}^{N}\mathbf{1}[g_j(u_i)=1]
```

一次请求的端到端延迟可以拆成：

```math
T_{\mathrm{e2e}}=T_{\mathrm{auth}}+T_{\mathrm{route}}+T_{\mathrm{queue}}+T_{\mathrm{tokenize}}+T_{\mathrm{prefill}}+T_{\mathrm{decode}}+T_{\mathrm{stream}}+T_{\mathrm{safety}}
```

TTFT 与 TPOT 可以近似写成：

```math
T_{\mathrm{ttft}}=T_{\mathrm{auth}}+T_{\mathrm{route}}+T_{\mathrm{queue}}+T_{\mathrm{tokenize}}+T_{\mathrm{prefill}}+T_{\mathrm{first}}
```

```math
T_{\mathrm{tpot}}=\frac{T_{\mathrm{decode}}}{N_{\mathrm{out}}}
```

KV cache 显存可以用常见估算式：

```math
M_{\mathrm{kv}}=2LBSH_{\mathrm{kv}}D_hB_{\mathrm{elem}}
```

其中，`L` 是层数，`B` 是活跃请求数，`S` 是每个请求的保留 token 数，`H_kv` 是 KV head 数，`D_h` 是 head dimension，`B_elem` 是每个元素字节数。

单位成本可以先按 token 粗算：

```math
K_{\mathrm{1k}}=\frac{1000K_{\mathrm{total}}}{N_{\mathrm{in}}+N_{\mathrm{out}}}
```

推理平台上线门禁可以写成：

```math
G_{\mathrm{infer}}=\mathbf{1}\left[\min_j C_j\ge \tau_j \land T_{\mathrm{ttft,p95}}\le B_{\mathrm{ttft}} \land T_{\mathrm{tpot,p95}}\le B_{\mathrm{tpot}} \land R_{\mathrm{kv}}\le \rho_{\mathrm{kv}} \land R_{\mathrm{err}}\le \rho_{\mathrm{err}} \land P_0=0\right]
```

下面的 0 依赖 demo 模拟一个推理平台总览审计。它不替代压测，但能帮助你把“推理平台要有哪些模块”转成可验收的指标表。

```python
from math import ceil


GATES = [
    "request_lifecycle_coverage",
    "slo_metric_contract",
    "model_registry_readiness",
    "model_router_policy_fit",
    "runtime_prefill_decode_fit",
    "continuous_batching_readiness",
    "kv_cache_capacity_governance",
    "autoscaling_warm_pool_fit",
    "release_rollback_control",
    "rate_limit_degrade_protection",
    "safety_governance_fit",
    "cost_attribution_optimization",
    "observability_trace_coverage",
    "multi_model_governance",
    "developer_api_self_service",
    "inference_platform_gate",
]


def p95(values):
    ordered = sorted(values)
    index = max(0, ceil(0.95 * len(ordered)) - 1)
    return ordered[index]


def all_present(mapping, names):
    return all(
        name in mapping
        and mapping[name] is not None
        and mapping[name] is not False
        for name in names
    )


def mib(bytes_value):
    return bytes_value / (1024 ** 2)


def evaluate_case(case):
    request = case["request_lifecycle"]
    slo = case["slo"]
    model = case["model_registry"]
    router = case["router"]
    runtime = case["runtime"]
    batching = case["batching"]
    kv = case["kv_cache"]
    autoscaling = case["autoscaling"]
    release = case["release"]
    protection = case["protection"]
    safety = case["safety"]
    cost = case["cost"]
    observability = case["observability"]
    governance = case["multi_model"]
    developer = case["developer"]

    layer_count = kv["layers"]
    active_requests = kv["active_requests"]
    sequence_length = kv["sequence_length"]
    kv_heads = kv["kv_heads"]
    head_dim = kv["head_dim"]
    elem_bytes = kv["elem_bytes"]
    kv_bytes = 2 * layer_count * active_requests * sequence_length * kv_heads * head_dim * elem_bytes
    kv_pressure = kv_bytes / kv["budget_bytes"]

    total_tokens = sum(slo["input_tokens"]) + sum(slo["output_tokens"])
    output_tokens = sum(slo["output_tokens"])
    wall_time_s = max(slo["finish_times_s"]) - min(slo["arrival_times_s"])
    tokens_per_second = total_tokens / wall_time_s
    output_tokens_per_second = output_tokens / wall_time_s
    cost_per_1k = 1000 * cost["total_cost_usd"] / total_tokens
    ttft_p95 = p95(slo["ttft_ms"])
    tpot_p95 = p95(slo["tpot_ms"])
    e2e_p99 = max(slo["e2e_ms"])

    results = {}
    results["request_lifecycle_coverage"] = all_present(
        request,
        ["auth", "rate_limit", "safety_in", "routing", "queue", "prefill", "decode", "streaming", "safety_out", "metrics", "cost", "audit"],
    )
    results["slo_metric_contract"] = all_present(
        slo,
        ["ttft_ms", "tpot_ms", "e2e_ms", "queue_ms", "input_tokens", "output_tokens", "error_count", "timeout_count", "slo_doc"],
    )
    results["model_registry_readiness"] = all_present(
        model,
        ["model_name", "version", "weight_uri", "tokenizer", "runtime", "context_limit", "quantization", "eval_report", "stage", "rollback_version"],
    )
    results["model_router_policy_fit"] = all_present(
        router,
        ["tenant_policy", "model_version", "latency_slo", "cost_budget", "load_aware", "fallback", "safety_tier"],
    )
    results["runtime_prefill_decode_fit"] = all_present(
        runtime,
        ["model_load", "tokenizer", "prefill", "decode", "kv_cache", "streaming", "runtime_metrics"],
    )
    results["continuous_batching_readiness"] = all_present(
        batching,
        ["dynamic_join", "dynamic_leave", "max_wait_ms", "max_batch_tokens", "priority_queue", "long_request_isolation"],
    )
    results["kv_cache_capacity_governance"] = (
        all_present(kv, ["block_manager", "eviction", "prefix_cache", "max_context", "admission_control", "metrics"])
        and kv_pressure <= kv["pressure_limit"]
    )
    results["autoscaling_warm_pool_fit"] = all_present(
        autoscaling,
        ["queue_signal", "ttft_signal", "kv_signal", "tokens_signal", "cold_start_s", "warm_pool", "scale_down_policy"],
    )
    results["release_rollback_control"] = all_present(
        release,
        ["canary", "ab_test", "traffic_split", "quality_guardrail", "latency_guardrail", "cost_guardrail", "rollback"],
    )
    results["rate_limit_degrade_protection"] = all_present(
        protection,
        ["tenant_quota", "model_quota", "max_input", "max_output", "queue_timeout", "execution_timeout", "circuit_breaker", "fallback_cache"],
    )
    results["safety_governance_fit"] = all_present(
        safety,
        ["authz", "input_policy", "output_policy", "pii_redaction", "log_redaction", "retention", "audit"],
    )
    results["cost_attribution_optimization"] = (
        all_present(cost, ["tenant", "model", "version", "input_tokens", "output_tokens", "gpu_seconds", "cache_savings", "budget_alert"])
        and cost["attribution_rate"] >= 0.9
    )
    results["observability_trace_coverage"] = all_present(
        observability,
        ["metrics", "logs", "traces", "request_id", "route_span", "queue_span", "prefill_span", "decode_span", "stream_span", "dashboard", "alerts"],
    )
    results["multi_model_governance"] = all_present(
        governance,
        ["model", "model_version", "endpoint", "deployment", "route_policy", "scaling_policy", "quota_policy", "safety_policy", "cost_policy"],
    )
    results["developer_api_self_service"] = all_present(
        developer,
        ["openapi", "sdk", "playground", "usage_dashboard", "slo_dashboard", "error_report", "quota_request"],
    )

    results["inference_platform_gate"] = (
        case["platform_review_passed"]
        and case["p0_risks"] == 0
        and ttft_p95 <= slo["ttft_slo_ms"]
        and tpot_p95 <= slo["tpot_slo_ms"]
        and e2e_p99 <= slo["e2e_slo_ms"]
        and kv_pressure <= kv["pressure_limit"]
        and slo["error_count"] / len(slo["ttft_ms"]) <= slo["error_rate_limit"]
        and tokens_per_second >= slo["tokens_per_second_min"]
    )
    examples = {
        "ttft_p95_ms": ttft_p95,
        "tpot_p95_ms": tpot_p95,
        "e2e_p99_ms": e2e_p99,
        "tokens_per_second": round(tokens_per_second, 1),
        "output_tokens_per_second": round(output_tokens_per_second, 1),
        "kv_cache_mib": round(mib(kv_bytes), 1),
        "kv_pressure": round(kv_pressure, 3),
        "cost_per_1k_tokens": round(cost_per_1k, 4),
    }
    return results, examples


def make_complete_case():
    return {
        "name": "complete_inference_platform",
        "request_lifecycle": {
            "auth": True,
            "rate_limit": True,
            "safety_in": True,
            "routing": True,
            "queue": True,
            "prefill": True,
            "decode": True,
            "streaming": True,
            "safety_out": True,
            "metrics": True,
            "cost": True,
            "audit": True,
        },
        "slo": {
            "ttft_ms": [180, 210, 240, 260, 290, 320],
            "tpot_ms": [18, 19, 20, 22, 24, 26],
            "e2e_ms": [1500, 1680, 1720, 1900, 2100, 2350],
            "queue_ms": [10, 20, 25, 30, 35, 45],
            "input_tokens": [800, 640, 1200, 300, 900, 500],
            "output_tokens": [180, 220, 160, 120, 260, 140],
            "arrival_times_s": [0, 0.2, 0.4, 0.8, 1.0, 1.4],
            "finish_times_s": [1.5, 1.9, 2.1, 2.2, 2.7, 3.0],
            "error_count": 0,
            "timeout_count": 0,
            "slo_doc": True,
            "ttft_slo_ms": 400,
            "tpot_slo_ms": 35,
            "e2e_slo_ms": 2600,
            "error_rate_limit": 0.01,
            "tokens_per_second_min": 1200,
        },
        "model_registry": {
            "model_name": True,
            "version": True,
            "weight_uri": True,
            "tokenizer": True,
            "runtime": True,
            "context_limit": True,
            "quantization": True,
            "eval_report": True,
            "stage": True,
            "rollback_version": True,
        },
        "router": {
            "tenant_policy": True,
            "model_version": True,
            "latency_slo": True,
            "cost_budget": True,
            "load_aware": True,
            "fallback": True,
            "safety_tier": True,
        },
        "runtime": {
            "model_load": True,
            "tokenizer": True,
            "prefill": True,
            "decode": True,
            "kv_cache": True,
            "streaming": True,
            "runtime_metrics": True,
        },
        "batching": {
            "dynamic_join": True,
            "dynamic_leave": True,
            "max_wait_ms": True,
            "max_batch_tokens": True,
            "priority_queue": True,
            "long_request_isolation": True,
        },
        "kv_cache": {
            "layers": 32,
            "active_requests": 6,
            "sequence_length": 4096,
            "kv_heads": 8,
            "head_dim": 128,
            "elem_bytes": 2,
            "budget_bytes": 16 * 1024 ** 3,
            "pressure_limit": 0.8,
            "block_manager": True,
            "eviction": True,
            "prefix_cache": True,
            "max_context": True,
            "admission_control": True,
            "metrics": True,
        },
        "autoscaling": {
            "queue_signal": True,
            "ttft_signal": True,
            "kv_signal": True,
            "tokens_signal": True,
            "cold_start_s": True,
            "warm_pool": True,
            "scale_down_policy": True,
        },
        "release": {
            "canary": True,
            "ab_test": True,
            "traffic_split": True,
            "quality_guardrail": True,
            "latency_guardrail": True,
            "cost_guardrail": True,
            "rollback": True,
        },
        "protection": {
            "tenant_quota": True,
            "model_quota": True,
            "max_input": True,
            "max_output": True,
            "queue_timeout": True,
            "execution_timeout": True,
            "circuit_breaker": True,
            "fallback_cache": True,
        },
        "safety": {
            "authz": True,
            "input_policy": True,
            "output_policy": True,
            "pii_redaction": True,
            "log_redaction": True,
            "retention": True,
            "audit": True,
        },
        "cost": {
            "tenant": True,
            "model": True,
            "version": True,
            "input_tokens": True,
            "output_tokens": True,
            "gpu_seconds": True,
            "cache_savings": True,
            "budget_alert": True,
            "total_cost_usd": 0.19,
            "attribution_rate": 0.97,
        },
        "observability": {
            "metrics": True,
            "logs": True,
            "traces": True,
            "request_id": True,
            "route_span": True,
            "queue_span": True,
            "prefill_span": True,
            "decode_span": True,
            "stream_span": True,
            "dashboard": True,
            "alerts": True,
        },
        "multi_model": {
            "model": True,
            "model_version": True,
            "endpoint": True,
            "deployment": True,
            "route_policy": True,
            "scaling_policy": True,
            "quota_policy": True,
            "safety_policy": True,
            "cost_policy": True,
        },
        "developer": {
            "openapi": True,
            "sdk": True,
            "playground": True,
            "usage_dashboard": True,
            "slo_dashboard": True,
            "error_report": True,
            "quota_request": True,
        },
        "platform_review_passed": True,
        "p0_risks": 0,
    }


def clone_case(case, name):
    copied = {}
    for key, value in case.items():
        if isinstance(value, dict):
            copied[key] = dict(value)
        elif isinstance(value, list):
            copied[key] = list(value)
        else:
            copied[key] = value
    copied["name"] = name
    return copied


def make_bad_cases(complete):
    bad_cases = []

    case = clone_case(complete, "request_lifecycle_missing_bad")
    case["request_lifecycle"]["audit"] = False
    bad_cases.append(case)

    case = clone_case(complete, "slo_metric_contract_missing_bad")
    case["slo"]["slo_doc"] = False
    bad_cases.append(case)

    case = clone_case(complete, "model_registry_gap_bad")
    case["model_registry"]["rollback_version"] = False
    bad_cases.append(case)

    case = clone_case(complete, "router_policy_gap_bad")
    case["router"]["cost_budget"] = False
    bad_cases.append(case)

    case = clone_case(complete, "runtime_prefill_decode_gap_bad")
    case["runtime"]["prefill"] = False
    bad_cases.append(case)

    case = clone_case(complete, "continuous_batching_gap_bad")
    case["batching"]["dynamic_leave"] = False
    bad_cases.append(case)

    case = clone_case(complete, "kv_cache_capacity_gap_bad")
    case["kv_cache"]["admission_control"] = False
    bad_cases.append(case)

    case = clone_case(complete, "autoscaling_warm_pool_gap_bad")
    case["autoscaling"]["warm_pool"] = False
    bad_cases.append(case)

    case = clone_case(complete, "release_rollback_gap_bad")
    case["release"]["rollback"] = False
    bad_cases.append(case)

    case = clone_case(complete, "rate_limit_degrade_gap_bad")
    case["protection"]["circuit_breaker"] = False
    bad_cases.append(case)

    case = clone_case(complete, "safety_governance_gap_bad")
    case["safety"]["log_redaction"] = False
    bad_cases.append(case)

    case = clone_case(complete, "cost_attribution_gap_bad")
    case["cost"]["model"] = False
    bad_cases.append(case)

    case = clone_case(complete, "observability_trace_gap_bad")
    case["observability"]["decode_span"] = False
    bad_cases.append(case)

    case = clone_case(complete, "multi_model_governance_gap_bad")
    case["multi_model"]["quota_policy"] = False
    bad_cases.append(case)

    case = clone_case(complete, "developer_self_service_gap_bad")
    case["developer"]["usage_dashboard"] = False
    bad_cases.append(case)

    case = clone_case(complete, "inference_platform_gate_missing_bad")
    case["platform_review_passed"] = False
    case["p0_risks"] = 1
    case["slo"]["ttft_ms"] = [600, 650, 700]
    bad_cases.append(case)

    return bad_cases


def summarize(cases):
    all_results = {}
    examples_by_case = {}
    for case in cases:
        result, examples = evaluate_case(case)
        all_results[case["name"]] = result
        examples_by_case[case["name"]] = examples

    metrics = {}
    for gate in GATES:
        passed = sum(1 for result in all_results.values() if result[gate])
        metrics[gate] = round(passed / len(cases), 3)

    failed_cases = [
        name for name, result in all_results.items()
        if not all(result[gate] for gate in GATES)
    ]
    failed_gates = [gate for gate in GATES if metrics[gate] < 1.0]
    return all_results, examples_by_case, metrics, failed_cases, failed_gates


complete_case = make_complete_case()
cases = [complete_case] + make_bad_cases(complete_case)
results, examples_by_case, metrics, failed_cases, failed_gates = summarize(cases)
examples = examples_by_case["complete_inference_platform"]

smoke = {
    "complete_case_passes": all(results["complete_inference_platform"].values()),
    "caught_missing_lifecycle": not results["request_lifecycle_missing_bad"]["request_lifecycle_coverage"],
    "caught_router_gap": not results["router_policy_gap_bad"]["model_router_policy_fit"],
    "caught_kv_gap": not results["kv_cache_capacity_gap_bad"]["kv_cache_capacity_governance"],
    "caught_observability_gap": not results["observability_trace_gap_bad"]["observability_trace_coverage"],
    "caught_gate_gap": not results["inference_platform_gate_missing_bad"]["inference_platform_gate"],
}

print(f"inference_platform_examples={examples}")
print(f"smoke={smoke}")
print(f"metrics={metrics}")
print(f"hard_blocker_count={len(failed_cases)}")
print(f"failed_cases={failed_cases}")
print(f"failed_gates={failed_gates}")
print(f"inference_platform_gate_pass={len(failed_cases) == 0}")
```

运行后应看到类似输出：

```text
inference_platform_examples={'ttft_p95_ms': 320, 'tpot_p95_ms': 26, 'e2e_p99_ms': 2350, 'tokens_per_second': 1806.7, 'output_tokens_per_second': 360.0, 'kv_cache_mib': 3072.0, 'kv_pressure': 0.188, 'cost_per_1k_tokens': 0.0351}
smoke={'complete_case_passes': True, 'caught_missing_lifecycle': True, 'caught_router_gap': True, 'caught_kv_gap': True, 'caught_observability_gap': True, 'caught_gate_gap': True}
metrics={'request_lifecycle_coverage': 0.941, 'slo_metric_contract': 0.941, 'model_registry_readiness': 0.941, 'model_router_policy_fit': 0.941, 'runtime_prefill_decode_fit': 0.941, 'continuous_batching_readiness': 0.941, 'kv_cache_capacity_governance': 0.941, 'autoscaling_warm_pool_fit': 0.941, 'release_rollback_control': 0.941, 'rate_limit_degrade_protection': 0.941, 'safety_governance_fit': 0.941, 'cost_attribution_optimization': 0.941, 'observability_trace_coverage': 0.941, 'multi_model_governance': 0.941, 'developer_api_self_service': 0.941, 'inference_platform_gate': 0.941}
hard_blocker_count=16
failed_cases=['request_lifecycle_missing_bad', 'slo_metric_contract_missing_bad', 'model_registry_gap_bad', 'router_policy_gap_bad', 'runtime_prefill_decode_gap_bad', 'continuous_batching_gap_bad', 'kv_cache_capacity_gap_bad', 'autoscaling_warm_pool_gap_bad', 'release_rollback_gap_bad', 'rate_limit_degrade_gap_bad', 'safety_governance_gap_bad', 'cost_attribution_gap_bad', 'observability_trace_gap_bad', 'multi_model_governance_gap_bad', 'developer_self_service_gap_bad', 'inference_platform_gate_missing_bad']
failed_gates=['request_lifecycle_coverage', 'slo_metric_contract', 'model_registry_readiness', 'model_router_policy_fit', 'runtime_prefill_decode_fit', 'continuous_batching_readiness', 'kv_cache_capacity_governance', 'autoscaling_warm_pool_fit', 'release_rollback_control', 'rate_limit_degrade_protection', 'safety_governance_fit', 'cost_attribution_optimization', 'observability_trace_coverage', 'multi_model_governance', 'developer_api_self_service', 'inference_platform_gate']
inference_platform_gate_pass=False
```

这段输出的含义是：完整样本通过，但 16 个 bad case 分别暴露请求生命周期、SLO、Model Registry、Router、runtime、batching、KV cache、扩缩容、发布回滚、保护机制、安全、成本、观测、多模型治理、开发者自助和最终门禁的缺口。面试时不需要背代码，但要能把这些维度讲成一条可观测、可回滚、可归因的在线服务链路。

## 26.21 常见误区

误区一：推理平台就是 K8s 部署模型服务。

K8s 只是底座。推理平台还需要路由、runtime、batching、cache、扩缩容、灰度、限流、观测和成本治理。

误区二：QPS 越高越好。

推理平台不能只看 QPS，还要看 token 吞吐、TTFT、TPOT、p99 和成本。

误区三：GPU 利用率高就是好。

GPU 利用率高但 p99 延迟崩掉，也不是好系统。在线服务要在吞吐和延迟之间平衡。

误区四：大模型统一上最大模型。

很多请求不需要最大模型。分层路由、小模型优先和按需升级是成本治理的关键。

误区五：灰度只看错误率。

模型灰度还要看质量、安全、延迟、成本和业务指标。

## 26.22 面试回答模板

如果面试官问：“你怎么设计一个大模型推理平台？”

可以这样回答：

```text
我会先澄清业务需求，包括模型数量、QPS、上下文长度、延迟 SLO、流式返回、租户隔离和成本目标。

架构上，我会把平台拆成 API Gateway、鉴权限流、Model Router、Inference Runtime、Model Registry、缓存层、自动扩缩容、发布系统、观测系统和成本审计系统。

请求进入后先经过认证和限流，再由 Router 根据模型版本、租户、负载、SLO 和成本选择 endpoint。Runtime 负责模型加载、prefill、decode、KV cache 和 continuous batching。平台侧记录 TTFT、TPOT、p95/p99、tokens/s、队列长度、GPU 和 KV cache 使用率。

发布上支持灰度、A/B 测试和回滚。稳定性上支持超时、熔断、降级和缓存。成本上按租户、模型、版本和 token 用量做归因，并通过分层模型路由、缓存、量化和自动扩缩容降低成本。
```

这个回答覆盖了需求、架构、关键指标、稳定性和成本，比较完整。

## 26.23 小练习

1. 为什么大模型推理平台不能只看 QPS？
2. TTFT 和 TPOT 分别代表什么？
3. Prefill 和 decode 的资源瓶颈有什么不同？
4. KV cache 为什么会成为显存瓶颈？
5. 为什么推理平台需要 Model Router？
6. 自动扩缩容为什么不能只看 GPU 利用率？
7. 大模型服务冷启动慢的原因是什么？
8. 模型灰度发布应该观察哪些指标？

## 26.24 本章小结

本章从单模型服务讲到多模型推理平台。

你需要记住：

1. 推理平台是在线服务系统，不只是模型 HTTP 包装。
2. 推理平台核心目标是低延迟、高吞吐、高可用和低成本。
3. TTFT、TPOT、p95/p99、tokens/s、队列长度和成本是关键指标。
4. Prefill、decode 和 KV cache 是理解大模型推理性能的基础。
5. 多模型平台需要 Model Registry、Model Router、Runtime、缓存、扩缩容、灰度和治理能力。
6. 成熟推理平台必须同时管理性能、稳定性、安全和成本。

下一章我们会继续深入模型服务运行时，讲 Triton、vLLM、TGI、SGLang 和自研 runtime 的差异。
