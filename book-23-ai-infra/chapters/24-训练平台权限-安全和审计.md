# 第 24 章 训练平台权限、安全和审计

上一章讲了训练数据供给。本章讲训练平台的权限、安全和审计。

训练平台连接了最贵的 GPU、最重要的数据、最敏感的模型权重和最复杂的代码执行环境。如果权限和审计没做好，轻则资源被滥用、成本失控，重则数据泄露、模型泄露、供应链攻击或合规事故。

先记住一句话：

> 训练平台安全不是给入口加登录，而是控制谁能用什么资源、读什么数据、运行什么镜像、产生什么模型、查看什么日志，并能事后追踪全过程。

## 24.0 本讲资料边界与第二轮精修口径

本讲第二轮精修时，资料口径主要对齐几类公开工程资料：Kubernetes RBAC 对 Role、ClusterRole、RoleBinding、ClusterRoleBinding 和 least privilege 的授权模型；Kubernetes ServiceAccount、Secrets、NetworkPolicy、Pod Security Standards 和 Auditing 对任务运行身份、密钥、网络边界、Pod 运行时约束和审计事件的稳定抽象；SLSA supply-chain security 对构建 provenance、依赖来源、构建完整性和供应链分层成熟度的通用口径；前文多租户隔离、容器环境、训练任务提交、训练可观测性和模型产物管理章节对 namespace、quota、镜像 digest、日志脱敏、artifact lineage 和权限继承的稳定抽象。

这里不把某个云厂商 IAM、某个内部权限系统、某个 Secret Manager、某个镜像扫描产品或某个合规框架写成唯一标准。正文只抽象训练平台安全里的稳定问题：身份模型、RBAC / ABAC、任务运行身份、数据权限、用途授权、产物等级传播、资源权限、镜像供应链、代码运行时隔离、密钥管理、网络 egress、日志脱敏、模型产物权限、高风险审批、审计日志、防篡改留存和安全事件响应。

第二轮精修重点放在三个方面：

1. 把“安全”从登录鉴权扩展为身份、资源、数据、运行时、供应链、密钥、网络、日志、artifact 和审计的完整控制面。
2. 补充可计算指标：越权阻断率、过度授权率、产物等级传播一致性、密钥泄露率、审计覆盖率和审批覆盖率。
3. 增加一个 0 依赖 Python demo，把训练平台安全从原则清单变成可运行门禁检查。

## 24.1 训练平台为什么高风险

训练平台有几个天然风险。

第一，它能访问大量数据。

训练数据可能包含用户数据、业务机密、代码、文档、图片、音频、日志和标注信息。

第二，它能运行用户代码。

训练脚本本质上是任意代码执行。如果没有限制，用户代码可以访问网络、文件系统、密钥和其他服务。

第三，它能使用昂贵资源。

GPU 成本高，资源滥用会直接造成损失。

第四，它会产生模型产物。

模型权重、checkpoint、adapter、评估报告都可能继承训练数据的敏感性。

第五，它有复杂供应链。

镜像、Python 包、CUDA kernel、训练框架、数据处理脚本都可能引入风险。

因此训练平台必须从设计上内置权限、安全和审计。

## 24.2 安全目标

训练平台安全要实现：

1. 身份可信。
2. 权限最小化。
3. 数据访问可控。
4. 资源使用可控。
5. 镜像和代码可信。
6. 密钥不泄露。
7. 日志和产物不越权。
8. 高风险操作可审批。
9. 全流程可审计。
10. 事故可追溯。

这些目标分别对应身份、权限、数据、运行时、供应链、密钥、审计和治理模块。

## 24.3 身份模型

训练平台里至少有几类身份：

1. 用户身份。
2. 团队或项目身份。
3. Service Account。
4. TrainingJob 身份。
5. 平台服务身份。
6. 管理员身份。

用户身份用于提交任务和查看结果。

TrainingJob 身份用于任务运行时访问数据、checkpoint、日志和模型仓库。

平台服务身份用于调度、创建 Pod、写审计日志、注册 artifact。

不要让所有训练任务共享一个高权限平台身份。

更好的方式是为每个任务分配最小权限的运行身份。

## 24.4 RBAC 和 ABAC

RBAC 是基于角色的访问控制。

例如：

1. platform_admin。
2. project_admin。
3. trainer。
4. viewer。
5. auditor。

ABAC 是基于属性的访问控制。

属性可以包括：

1. project。
2. team。
3. environment。
4. data_classification。
5. resource_pool。
6. job_type。
7. model_stage。

训练平台通常需要 RBAC + ABAC。

例如：

```text
用户是 trainer，但只能在 project_a 下提交 normal priority 任务，且只能访问 internal 等级数据，不能访问 restricted 数据。
```

只用 RBAC 很难表达这种细粒度策略。

## 24.5 数据权限

训练平台必须控制数据访问。

数据权限要回答：

1. 用户能不能看到数据集？
2. 任务能不能读取数据？
3. 数据能不能用于训练？
4. 数据能不能跨区域读取？
5. 数据是否需要脱敏？
6. 训练出的模型是否继承数据等级？

数据权限不应该只靠文件系统 ACL。

平台需要理解 dataset_id、version、classification、owner、allowed_projects、allowed_purpose。

训练任务提交时必须校验数据权限。

运行时也要用任务身份访问数据，而不是用户把长期密钥写进脚本。

## 24.6 数据等级传播

使用敏感数据训练出的产物可能也敏感。

产物包括：

1. checkpoint。
2. final model。
3. LoRA / adapter。
4. eval report。
5. logs。
6. sample outputs。

规则示例：

```text
如果训练使用 restricted 数据，则 checkpoint、模型权重和 eval report 默认标记为 restricted。
```

这叫数据等级传播。

没有这条规则，用户可能无法读取原始数据，却可以下载用该数据训练出的模型。

## 24.7 资源权限

资源权限控制谁能用哪些 GPU 和队列。

常见策略：

1. 普通用户只能使用 normal 队列。
2. 高优先级队列需要审批。
3. H100 资源池只对关键项目开放。
4. 生产推理资源池禁止训练任务使用。
5. 可抢占资源允许实验任务使用。
6. 每个团队有 GPU-hour 预算。

资源权限和成本治理相关。

如果没有资源权限，昂贵 GPU 很容易被低价值任务占满。

## 24.8 镜像安全

训练任务运行镜像，因此镜像是安全入口。

风险：

1. 镜像包含漏洞。
2. 镜像包含恶意代码。
3. 镜像包含密钥。
4. 镜像来源不可信。
5. tag 被覆盖。
6. 基础镜像过旧。

平台应要求：

1. 只允许可信 registry。
2. 镜像扫描通过。
3. 记录 digest。
4. 高风险漏洞阻断。
5. 禁止密钥进入镜像。
6. 镜像签名。
7. 基础镜像白名单。

提交任务时就应该校验镜像，不要等 Pod 启动后才发现风险。

## 24.9 代码安全

训练平台运行用户代码。

代码风险包括：

1. 访问未授权数据。
2. 向外发送数据。
3. 读取宿主机路径。
4. 打印敏感信息。
5. 下载未知依赖。
6. 执行危险命令。

治理措施：

1. 限制运行时权限。
2. 禁止 privileged 容器。
3. 限制 hostPath。
4. 控制出网。
5. 代码仓库权限校验。
6. 高风险任务审批。
7. 日志脱敏。

训练平台不能假设所有训练代码都是安全的。

## 24.10 密钥管理

训练任务可能需要访问对象存储、数据服务、模型仓库和实验追踪系统。

不要把密钥写入：

1. 镜像。
2. 代码。
3. 配置文件。
4. 日志。
5. checkpoint。

更好的方式：

1. 使用 Secret Manager。
2. 按任务注入短期凭证。
3. 使用 Service Account。
4. 最小权限。
5. 自动轮换。
6. 访问审计。
7. 日志脱敏。

密钥泄露是训练平台重大事故。

## 24.11 网络安全

训练任务是否能访问外网、生产数据库、内部服务，需要严格控制。

网络策略包括：

1. 默认禁止跨租户访问。
2. egress 白名单。
3. 禁止访问生产敏感服务。
4. 只允许访问必要数据 endpoint。
5. 限制调试端口暴露。
6. 记录外部访问日志。

很多数据泄露不是从存储权限发生，而是训练代码把数据发到了外部 endpoint。

## 24.12 日志安全

训练日志可能包含敏感信息。

平台要控制：

1. 谁能看日志。
2. 日志保留多久。
3. 日志是否脱敏。
4. 是否允许下载。
5. 是否记录查看行为。

日志脱敏对象包括：

1. token。
2. 密钥。
3. 邮箱。
4. 手机号。
5. 用户输入。
6. 数据样本。
7. 连接串。

日志系统本身也是安全系统的一部分。

## 24.13 模型产物权限

模型产物包括：

1. checkpoint。
2. final model。
3. adapter。
4. tokenizer。
5. config。
6. eval report。
7. deployment package。

产物权限要根据：

1. 项目。
2. 数据等级。
3. 模型阶段。
4. owner。
5. 发布状态。
6. 审批结果。

例如训练中的 checkpoint 只能项目成员读，发布后的模型可以给更多应用调用，但下载权仍需限制。

模型权重是资产，不是普通文件。

## 24.14 高风险操作审批

高风险操作包括：

1. 使用 restricted 数据训练。
2. 导出模型权重。
3. 发布到生产。
4. 使用高优先级队列。
5. 关闭日志或 checkpoint。
6. 使用未审核镜像。
7. 访问外部网络。
8. 删除重要 checkpoint。

这些操作应该触发审批或策略阻断。

审批记录要进入审计日志。

## 24.15 审计日志

审计日志要记录谁在什么时候做了什么。

训练平台审计对象包括：

1. 任务提交。
2. 任务取消。
3. 数据访问。
4. 镜像使用。
5. checkpoint 创建和删除。
6. 模型注册和下载。
7. 权限修改。
8. 配额修改。
9. 高风险审批。
10. 日志查看。

审计日志字段：

1. actor。
2. action。
3. resource。
4. timestamp。
5. decision。
6. reason。
7. request_id。
8. ip / source。
9. metadata。

审计日志要防篡改，并有留存策略。

## 24.16 安全事件响应

训练平台要能响应安全事件。

例如：

1. 密钥泄露。
2. 未授权数据访问。
3. 镜像漏洞。
4. 模型权重误下载。
5. 日志泄露。
6. 异常外联。

响应流程：

1. 发现事件。
2. 冻结相关任务。
3. 撤销密钥。
4. 隔离产物。
5. 查询审计日志。
6. 通知 owner。
7. 修复策略。
8. 复盘。

没有审计，就无法做安全响应。

## 24.17 训练平台安全审计指标与最小 demo

这一节把训练平台安全改写为可审计门禁。真实平台可以把 IdP / IAM、Kubernetes RBAC、admission controller、NetworkPolicy、Secret Manager、registry、audit log、artifact registry 和 incident response system 串起来；教学 demo 先用 toy 字段模拟。

一个训练平台安全样本可以抽象成：

```math
s_i=(u_i,r_i,d_i,a_i,q_i,m_i,c_i,e_i,n_i,l_i,w_i,h_i,o_i,t_i,z_i)
```

其中，`u_i` 是身份和运行身份，`r_i` 是 RBAC / ABAC，`d_i` 是数据权限，`a_i` 是 artifact 等级传播，`q_i` 是资源和队列权限，`m_i` 是镜像供应链，`c_i` 是代码运行时隔离，`e_i` 是密钥，`n_i` 是网络，`l_i` 是日志，`w_i` 是模型产物权限，`h_i` 是高风险审批，`o_i` 是审计日志，`t_i` 是事件响应，`z_i` 是最终安全门禁。

统一覆盖率可以写成：

```math
C_j=\frac{1}{N}\sum_{i=1}^{N}\mathbf{1}[g_j(s_i)=1]
```

越权阻断率可以写成：

```math
R_{\mathrm{block}}=\frac{N_{\mathrm{denied\_unauth}}}{N_{\mathrm{unauth}}}
```

过度授权率可以写成：

```math
R_{\mathrm{over}}=\frac{N_{\mathrm{granted}}-N_{\mathrm{used}}}{N_{\mathrm{granted}}}
```

如果 `R_over` 很高，说明权限授予远大于真实使用，最小权限原则没有落地。

数据等级传播可以写成：

```math
V_{\mathrm{artifact}}=\mathbf{1}\left[\mathrm{class}(A)\ge \max_k \mathrm{class}(D_k)\right]
```

其中，`D_k` 是训练使用的第 `k` 个数据集，`A` 是产物。使用 restricted 数据训练出的 checkpoint、模型、adapter、eval report 和日志不能自动降级。

密钥泄露率可以写成：

```math
R_{\mathrm{secret}}=\frac{N_{\mathrm{secret\_leak}}}{N_{\mathrm{secret\_event}}}
```

审计覆盖率可以写成：

```math
R_{\mathrm{audit}}=\frac{N_{\mathrm{audited}}}{N_{\mathrm{required}}}
```

最终训练平台安全门禁可以写成：

```math
G_{\mathrm{security}}=\mathbf{1}\left[\min_j C_j\ge \tau_j \land R_{\mathrm{block}}=1 \land R_{\mathrm{over}}\le \rho_{\mathrm{over}} \land V_{\mathrm{artifact}}=1 \land R_{\mathrm{secret}}=0 \land R_{\mathrm{audit}}\ge \rho_{\mathrm{audit}} \land P_0=0\right]
```

这个门禁背后的含义是：训练平台安全不是一个登录页，而是每个任务、数据、镜像、密钥、日志、artifact 和审批动作都有可证明的权限决策和审计证据。

下面的 0 依赖 Python demo 演示一个最小训练平台安全审计器。

```python
from copy import deepcopy


GATES = [
    "identity_runtime_binding",
    "rbac_abac_policy_fit",
    "dataset_permission_purpose",
    "artifact_classification_propagation",
    "resource_quota_priority_governance",
    "image_supply_chain_trust",
    "runtime_code_isolation",
    "secret_scope_rotation",
    "network_egress_control",
    "log_redaction_access",
    "model_artifact_permission",
    "high_risk_approval",
    "audit_log_completeness",
    "audit_integrity_retention",
    "incident_response_readiness",
    "training_platform_security_gate",
]


LEVEL = {"public": 1, "internal": 2, "restricted": 3}


def base_case():
    return {
        "name": "complete_training_security_case",
        "identity": {
            "user_idp": True,
            "project_bound": True,
            "service_account_per_job": True,
            "shared_admin_identity": False,
            "workload_identity": True,
        },
        "permission": {
            "rbac_roles": ["trainer", "viewer"],
            "abac_attributes": ["project", "team", "data_classification", "resource_pool", "purpose"],
            "wildcard_permissions": False,
            "granted_permissions": 20,
            "used_permissions": 18,
            "unauthorized_attempts": 7,
            "denied_unauthorized": 7,
        },
        "data": {
            "dataset_id": "restricted_sales_dialog",
            "version": "v2026_05",
            "classification": "restricted",
            "allowed_project": True,
            "allowed_purpose": "training",
            "requested_purpose": "training",
            "runtime_identity_checked": True,
        },
        "artifact": {
            "input_classes": ["internal", "restricted"],
            "checkpoint_class": "restricted",
            "model_class": "restricted",
            "eval_report_class": "restricted",
            "log_class": "restricted",
        },
        "resource": {
            "quota_checked": True,
            "priority_approved": True,
            "gpu_pool_allowed": True,
            "gpu_hour_budget_ok": True,
        },
        "image": {
            "trusted_registry": True,
            "digest_pinned": True,
            "signed": True,
            "scan_passed": True,
            "critical_vulns": 0,
            "secret_in_image": False,
            "provenance_attested": True,
        },
        "runtime": {
            "privileged": False,
            "hostpath_allowed": False,
            "root_user": False,
            "egress_policy_enforced": True,
            "pod_security_restricted": True,
        },
        "secret": {
            "secret_manager": True,
            "short_lived": True,
            "least_privilege_scope": True,
            "rotation_enabled": True,
            "leak_events": 0,
            "secret_events": 5,
            "log_redaction": True,
        },
        "network": {
            "default_deny": True,
            "egress_allowlist": True,
            "prod_service_blocked": True,
            "external_access_logged": True,
        },
        "logs": {
            "rbac_enforced": True,
            "redaction_enabled": True,
            "download_audited": True,
            "retention_policy": True,
            "sensitive_pattern_scan": True,
        },
        "model_artifact": {
            "registry_rbac": True,
            "download_restricted": True,
            "stage_policy": True,
            "approval_before_release": True,
            "lineage_bound": True,
        },
        "approval": {
            "required_actions": ["restricted_data_train", "model_export", "prod_release", "external_egress"],
            "approved_actions": ["restricted_data_train", "model_export", "prod_release", "external_egress"],
            "approval_logged": True,
        },
        "audit": {
            "required_events": [
                "job_submit",
                "data_access",
                "image_use",
                "checkpoint_create",
                "model_download",
                "permission_change",
                "quota_change",
                "approval_decision",
                "log_view",
            ],
            "audited_events": [
                "job_submit",
                "data_access",
                "image_use",
                "checkpoint_create",
                "model_download",
                "permission_change",
                "quota_change",
                "approval_decision",
                "log_view",
            ],
            "actor_action_resource": True,
            "request_id": True,
            "tamper_evident": True,
            "retention_days": 365,
        },
        "incident": {
            "freeze_job_ready": True,
            "revoke_secret_ready": True,
            "isolate_artifact_ready": True,
            "audit_query_ready": True,
            "owner_notify_ready": True,
            "postmortem_ready": True,
        },
        "training_platform_security_gate": True,
    }


def contains_all(values, required):
    return set(required).issubset(set(values))


def rate(num, den):
    return num / max(den, 1e-9)


def artifact_propagates(case):
    max_input = max(LEVEL[x] for x in case["artifact"]["input_classes"])
    outputs = [
        case["artifact"]["checkpoint_class"],
        case["artifact"]["model_class"],
        case["artifact"]["eval_report_class"],
        case["artifact"]["log_class"],
    ]
    return all(LEVEL[x] >= max_input for x in outputs)


def evaluate(case):
    identity = case["identity"]
    permission = case["permission"]
    data = case["data"]
    resource = case["resource"]
    image = case["image"]
    runtime = case["runtime"]
    secret = case["secret"]
    network = case["network"]
    logs = case["logs"]
    model_artifact = case["model_artifact"]
    approval = case["approval"]
    audit = case["audit"]
    incident = case["incident"]

    overprivilege = rate(
        permission["granted_permissions"] - permission["used_permissions"],
        permission["granted_permissions"],
    )
    unauthorized_block = rate(permission["denied_unauthorized"], permission["unauthorized_attempts"])
    secret_leak = rate(secret["leak_events"], secret["secret_events"])
    audit_coverage = rate(len(audit["audited_events"]), len(audit["required_events"]))

    checks = {}
    checks["identity_runtime_binding"] = (
        identity["user_idp"]
        and identity["project_bound"]
        and identity["service_account_per_job"]
        and identity["workload_identity"]
        and not identity["shared_admin_identity"]
    )
    checks["rbac_abac_policy_fit"] = (
        "trainer" in permission["rbac_roles"]
        and contains_all(permission["abac_attributes"], ["project", "data_classification", "resource_pool", "purpose"])
        and not permission["wildcard_permissions"]
        and overprivilege <= 0.2
        and unauthorized_block == 1.0
    )
    checks["dataset_permission_purpose"] = (
        data["allowed_project"]
        and data["allowed_purpose"] == data["requested_purpose"]
        and data["runtime_identity_checked"]
        and data["classification"] in LEVEL
        and bool(data["version"])
    )
    checks["artifact_classification_propagation"] = artifact_propagates(case)
    checks["resource_quota_priority_governance"] = (
        resource["quota_checked"]
        and resource["priority_approved"]
        and resource["gpu_pool_allowed"]
        and resource["gpu_hour_budget_ok"]
    )
    checks["image_supply_chain_trust"] = (
        image["trusted_registry"]
        and image["digest_pinned"]
        and image["signed"]
        and image["scan_passed"]
        and image["critical_vulns"] == 0
        and not image["secret_in_image"]
        and image["provenance_attested"]
    )
    checks["runtime_code_isolation"] = (
        not runtime["privileged"]
        and not runtime["hostpath_allowed"]
        and not runtime["root_user"]
        and runtime["egress_policy_enforced"]
        and runtime["pod_security_restricted"]
    )
    checks["secret_scope_rotation"] = (
        secret["secret_manager"]
        and secret["short_lived"]
        and secret["least_privilege_scope"]
        and secret["rotation_enabled"]
        and secret["log_redaction"]
        and secret_leak == 0
    )
    checks["network_egress_control"] = (
        network["default_deny"]
        and network["egress_allowlist"]
        and network["prod_service_blocked"]
        and network["external_access_logged"]
    )
    checks["log_redaction_access"] = (
        logs["rbac_enforced"]
        and logs["redaction_enabled"]
        and logs["download_audited"]
        and logs["retention_policy"]
        and logs["sensitive_pattern_scan"]
    )
    checks["model_artifact_permission"] = (
        model_artifact["registry_rbac"]
        and model_artifact["download_restricted"]
        and model_artifact["stage_policy"]
        and model_artifact["approval_before_release"]
        and model_artifact["lineage_bound"]
    )
    checks["high_risk_approval"] = (
        contains_all(approval["approved_actions"], approval["required_actions"])
        and approval["approval_logged"]
    )
    checks["audit_log_completeness"] = (
        contains_all(audit["audited_events"], audit["required_events"])
        and audit["actor_action_resource"]
        and audit["request_id"]
        and audit_coverage >= 1.0
    )
    checks["audit_integrity_retention"] = audit["tamper_evident"] and audit["retention_days"] >= 180
    checks["incident_response_readiness"] = all(incident.values())
    checks["training_platform_security_gate"] = bool(case["training_platform_security_gate"])
    return checks


def summarize_examples(case):
    permission = case["permission"]
    secret = case["secret"]
    audit = case["audit"]
    approval = case["approval"]
    return {
        "unauthorized_block_rate": round(rate(permission["denied_unauthorized"], permission["unauthorized_attempts"]), 3),
        "overprivilege_rate": round(rate(permission["granted_permissions"] - permission["used_permissions"], permission["granted_permissions"]), 3),
        "artifact_level_propagation": artifact_propagates(case),
        "secret_exposure_rate": round(rate(secret["leak_events"], secret["secret_events"]), 3),
        "audit_coverage": round(rate(len(audit["audited_events"]), len(audit["required_events"])), 3),
        "approval_coverage": round(rate(len(approval["approved_actions"]), len(approval["required_actions"])), 3),
    }


def mutate(name, editor):
    case = deepcopy(base_case())
    case["name"] = name
    editor(case)
    return case


def build_cases():
    cases = [base_case()]
    cases.append(mutate("identity_shared_admin_bad", lambda c: c["identity"].update({"service_account_per_job": False, "shared_admin_identity": True})))
    cases.append(mutate("rbac_abac_wildcard_bad", lambda c: c["permission"].update({"wildcard_permissions": True, "granted_permissions": 100, "used_permissions": 10})))
    cases.append(mutate("dataset_permission_purpose_bad", lambda c: c["data"].update({"allowed_purpose": "evaluation"})))
    cases.append(mutate("artifact_classification_downgrade_bad", lambda c: c["artifact"].update({"model_class": "internal"})))
    cases.append(mutate("resource_priority_unapproved_bad", lambda c: c["resource"].update({"priority_approved": False})))
    cases.append(mutate("image_supply_chain_untrusted_bad", lambda c: c["image"].update({"digest_pinned": False, "signed": False})))
    cases.append(mutate("runtime_privileged_bad", lambda c: c["runtime"].update({"privileged": True, "pod_security_restricted": False})))
    cases.append(mutate("secret_scope_rotation_bad", lambda c: c["secret"].update({"short_lived": False, "leak_events": 1})))
    cases.append(mutate("network_egress_open_bad", lambda c: c["network"].update({"default_deny": False, "egress_allowlist": False})))
    cases.append(mutate("log_redaction_missing_bad", lambda c: c["logs"].update({"redaction_enabled": False, "download_audited": False})))
    cases.append(mutate("model_artifact_permission_bad", lambda c: c["model_artifact"].update({"download_restricted": False, "lineage_bound": False})))
    cases.append(mutate("high_risk_approval_missing_bad", lambda c: c["approval"].update({"approved_actions": ["restricted_data_train"]})))
    cases.append(mutate("audit_log_incomplete_bad", lambda c: c["audit"].update({"audited_events": ["job_submit", "data_access"]})))
    cases.append(mutate("audit_integrity_retention_bad", lambda c: c["audit"].update({"tamper_evident": False, "retention_days": 30})))
    cases.append(mutate("incident_response_missing_bad", lambda c: c["incident"].update({"revoke_secret_ready": False, "postmortem_ready": False})))
    cases.append(mutate("security_gate_missing_bad", lambda c: c.update({"training_platform_security_gate": False})))
    return cases


def main():
    cases = build_cases()
    evaluations = {case["name"]: evaluate(case) for case in cases}
    metrics = {
        gate: round(sum(1 for case in cases if evaluations[case["name"]][gate]) / len(cases), 3)
        for gate in GATES
    }
    failed_cases = [
        case["name"]
        for case in cases
        if not all(evaluations[case["name"]].values())
    ]
    failed_gates = [
        gate
        for gate in GATES
        if any(not evaluations[case["name"]][gate] for case in cases)
    ]
    complete = cases[0]
    smoke = {
        "complete_case_passes": all(evaluations[complete["name"]].values()),
        "caught_shared_admin": not evaluations["identity_shared_admin_bad"]["identity_runtime_binding"],
        "caught_artifact_downgrade": not evaluations["artifact_classification_downgrade_bad"]["artifact_classification_propagation"],
        "caught_secret_leak": not evaluations["secret_scope_rotation_bad"]["secret_scope_rotation"],
        "caught_audit_gap": not evaluations["audit_log_incomplete_bad"]["audit_log_completeness"],
        "caught_gate_gap": not evaluations["security_gate_missing_bad"]["training_platform_security_gate"],
    }
    training_platform_security_gate_pass = not failed_cases and all(v >= 1.0 for v in metrics.values())

    print("training_platform_security_examples=", summarize_examples(complete))
    print("smoke=", smoke)
    print("metrics=", metrics)
    print("hard_blocker_count=", len(failed_cases))
    print("failed_cases=", failed_cases)
    print("failed_gates=", failed_gates)
    print("training_platform_security_gate_pass=", training_platform_security_gate_pass)


if __name__ == "__main__":
    main()
```

预期输出类似：

```text
training_platform_security_examples= {'unauthorized_block_rate': 1.0, 'overprivilege_rate': 0.1, 'artifact_level_propagation': True, 'secret_exposure_rate': 0.0, 'audit_coverage': 1.0, 'approval_coverage': 1.0}
smoke= {'complete_case_passes': True, 'caught_shared_admin': True, 'caught_artifact_downgrade': True, 'caught_secret_leak': True, 'caught_audit_gap': True, 'caught_gate_gap': True}
metrics= {'identity_runtime_binding': 0.941, 'rbac_abac_policy_fit': 0.941, 'dataset_permission_purpose': 0.941, 'artifact_classification_propagation': 0.941, 'resource_quota_priority_governance': 0.941, 'image_supply_chain_trust': 0.941, 'runtime_code_isolation': 0.941, 'secret_scope_rotation': 0.941, 'network_egress_control': 0.941, 'log_redaction_access': 0.941, 'model_artifact_permission': 0.941, 'high_risk_approval': 0.941, 'audit_log_completeness': 0.941, 'audit_integrity_retention': 0.941, 'incident_response_readiness': 0.941, 'training_platform_security_gate': 0.941}
hard_blocker_count= 16
failed_cases= ['identity_shared_admin_bad', 'rbac_abac_wildcard_bad', 'dataset_permission_purpose_bad', 'artifact_classification_downgrade_bad', 'resource_priority_unapproved_bad', 'image_supply_chain_untrusted_bad', 'runtime_privileged_bad', 'secret_scope_rotation_bad', 'network_egress_open_bad', 'log_redaction_missing_bad', 'model_artifact_permission_bad', 'high_risk_approval_missing_bad', 'audit_log_incomplete_bad', 'audit_integrity_retention_bad', 'incident_response_missing_bad', 'security_gate_missing_bad']
failed_gates= ['identity_runtime_binding', 'rbac_abac_policy_fit', 'dataset_permission_purpose', 'artifact_classification_propagation', 'resource_quota_priority_governance', 'image_supply_chain_trust', 'runtime_code_isolation', 'secret_scope_rotation', 'network_egress_control', 'log_redaction_access', 'model_artifact_permission', 'high_risk_approval', 'audit_log_completeness', 'audit_integrity_retention', 'incident_response_readiness', 'training_platform_security_gate']
training_platform_security_gate_pass= False
```

面试里可以把这个 demo 压缩成一句话：训练平台安全要证明“谁、以什么运行身份、因为什么策略、访问了什么数据和产物、运行了什么镜像和代码、用了什么密钥、留下了什么审计证据”，而不是只证明用户已经登录。

## 24.18 面试中如何回答训练平台安全

如果面试官问：

```text
大模型训练平台如何做权限、安全和审计？
```

可以这样回答：

```text
我会从身份、资源、数据、镜像、代码、密钥、网络、日志、模型产物和审计几个层面设计。

身份层接入统一 IdP，区分用户身份、TrainingJob 运行身份、Service Account 和平台服务身份。权限层用 RBAC + ABAC，控制谁能提交任务、使用哪些资源池、访问哪些数据、查看哪些日志、注册和发布哪些模型。

数据层按 dataset_id、version、classification 和 allowed_purpose 做授权，并做数据等级传播：使用敏感数据训练出的 checkpoint、模型和 eval report 继承敏感等级。资源层用队列、配额和优先级控制 GPU 使用。

运行层只允许可信镜像 registry，做镜像扫描、签名和 digest 记录；禁止 privileged、限制 hostPath、控制 egress，密钥通过 Secret Manager 注入短期凭证，避免写入镜像和日志。

审计层记录任务提交、数据访问、镜像使用、checkpoint 创建删除、模型注册下载、权限变更、高风险审批和日志查看。高风险操作如使用 restricted 数据、导出权重、生产发布和关闭日志需要审批。
```

## 24.19 常见误区

误区一：训练平台安全就是登录鉴权。

登录只是身份入口，还要控制数据、资源、镜像、密钥、网络、日志和模型产物。

误区二：用户能读数据，就能随便训练。

数据使用目的也要授权，训练产物可能继承数据敏感等级。

误区三：平台管理员可以看所有日志。

日志可能包含敏感数据，管理员访问也应最小权限和审计。

误区四：镜像能跑就行。

镜像必须可信、扫描、签名、记录 digest，并防止密钥泄露。

误区五：审计只在事故后需要。

审计是日常治理、合规、成本和安全响应的基础。

## 24.20 面试题

### 题 1：训练平台有哪些身份？

答：包括用户身份、团队或项目身份、TrainingJob 运行身份、Service Account、平台服务身份和管理员身份。任务运行时应使用最小权限的任务身份，而不是共享高权限身份。

### 题 2：为什么训练产物要继承数据等级？

答：模型权重、checkpoint、adapter 和评估报告可能包含或泄露训练数据的信息。如果使用敏感数据训练，产物也应继承相应敏感等级，避免用户绕过原始数据权限访问模型产物。

### 题 3：镜像安全要做哪些事？

答：使用可信 registry、镜像扫描、漏洞阻断、镜像签名、digest 记录、基础镜像白名单、禁止密钥进入镜像，并在任务提交时校验镜像策略。

### 题 4：哪些训练操作属于高风险操作？

答：使用敏感数据训练、导出模型权重、发布到生产、使用高优先级队列、关闭日志或 checkpoint、使用未审核镜像、访问外部网络、删除重要 checkpoint 等。

### 题 5：审计日志应该记录什么？

答：应记录 actor、action、resource、timestamp、decision、reason、request_id、source 和 metadata。覆盖任务提交、数据访问、镜像使用、checkpoint、模型产物、权限变更、审批和日志查看。

## 24.21 小练习

练习一：设计一个训练平台权限模型。

要求：包含 admin、project_admin、trainer、viewer、auditor 五类角色和对应权限。

练习二：设计数据等级传播规则。

要求：说明 public、internal、restricted 数据训练出的 checkpoint、model、eval report 应如何定级。

练习三：设计高风险操作审批流程。

要求：以“使用 restricted 数据训练并导出模型权重”为例，列出审批、审计和产物权限规则。

练习四：设计审计日志 schema。

要求：包含 actor、action、resource、decision、reason、timestamp、request_id 和 metadata。

## 24.22 本章小结

本章讲了训练平台权限、安全和审计。

你需要掌握：

1. 训练平台安全覆盖身份、资源、数据、镜像、代码、密钥、网络、日志、模型产物和审计。
2. 训练任务运行时应使用最小权限任务身份。
3. RBAC + ABAC 比单纯角色权限更适合训练平台。
4. 数据权限要控制使用目的，并处理产物敏感等级传播。
5. 资源权限和成本治理相关，高价值 GPU 和高优先级队列需要控制。
6. 镜像安全和代码运行时安全是供应链和执行安全的关键。
7. 密钥必须通过 Secret Manager 和短期凭证管理，不能写进镜像、代码或日志。
8. 日志和模型产物都需要权限和审计。
9. 高风险操作需要审批。
10. 审计日志是安全响应、合规和复盘的基础。

下一章我们会用系统设计题的方式，把第三部分串起来：训练平台系统设计面试题。
