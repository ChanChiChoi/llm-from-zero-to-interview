# 学习进度追踪

## 当前阶段

阶段：第二轮全系列精修阶段

## 书籍完成度

1. 第一册：大纲完成；第 1-38 讲已扩写，正文第一版完成
2. 第二册：大纲完成；第 1-120 讲已扩写，正文第一版完成
3. 第三册：大纲完成；第 1-72 讲已扩写，正文第一版完成
4. 第四册：大纲完成；第 1-18 章已扩写，正文第一版完成
5. 第五册：大纲完成；第 1-14 章已扩写，正文第一版完成
6. 第六册：大纲完成；第 1-13 章已扩写，正文第一版完成
7. 第七册：大纲完成；第 1-12 章已扩写，正文第一版完成
8. 第八册：大纲完成；第 1-12 章已扩写，正文第一版完成
9. 第九册：大纲完成；第 1-12 章已扩写，正文第一版完成
10. 第十册：大纲完成；第 1-13 章已扩写，正文第一版完成
11. 第十一册：大纲完成；第 1-14 章已扩写，正文第一版完成
12. 第十二册：大纲完成；第 1-13 章已扩写，正文第一版完成
13. 第十三册：大纲完成；第 1-10 章已扩写，正文第一版完成
14. 第十四册：大纲完成；第 1-10 章已扩写，正文第一版完成
15. 第十五册：大纲完成；第 1-12 章已扩写，正文第一版完成
16. 第十六册：大纲完成；第 1-12 章已扩写，正文第一版完成
17. 第十七册：大纲完成；第 1-12 章已扩写，正文第一版完成
18. 第十八册：大纲完成；第 1-11 章已扩写，正文第一版完成
19. 第十九册：大纲完成；第 1-15 章已扩写，正文第一版完成
20. 第二十册：大纲完成；第 1-16 章已扩写，正文第一版完成
21. 第二十一册：大纲完成；第 1-12 章已扩写，正文第一版完成
22. 第二十二册：大纲完成；第 1-50 章已扩写，正文第一版完成
23. 第二十三册：大纲完成；第 1-60 章已扩写，正文第一版完成；另有早期 7 个合并版章节文件保留作历史材料或后续清理参考
24. 第二十四册：大纲完成；第 1-60 章已扩写，正文第一版完成
25. 大模型工程师面试补充章节：第 60-82 章已扩写，补充篇第一版完成

## 第二轮精修进度

1. 第一册 `book-01-core-30/chapters/01-基础与语言模型.md`：第 1-5 讲已完成首轮公式可读性修正与 demo 补充；新增批量 cross entropy、学习率对比、AdamW 解耦 weight decay 的 NumPy demo，并已用 `uv run python` 验证可运行。
2. 第一册 `book-01-core-30/chapters/02-transformer核心.md`：第 7-9 讲和第 11 讲 causal mask 相关内容已完成首轮公式可读性修正与 demo 补充；新增 Embedding/位置编码、scaled dot-product attention、attention 复杂度、mask-before-softmax 的最小 demo，并已用 `uv run --no-sync python` 验证可运行。
3. 第一册 `book-01-core-30/chapters/02-transformer核心.md`：第 10 讲 Multi-Head Attention 已完成首轮公式可读性修正与 demo 补充；将 `d_model`、`d_head` 等 shape 表达改为 GitHub Markdown 更稳的文本或简化数学符号，新增带 shape 断言和 causal mask 的最小 MHA demo，并已用 `uv run --no-sync python` 验证可运行。
4. 第一册 `book-01-core-30/chapters/02-transformer核心.md`：第 12 讲 Transformer Block 已完成首轮公式可读性修正与 demo 补充；将 Pre-LN/Post-LN、MLP 和残差公式改为 fenced math 或文本 shape 表达，新增带 shape 断言的 TinyPreLNBlock demo，用于验证 attention/MLP 输出必须和残差分支同形，并已用 `uv run --no-sync python` 验证可运行。
5. 第一册 `book-01-core-30/chapters/02-transformer核心.md`：第 13 讲 LayerNorm、RMSNorm 与残差连接已完成首轮公式可读性修正与 demo 补充；清理本讲 `\text{...}` 和 `d_{\text{model}}` 写法，改用 `z`、`d`、文本 shape 和 fenced math，新增手写 LayerNorm/RMSNorm 数值对比 demo，并已用 `uv run --no-sync python` 验证可运行。
6. 第一册 `book-01-core-30/chapters/02-transformer核心.md`：第 14 讲 RoPE 与长上下文已完成首轮公式可读性修正与 demo 补充；清理本讲 `\text{position}` 写法，将 RoPE 旋转公式改为 `R_i q_i`、`R_j k_j` 等更稳表达，新增二维旋转 demo 展示 attention score 随相对位置差变化，并已用 `uv run --no-sync python` 验证可运行。
7. 第一册 `book-01-core-30/chapters/02-transformer核心.md`：第 15 讲从零实现一个小 GPT 已完成首轮公式可读性修正与 demo 补充；将 input/label 右移公式改为文本 shape 表达，补充 causal LM input/label 构造 demo，并已用 `uv run --no-sync python` 验证可运行。
8. 第一册 `book-01-core-30/chapters/02-transformer核心.md`：完成全文件公式风险收尾检查；已清理剩余 `\text{...}`、`d_{\text{model}}`、`x_{<...}`、`y_{<...}` 等 GitHub Markdown 风险命中，并完成广告注入关键词和 `git diff --check` 检查。
9. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 16 讲 LLM 预训练流程已完成首轮 demo 补充；新增文档 packing、EOS 插入、定长切块和 input/label 右移构造的最小 Python demo，并已用 `uv run --no-sync python` 验证可运行；广告关键词命中仅为数据清洗正常语境。
10. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 17 讲数据质量与 Scaling Law 已完成首轮 demo 补充；新增固定 compute 下参数量与训练 token 数权衡的 FLOPs 估算 demo，展示 `C ≈ 6ND` 的直觉用途，并已用 `uv run --no-sync python` 验证可运行；广告关键词命中仅为数据质量正常语境。
11. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 18 讲分布式训练基础已完成首轮 demo 补充；新增 DDP、ZeRO-1、ZeRO-2、ZeRO-3/FSDP 的训练状态显存粗略估算 demo，展示参数、梯度和 AdamW optimizer states 分片对单卡显存的影响，并已用 `uv run --no-sync python` 验证可运行。
12. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 19 讲 Instruction Tuning 已完成首轮 demo 补充；新增不依赖外部 tokenizer 的 chat template 与 assistant-only label mask demo，展示 system/user token 设为 `-100`、assistant token 参与监督训练，并已用 `uv run --no-sync python` 验证可运行。
13. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 20 讲 SFT 已完成首轮公式可读性修正与 demo 补充；将 SFT 条件概率和目标函数改为 fenced math，新增 `torch.nn.functional.cross_entropy(ignore_index=-100)` demo，验证只有 assistant label 位置参与 loss，并已用 `uv run --no-sync python` 验证可运行。
14. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 21 讲 RLHF 已完成首轮公式可读性修正与 demo 补充；将 Reward Model pairwise ranking、pairwise loss 和 KL penalty 公式改为 GitHub Markdown 更稳的 fenced math 与简化下标，新增 `-log sigmoid(r_w-r_l)` 的最小 PyTorch demo，并已用 `uv run --no-sync python` 验证可运行。
15. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 22 讲 DPO 已完成首轮公式可读性修正与 demo 补充；将 DPO 偏好概率、policy/reference log probability 差和 DPO loss 改为 fenced math 与 `\mathrm{ref}`/`\mathrm{DPO}` 稳定下标，新增最小 DPO loss PyTorch demo，并已用 `uv run --no-sync python` 验证可运行。
16. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 23 讲 Reward Model 与 Reward Hacking 已完成首轮公式可读性修正与 demo 补充；将 pairwise ranking、pairwise loss 和 distribution shift 公式改为 fenced math 与稳定下标，新增代理 RM 分数和真实偏好背离的 reward hacking 玩具 demo，并已用 `uv run --no-sync python` 验证可运行。
17. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 24 讲推理采样策略已完成首轮公式可读性修正与 demo 补充；将 greedy decoding 和 temperature softmax 公式改为 fenced math 与简化变量，新增 temperature、top-k、top-p 分布过滤 demo，并已用 `uv run --no-sync python` 验证可运行。
18. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 25 讲 KV Cache 与推理加速已完成首轮公式可读性修正与 demo 补充；将 KV Cache 显存公式改为稳定符号 `M_kv`、`H_kv`、`D_h`、`b`，新增 MHA/GQA/MQA KV Cache 显存估算 demo，并已用 `uv run --no-sync python` 验证可运行。
19. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 26 讲 FlashAttention 与显存优化已完成首轮公式可读性修正与 demo 补充；将 attention 公式、复杂度和 `T^2` 示例改为 fenced math 或文本表达，新增显式 attention scores/probs 矩阵显存随序列长度平方增长的估算 demo，并已用 `uv run --no-sync python` 验证可运行。
20. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 27 讲量化与部署已完成首轮公式可读性修正与 demo 补充；将量化/反量化公式改为 `x_q`、`x_hat` 等稳定符号，将 13B 权重显存估算改为文本表达，新增 symmetric INT8 量化/反量化误差 demo，并已用 `uv run --no-sync python` 验证可运行；本文件常见公式风险模式已清理完毕。
21. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 28 讲幻觉、评估与安全已完成首轮 demo 补充；新增按任务切片和错误类型汇总评估结果的最小 Python demo，用于说明不能只看总分，还要追踪事实幻觉、推理错误、工具参数错误、过度拒答等错误类型，并已用 `uv run --no-sync python` 验证可运行。
22. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 29 讲开放研究问题已完成首轮结构化模板补充；新增研究卡片模板，将开放题拆成 research question、hypothesis、baseline、experiment、metrics、ablation 和 risks，帮助把研究方向转化为可验证实验计划；已完成公式风险、广告注入关键词和 `git diff --check` 检查。
23. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：第 30 讲模拟面试与复盘已完成首轮结构化模板补充；新增 mock interview 六维打分表，覆盖 correctness、structure、trade-off、engineering、evaluation 和 communication，并给出弱点修复动作示例。
24. 第一册 `book-01-core-30/chapters/03-训练对齐推理.md`：完成全文件第二轮阶段性收尾检查；第 16-30 讲已完成公式可读性修正、demo/模板补充或结构化增强，常见公式风险模式无命中，广告关键词仅保留数据清洗正常语境，`git diff --check` 通过。
25. 第一册 `book-01-core-30/chapters/04-多模态基础.md`：第 31 讲多模态大模型概览已完成首轮 demo 补充；新增图像 patch token 与视频帧 token 成本估算 demo，用于说明多模态输入会显著增加上下文长度、显存和延迟，并已用 `uv run --no-sync python` 验证可运行。
26. 第一册 `book-01-core-30/chapters/04-多模态基础.md`：第 32 讲 CLIP 与图文对齐已完成首轮 demo 补充与验证；验证原有 CLIP batch 内对比 loss 代码可运行，新增 zero-shot classification 相似度选择 demo，展示 image embedding 与多个文本 prompt embedding 比较后选择最高相似度类别，并已用 `uv run --no-sync python` 验证可运行。
27. 第一册 `book-01-core-30/chapters/04-多模态基础.md`：第 33 讲 Vision-Language Model 已完成首轮 demo 补充；新增 connector 将 image features 从 vision hidden size 投影到 LLM hidden size，并与 text embeddings 拼接的 shape demo，验证 `[B,N,d_v] -> [B,N,d] -> [B,N+T,d]` 数据流，并已用 `uv run --no-sync python` 验证可运行。
28. 第一册 `book-01-core-30/chapters/04-多模态基础.md`：第 34 讲 Diffusion Model 与图像生成已完成首轮 demo 补充与验证；验证原有单步 forward noising 代码可运行，新增不同 `alpha_bar` 下加噪强度变化的一维向量 demo，展示 timestep 越靠后原始信号占比越低、噪声占比越高，并已用 `uv run --no-sync python` 验证可运行。
29. 第一册 `book-01-core-30/chapters/04-多模态基础.md`：第 35 讲语音大模型已完成首轮 demo 补充；新增 16 kHz 音频在 20 ms frame shift 下的帧数估算 demo，展示音频输入随时长线性增长，并用于解释实时语音助手的 streaming、chunk size、首包延迟和打断处理成本，已用 `uv run --no-sync python` 验证可运行。
30. 第一册 `book-01-core-30/chapters/04-多模态基础.md`：第 36 讲视频生成与 Sora 类模型基础已完成首轮 demo 补充；新增视频时空 patch token 成本估算 demo，展示 token 数随帧数和分辨率快速增长，并已用 `uv run --no-sync python` 验证可运行。
31. 第一册 `book-01-core-30/chapters/04-多模态基础.md`：完成全文件第二轮阶段性收尾检查；第 31-36 讲已完成 demo 补充或结构化增强，常见公式风险模式、广告注入关键词和无关链接均无命中，`git diff --check` 通过。
32. 第一册 `book-01-core-30/chapters/05-面试与复习.md`：第 37 讲 OpenAI 风格开放题已完成首轮结构化模板补充；新增 10 分制开放题回答评分表，覆盖目标澄清、问题拆解、实验设计、trade-off 和表达可信度，帮助将开放题练习转为可复盘标准；已完成公式风险、广告注入关键词和 `git diff --check` 检查。
33. 第一册 `book-01-core-30/chapters/05-面试与复习.md`：第 38 讲模拟面试与总复习已完成首轮结构化模板补充；新增第一册最终验收打卡表，覆盖基础、优化、Transformer、训练、分布式、后训练、推理、评估安全、多模态、开放题、项目表达和 mock interview，用于进入第二册前的自查。
34. 第一册 `book-01-core-30/chapters/01-基础与语言模型.md`：完成全文件公式风险补充收尾；清理残留 `\text{...}`、`x_{<t}` 和 `\theta_{\text{new}}` 等 GitHub Markdown 风险写法，改为文本表达或 `\mathrm{...}` 下标，保持原有语义不变。
35. 第一册 `book-01-core-30/chapters/05-面试与复习.md`：完成全文件第二轮阶段性收尾检查；第 37-38 讲已完成结构化模板补充，常见公式风险模式、广告注入关键词和无关链接均无命中，`git diff --check` 通过。
36. 第一册 `book-01-core-30/chapters/`：完成第一册第二轮阶段性整体验收；5 个章节文件常见公式风险模式均无命中，广告关键词仅保留 `03-训练对齐推理.md` 数据清洗正常语境，`git diff --check` 通过。
37. 第二册 `book-02-advanced-100/chapters/01-深度学习与概率基础进阶.md`：第 1 讲概率建模视角下的语言模型已启动第二轮精修；将联合概率、自回归链式分解和三变量链式法则公式改为 fenced math，新增条件概率乘积与 negative log likelihood 的最小 Python demo，并已用 `uv run --no-sync python` 验证可运行。
38. 第二册 `book-02-advanced-100/chapters/01-深度学习与概率基础进阶.md`：第 2 讲交叉熵、KL 散度与信息论统一视角已完成首轮公式和代码精修；将交叉熵、KL、MLE、NLL 和 perplexity 公式改为 fenced math，新增数值稳定 softmax 与 cross entropy demo，说明减最大 logit 避免 `exp` 溢出，并已用 `uv run --no-sync python` 验证可运行。
39. 第二册 `book-02-advanced-100/chapters/01-深度学习与概率基础进阶.md`：第 3 讲优化景观与深度网络训练动态已完成首轮代码补充；新增一维二次函数上不同 learning rate 的梯度下降轨迹 demo，展示学习率过小收敛慢、合适学习率稳定下降、过大学习率震荡发散，并已用 `uv run --no-sync python` 验证可运行。
40. 第二册 `book-02-advanced-100/chapters/01-深度学习与概率基础进阶.md`：第 4 讲泛化、过拟合与双下降现象已完成首轮资料边界增强和代码补充；按 `WRITING_PLAN.md` 联网核对 Deep Double Descent 论文，补充 double descent 也可能随 epoch、数据量和 effective model complexity 出现的谨慎表述，新增双下降玩具曲线 demo，并已用 `uv run --no-sync python` 验证可运行。
41. 第二册 `book-02-advanced-100/chapters/01-深度学习与概率基础进阶.md`：第 5 讲贝叶斯视角、先验与不确定性已完成首轮代码补充；新增 Beta-Binomial 硬币后验更新 demo，展示先验、观测数据和后验均值的关系，用于解释数据少时先验影响大、数据多时似然影响大的直觉，并已用 `uv run --no-sync python` 验证可运行。
42. 第二册 `book-02-advanced-100/chapters/01-深度学习与概率基础进阶.md`：第 6 讲损失函数设计与优化目标错配已完成首轮代码补充；新增可运行的代理指标错配 demo，展示模型 loss 更低但 exact match 和 safety pass rate 更差的情况，用于说明单一训练指标不能替代任务指标、安全指标和 bad case 分析，并已用 `uv run --no-sync python` 验证可运行。
43. 第二册 `book-02-advanced-100/chapters/01-深度学习与概率基础进阶.md`：第 7 讲归一化、初始化与训练稳定性已完成首轮代码补充；按 `WRITING_PLAN.md` 核对 PyTorch `clip_grad_norm_` 接口资料，新增可直接运行的 tiny model 梯度范数裁剪 demo，展示裁剪前后 grad norm 变化，并已用 `uv run --no-sync python` 验证可运行；运行时出现本机 CUDA driver 警告但 CPU demo 正常完成。
44. 第二册 `book-02-advanced-100/chapters/01-深度学习与概率基础进阶.md`：第 8 讲激活函数、门控结构与 SwiGLU 已完成首轮代码补充；新增可运行的 GELU FFN 与 SwiGLU MLP shape/参数量对比 demo，验证 SwiGLU 输入输出 shape 不变，并展示通过调小 hidden dimension 可让 SwiGLU 参数量接近普通 `4*d_model` GELU FFN，已用 `uv run --no-sync python` 验证可运行。
45. 第二册 `book-02-advanced-100/chapters/01-深度学习与概率基础进阶.md`：第 9 讲优化器进阶已完成首轮资料边界核对和代码补充；按 `WRITING_PLAN.md` 核对 AdamW decoupled weight decay 论文与 PyTorch AdamW 文档，补充可运行的 tiny model 参数分组 demo，验证 Linear weight 进入 decay group、bias/norm 参数进入 no_decay group，并已用 `uv run --no-sync python` 验证可运行。
46. 第二册 `book-02-advanced-100/chapters/01-深度学习与概率基础进阶.md`：第 10 讲深度学习基础高频开放题已完成首轮代码化模板补充；新增开放题回答自评脚本，用关键词维度检查回答是否覆盖 target、mechanism、evaluation 和 tradeoff，帮助把开放题训练转化为可复盘标准，并已用 `uv run --no-sync python` 验证可运行。
47. 第二册 `book-02-advanced-100/chapters/01-深度学习与概率基础进阶.md`：完成全文件第二轮阶段性收尾检查；第 1-10 讲已完成公式兼容修正、代码 demo 补充、资料边界核对或结构化增强，常见公式风险模式、广告注入关键词和无关链接均无命中，`git diff --check` 通过。
48. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 11 讲 Self-Attention 的矩阵视角与信息路由已完成首轮公式和代码精修；将 `d_{\text{head}}` 等 attention 公式改为 fenced math 和 `d_h` 稳定符号，新增可运行的 causal self-attention shape/mask demo，验证输出 shape 和未来 token attention 权重为 0，并已用 `uv run --no-sync python` 验证可运行。
49. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 12 讲 Attention 复杂度瓶颈与稀疏化思路已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 FlashAttention、Longformer 和 BigBird 论文摘要边界，将复杂度符号统一为 `d_h`/`d_model`，补充 KV Cache 元素量公式、FlashAttention 与稀疏/线性 attention 的区别，并新增 sliding window mask 与 attention 成本估算 demo，已用 `uv run --no-sync python` 验证可运行；常见公式风险模式、广告注入关键词和 `git diff --check` 均通过。
50. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 13 讲 Pre-LN、Post-LN、Sandwich-LN 与 DeepNorm 已完成首轮资料边界核对和代码补充；按 `WRITING_PLAN.md` 核对 DeepNet/DeepNorm 论文摘要，补充 DeepNorm 是 residual connection 修改与配套初始化的组合设计，新增可运行的 Pre-LN/Post-LN 多层 toy block 输入梯度范数对比 demo，并已用 `uv run --no-sync python` 验证可运行；运行时出现本机 CUDA driver 警告但 CPU demo 正常完成。
51. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 14 讲 RMSNorm、ScaleNorm 与归一化简化已完成首轮资料边界核对、公式表达修正和代码补充；按 `WRITING_PLAN.md` 核对 RMSNorm、ScaleNorm 论文摘要及 PyTorch RMSNorm 文档入口，补充 RMSNorm 去掉 re-centering 但保留 re-scaling 的边界、ScaleNorm 的 L2 norm + 单一 scale 表述，并新增 LayerNorm/RMSNorm/ScaleNorm 手写对比 demo，已用 `uv run --no-sync python` 验证可运行；本讲新增内容已完成常见公式风险、广告注入关键词和 `git diff --check` 检查。
52. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 15 讲 FFN、GeLU、SwiGLU 与门控 MLP 已完成首轮资料边界核对、公式表达修正和代码补充；按 `WRITING_PLAN.md` 核对 GeLU、GLU 和 GLU variants/SwiGLU 论文摘要，补充 GeLU 的 `z * Phi(z)` 边界、GLU/SwiGLU 是逐 token MLP 门控而非 attention 机制的说明，将参数量公式改为更稳的 ASCII 表达，并新增 GeLU FFN 与 SwiGLU MLP 的 shape/参数量对比 demo，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
53. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 16 讲 RoPE 的数学直觉与位置外推已完成首轮资料边界核对、公式表达修正和代码补充；按 `WRITING_PLAN.md` 核对 RoFormer/RoPE、Position Interpolation 和 YaRN 论文摘要，补充 RoPE 是用旋转矩阵编码绝对位置并在 self-attention 中体现相对位置依赖的边界，以及长上下文扩展不能只靠直接外推或改 max length 的说明；新增二维 RoPE 点积相对距离等价 demo，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
54. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 17 讲 ALiBi、相对位置编码与长度泛化已完成首轮资料边界核对、公式表达修正和代码补充；按 `WRITING_PLAN.md` 核对 ALiBi 原论文和 T5 relative position bias 资料边界，补充 ALiBi 不加位置 embedding、而是在 query-key attention score 上加距离成比例惩罚的说明，并区分 ALiBi 线性 bias 与 T5 bucket learned bias；新增多 head ALiBi bias demo，验证 causal mask、距离越远 bias 越负、不同 slope 产生不同惩罚强度，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
55. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 18 讲 MQA、GQA 与推理效率优化已完成首轮资料边界核对、公式表达修正和代码补充；按 `WRITING_PLAN.md` 核对 MQA、GQA 和 PagedAttention 论文摘要，补充 MQA 通过共享 K/V 降低增量解码内存带宽成本、GQA 是 MQA 的中间 KV head 数泛化、PagedAttention 是 KV Cache 管理而非减少 KV head 数的边界；将 KV Cache 估算公式改为 ASCII 稳定表达，并新增 MHA/GQA/MQA KV Cache MiB 估算与 query head 到 KV head 分组映射 demo，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
56. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 19 讲 MoE 架构基础已完成首轮资料边界核对、公式表达修正和代码补充；按 `WRITING_PLAN.md` 核对 Sparsely-Gated MoE、GShard 和 Switch Transformer 论文摘要，补充 MoE 的 conditional computation 边界、GShard 的自动分片/通信视角以及 Switch top-1 routing 仍需负载均衡和稳定化技巧的说明；将 MoE 聚合公式改为 ASCII 稳定表达，并新增可运行 TinyTop1MoE routing demo，验证输出 shape、expert id 和 token counts，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
57. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 20 讲 MoE 训练稳定性与负载均衡已完成首轮资料边界核对、公式表达修正和代码补充；沿用并核对 Sparsely-Gated MoE、GShard 和 Switch Transformer 关于负载均衡、capacity、top-1 routing 与 router 稳定化的边界，补充 MoE 不能只堆 expert 数量、Switch z-loss 稳定 router logits 但不替代 load balancing loss 的说明，并新增可运行 capacity/overflow demo，展示 capacity factor 对 kept/dropped tokens 和 expert token counts 的影响，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
58. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 21 讲 Encoder-only、Decoder-only 与 Encoder-Decoder 再比较已完成首轮资料边界核对和代码补充；按 `WRITING_PLAN.md` 核对 BERT、GPT 类 decoder-only 和 T5 text-to-text encoder-decoder 的架构边界，补充 BERT 是 deep bidirectional representations、GPT 类训练/推理/产品交互的 next-token 闭环、T5 统一 text-to-text 但仍保留 encoder-decoder 条件生成优势的说明；新增可运行 attention mask 对比 demo，展示 encoder-only 双向 self-attention、decoder-only causal self-attention、encoder-decoder 的 encoder self-attention、decoder self-attention 与 cross-attention 信息流，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
59. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 22 讲状态空间模型与 Transformer 替代路线已完成首轮资料边界核对、公式表达修正和代码补充；按 `WRITING_PLAN.md` 核对 S4、Mamba 和 Mamba-2/SSD 论文摘要，补充 S4 是结构化 SSM 长序列路线、Mamba 通过输入相关 selective SSM 弥补内容相关推理弱点并使用硬件感知 scan、Mamba-2/SSD 强调 SSM 与 attention 结构联系而非简单替代的边界；将本讲 `O(T²)` 风险表达改为 `O(T^2)`，并新增可运行 scalar SSM 递推 demo，展示状态对历史输入的滚动压缩和 `A` 对遗忘速度的影响，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
60. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 23 讲混合架构：Attention、SSM 与卷积的结合已完成首轮资料边界核对和代码补充；按 `WRITING_PLAN.md` 核对 Mamba-2/SSD、Mamba 和 Conformer 论文摘要，补充混合架构不是简单拼装，而是在 attention/SSM/卷积的归纳偏置、效率和硬件实现之间重新组合的边界；新增可运行混合层粗略成本估算 demo，比较 all-attention、hybrid 和 mostly-SSM 三种层比例在长序列下的 relative cost，并强调成本下降不等于质量必然更好，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
61. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 24 讲模型宽度、深度、head 数和 hidden size 的设计已完成首轮资料边界核对、公式表达修正和代码补充；按 `WRITING_PLAN.md` 核对 Chinchilla compute-optimal scaling、LLaMA 和 GLU/SwiGLU 资料边界，补充模型尺寸不能脱离训练 token/compute、相对高效模型可通过更充分训练提升质量、SwiGLU intermediate size 需要按参数量和硬件取整设计的说明；将本讲 `d_model²` 和 `≈` 风险公式改为 ASCII 稳定表达，并新增可运行 Transformer block 参数量估算 demo，比较 GeLU `4d` FFN 与取整后 SwiGLU intermediate size 的参数量，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
62. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：第 25 讲现代 LLM 架构案例拆解已完成首轮资料边界核对、公式表达修正和代码补充；按 `WRITING_PLAN.md` 核对 LLaMA、GQA 和 RoPE 资料边界，补充 LLaMA 风格是现代 decoder-only 组件组合代表、GQA/MQA 主要服务推理效率、RoPE 服务位置建模且需结合训练长度和评估理解的说明；将案例中的 `intermediate_size ≈ 11008` 改为稳定表达，并新增可运行 LLM config 解析 demo，自动判断 GQA、KV Cache 相对 MHA 比例、SwiGLU-like MLP、RMSNorm 和 RoPE，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
63. 第二册 `book-02-advanced-100/chapters/02-transformer架构进阶.md`：完成第 11-25 讲首轮阶段性收尾；本文件已覆盖 Self-Attention、attention 复杂度、norm placement、RMSNorm/ScaleNorm、FFN/SwiGLU、RoPE、ALiBi、MQA/GQA、MoE、架构范式、SSM/混合架构、尺寸设计和现代 LLM 案例拆解的资料边界核对、公式表达修正和可运行 demo 补充；新增内容均已完成广告注入关键词和 `git diff --check` 检查。
64. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 26 讲 Tokenizer 进阶：BPE、Unigram 与 SentencePiece 已完成首轮资料边界核对和代码补充；按 `WRITING_PLAN.md` 核对 BPE 子词、Unigram/subword regularization 和 SentencePiece 资料边界，补充 BPE 缓解 open-vocabulary/rare words、Unigram 支持概率候选切分、SentencePiece 从 raw sentence 直接训练且不依赖空格预分词的说明；新增可运行 BPE merge 与字符/UTF-8 byte 压缩率对比 demo，展示高频 pair 合并和中文在 byte 粒度下更长的现象，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
65. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 27 讲数据清洗 pipeline 设计已完成首轮资料边界核对和代码补充；按 `WRITING_PLAN.md` 核对 C4/T5、Gopher 和 RefinedWeb 资料边界，补充数据清洗不是越狠越好，而是在规模、质量、多样性、安全和可追踪之间平衡，properly filtered and deduplicated web data 也可以有竞争力的说明；新增可运行 toy data cleaning pipeline demo，覆盖 HTML 清理、简单语言识别、质量过滤、安全关键词过滤、exact dedup、lineage 字段和统计输出，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
66. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 28 讲数据去重与 Benchmark Contamination 已完成首轮资料边界核对和代码补充；按 `WRITING_PLAN.md` 核对 Deduplicating Training Data Makes Language Models Better、RefinedWeb 和 rephrased contamination 资料边界，补充去重可降低记忆和 train-test overlap、filtered + deduplicated web data 的重要性，以及纯字符串匹配不足以覆盖改写/翻译污染的说明；新增可运行 exact dedup、word-set Jaccard near dedup 和 benchmark n-gram overlap contamination demo，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
67. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 29 讲数据配比与 Curriculum Learning 已完成首轮资料边界核对和代码补充；按 `WRITING_PLAN.md` 核对 T5/C4、Chinchilla compute-optimal scaling，并尝试核对 Curriculum Learning 原始资料但 ACM 页面受限，补充数据 mixture 不能脱离训练目标、数据质量和 compute budget，curriculum 是可实验 schedule 而非默认优于随机混合的说明；新增可运行 mixture/curriculum sampling demo，展示不同阶段 planned tokens、exposure multiplier 和小数据集重复暴露风险，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
68. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 30 讲合成数据的价值与风险已完成首轮资料边界核对和代码补充；按 `WRITING_PLAN.md` 核对 Self-Instruct、Orca 和 rephrased contamination 资料边界，补充合成指令需要过滤无效/重复/相似样本、蒸馏应关注复杂解释轨迹而非只模仿风格、合成数据也可能造成 benchmark contamination 的说明；新增可运行 synthetic data filtering demo，覆盖答案存在性、质量阈值、near duplicate、benchmark overlap 和 synthetic ratio 计算，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
69. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 31 讲 Scaling Law 经典形式已完成首轮资料边界核对、公式表达修正和代码补充；按 `WRITING_PLAN.md` 核对 Kaplan scaling law、Chinchilla 和 emergent abilities 资料边界，补充经典 scaling law 主要拟合 cross-entropy loss 的幂律趋势、曲线依赖数据/架构/tokenizer/recipe 条件、compute-optimal scaling 关注预算切分、下游离散能力可能呈非线性显现的说明；将本讲 `C ≈ 6ND`、`L(N)=A/N^α+...` 等风险公式改为 ASCII 稳定表达，并新增可运行 power-law loss toy demo，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
70. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 32 讲 Chinchilla Scaling Law 已完成首轮资料边界核对、公式表达修正和代码补充；按 `WRITING_PLAN.md` 核对 Chinchilla、Gopher 和 Kaplan scaling law 资料边界，补充 Chinchilla 研究的是固定 compute 下模型大小与训练 token 数的分配，指出许多早期大模型 undertrained，但不是机械主张模型越小越好的说明；将本讲 `C ≈ 6ND` 风险公式改为 `C = 6 * N * D` 稳定表达，并新增可运行固定 compute 权衡 toy demo，展示参数量增大时可训练 token 数下降且存在中间候选最优点，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
71. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 33 讲 Scaling Law 的局限与新趋势已完成首轮资料边界核对和代码补充；按 `WRITING_PLAN.md` 核对 emergent abilities 和 self-consistency/test-time compute 资料边界，补充 emergent abilities 不等于所有能力神秘不可预测、平均 loss 可较平滑但离散任务指标可能因阈值或评分方式非线性显现的说明，并明确第三个检索结果偏离主题未纳入正文；新增可运行 scaling extrapolation shift demo，展示小规模 power-law 外推在大规模数据分布变化后会系统性低估 loss 风险，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
72. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 34 讲训练 loss、验证 loss 与下游能力的关系已完成首轮资料边界核对、公式表达修正和代码补充；按 `WRITING_PLAN.md` 核对 Hugging Face perplexity 文档和 Kaplan scaling law，补充 PPL 是序列平均 negative log-likelihood 指数形式、适用于 causal LM 且受 tokenizer/验证集/上下文切分影响，loss/PPL 是预训练质量信号但不能替代能力、偏好和安全评估的说明；将本讲 PPL 和 LM loss 风险公式改为稳定文本表达，并新增完整可运行 loss/PPL 与目标能力错配 demo，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
73. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 35 讲训练稳定性：loss spike、NaN 与梯度爆炸已完成首轮资料边界核对和代码补充；按 `WRITING_PLAN.md` 核对 PyTorch `clip_grad_norm_` 和 `torch.isfinite` 文档入口，补充 `clip_grad_norm_` 计算全局梯度范数并原地裁剪、返回裁剪前范数，AMP 页面请求失败但保留先 `unscale_` 再 clip 的标准流程说明；新增完整可运行 tiny gradient clipping demo，验证 finite loss 检查、裁剪前 grad norm、`clip_grad_norm_` 返回值和裁剪后 grad norm，已用 `uv run --no-sync python` 验证可运行；运行时出现本机 CUDA driver 警告但 CPU demo 正常完成，本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
74. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 36 讲混合精度训练：FP16、BF16 与 FP8 已完成首轮资料边界核对、公式表达修正和代码补充；按 `WRITING_PLAN.md` 核对 PyTorch AMP/type info 文档入口和 NVIDIA Transformer Engine FP8 文档，补充 FP8 依赖 scaling recipe、amax 统计、FP8 tensor 格式、高精度 accumulation 和硬件 kernel，不是单独 dtype 开关的说明；将稳定 softmax 公式改为 ASCII 文本表达，并新增可运行 `torch.finfo` dtype 范围与 FP16 overflow demo，展示 FP16 在 70000 以上溢出而 BF16 保持 finite 但精度更粗，已用 `uv run --no-sync python` 验证可运行；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
75. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 37 讲 Checkpoint 策略与训练恢复已完成首轮资料边界核对和代码补充；按 `WRITING_PLAN.md` 核对 PyTorch 保存/加载模型、checkpoint 和随机性资料，修正 `torch.load` 为可信 training checkpoint 场景下显式 `weights_only=False`，补充可运行完整恢复 vs 只恢复权重 demo，验证完整恢复参数差异 `0.0`、只恢复权重差异约 `0.0151`，说明 optimizer state 和 scheduler state 对恢复轨迹的影响；本讲范围内常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
76. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 38 讲评估集设计与 early stopping 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 PyTorch-Ignite EarlyStopping、PyTorch 保存最佳模型资料和 early stopping 官方示例边界，补充 validation loss early stopping 的 `min_delta`/`patience` 判定公式、二项准确率标准误差与粗略 95% 置信区间公式，并修正 checkpoint selection demo 的典型输出为实际触发位置 `index: 4`；已用 `env UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 checkpoint selection demo 可运行，输出 eligible checkpoint 为 `ckpt_1T`。
77. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 39 讲多语言预训练已启动首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 mT5 与 XLM-R 等多语言预训练资料边界，将温度采样公式改为 `q_i = p_i^alpha / sum_j p_j^alpha` 并补充 `alpha = 1 / T` 的等价关系，新增可运行 temperature sampling 与低资源语言 exposure demo，展示温度升高会提高低资源语言采样比例但也增加重复暴露风险；已用 `env UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行；本次新增内容已完成常见公式风险、广告注入关键词和 `git diff --check` 检查，命中项均为历史记录或数据清洗正常语境。
78. 第二册 `book-02-advanced-100/chapters/03-tokenization数据与预训练进阶.md`：第 40 讲代码模型预训练已启动首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 HumanEval/Codex pass@k 评估公式和 Fill-in-the-Middle 训练资料边界，新增 FIM 样本构造 demo，说明 `<fim_prefix>`、`<fim_suffix>`、`<fim_middle>` 的左右上下文与中间片段关系；补充 pass@k 无偏估计公式 `1 - C(n-c,k) / C(n,k)` 和最小 Python 实现，展示 `pass@k` 随候选数上升但不能替代 `pass@1` 与真实产品可靠性；已用 `env UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证两个 demo 均可运行；本次新增内容已完成常见公式风险、广告注入关键词和 `git diff --check` 检查，命中项均为历史记录或数据清洗正常语境。
79. 第二册 `book-02-advanced-100/chapters/04-大规模训练系统进阶.md`：第 42 讲 ZeRO 与 FSDP 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 ZeRO/FSDP 训练状态分片资料边界，补充 mixed precision AdamW 下参数、梯度、optimizer state 显存估算公式，以及 DDP、ZeRO-1、ZeRO-2、ZeRO-3/FSDP 单卡训练状态显存对比公式；新增可运行纯 Python memory_by_stage demo，展示 7B 模型、8 卡口径下 DDP/ZeRO-1/ZeRO-2/ZeRO-3 单卡训练状态显存约为 104.31/35.86/24.45/13.04 GiB；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险和广告关键词检查通过，`git diff --check` 通过。
80. 第二册 `book-02-advanced-100/chapters/04-大规模训练系统进阶.md`：第 43 讲 Tensor Parallel 已完成首轮代码精修；按 `WRITING_PLAN.md` 核对 Megatron-LM 论文关于 intra-layer model parallelism 的资料边界，将原 Column Parallel/Row Parallel 伪代码升级为可运行 PyTorch demo，模拟 column-parallel up projection、local activation、row-parallel down projection 和 all-reduce sum，并验证 TP 输出与完整 MLP 输出在浮点容差内一致；已用 `uv run --no-sync python` 验证 demo 可运行，常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
81. 第二册 `book-02-advanced-100/chapters/04-大规模训练系统进阶.md`：第 44 讲 Pipeline Parallel 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 GPipe 论文和 Megatron-LM 大规模训练论文资料边界，补充 pipeline bubble 的粗略利用率公式 `U ≈ M / (M + P - 1)` 和 bubble 比例 `B ≈ (P - 1) / (M + P - 1)`，并新增 forward-only pipeline ASCII timeline demo，对比 `P=4`、`M=2` 与 `M=8` 时的利用率和 bubble；已用 `uv run --no-sync python` 验证 demo 可运行，常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
82. 第二册 `book-02-advanced-100/chapters/04-大规模训练系统进阶.md`：第 45 讲 Sequence Parallel 与 Context Parallel 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 Reducing Activation Recomputation in Large Transformer Models 和 Ring Attention 论文资料边界，补充 activation 沿 sequence 分片的显存公式 `M_full = B*T*H*b` 与 `M_sp = B*T*H*b/S` 的稳定 MathJax 写法，新增可运行 demo 展示 SP=4 时 `[B,T,H]` activation 单 rank 显存下降，以及 blockwise online softmax attention 与 full attention 输出等价；已用 `uv run --no-sync python` 验证 demo 可运行，常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
83. 第二册 `book-02-advanced-100/chapters/04-大规模训练系统进阶.md`：第 46 讲 3D Parallelism 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 Megatron-LM 论文关于 tensor/pipeline/data parallel 组合的资料边界，将 `world_size = DP * TP * PP` 改为稳定数学符号 `W = D*T*P` 并补充单模型副本 rank 数 `G_replica = T*P`、`D = W/(T*P)` 的解释，新增可运行 rank group mapping demo，展示 `TP=2, PP=4, DP=2` 时 TP group、PP group 和 DP group 如何由 16 个 rank 组成；已用 `uv run --no-sync python` 验证 demo 可运行，常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
84. 第二册 `book-02-advanced-100/chapters/04-大规模训练系统进阶.md`：第 47 讲 Activation Checkpointing 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 PyTorch checkpoint 文档入口和 Reducing Activation Recomputation in Large Transformer Models 论文资料边界，补充 activation 显存线性估算 `M_act ∝ B*T*H*L` 与 `M_act ≈ B*T*H*L*c*b` 的稳定 MathJax 写法，新增可运行 CountingBlock demo，验证不开 checkpoint 时 forward 调用 1 次、开启 checkpoint 后 forward 调用 2 次且梯度范数一致；已用 `uv run --no-sync python` 验证 demo 可运行，运行时本机 CUDA driver warning 不影响 CPU demo 完成，常见公式风险、广告注入关键词和 `git diff --check` 检查通过。
85. 第二册 `book-02-advanced-100/chapters/04-大规模训练系统进阶.md`：第 48 讲通信与计算重叠已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 PyTorch DDP/FSDP 文档和 NCCL/CUDA stream 资料边界，补充 `T_serial = T_comp + T_comm`、`T_overlap = T_comp + T_exposed`、`T_hidden = T_comm - T_exposed` 和 overlap efficiency 的稳定 MathJax 写法，新增可运行纯 Python backward bucket overlap 时间线 demo，展示 105 ms 通信中 70 ms 被 backward compute 隐藏、最终 step time 从串行 205 ms 降到 135 ms；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险和广告关键词检查通过，`git diff --check` 通过。
86. 第二册 `book-02-advanced-100/chapters/04-大规模训练系统进阶.md`：第 49 讲训练吞吐指标与性能分析已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 MFU/HFU、PyTorch Profiler 和 Nsight 资料边界，将 tokens/s、`C_train ≈ 6ND`、MFU、scaling efficiency、GPU-days 等关键指标改为稳定 MathJax 写法，新增可运行纯 Python training metrics demo，计算 tokens/step、global tokens/s、per-GPU tokens/s、model FLOPs/s、MFU 和 scaling efficiency，并修正示例配置避免 MFU 大于 1；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险和广告关键词检查通过，`git diff --check` 通过。
87. 第二册 `book-02-advanced-100/chapters/04-大规模训练系统进阶.md`：第 50 讲大规模训练工程事故分析已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 PyTorch `torch.isfinite`、checkpoint 保存/加载、NCCL troubleshooting 和 NVIDIA Xid 资料边界，补充 `r_loss`、`r_tok`、`u_mem`、`s_rank` 等事故报警指标的稳定 MathJax 写法，新增可运行纯 Python incident classifier demo，基于 loss、grad norm、tokens/s、显存、per-rank step time、checkpoint 状态和 heartbeat 自动标记 loss spike、throughput drop、OOM risk、straggler、checkpoint bad 和 hang risk；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险和广告关键词检查通过，`git diff --check` 通过。
88. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 51 讲 Base Model 到 Assistant Model 的能力转换已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 InstructGPT、OpenAI Model Spec 和后训练/SFT 资料边界，新增 assistant SFT loss、pairwise preference loss 和 unsafe risk rate 的稳定 MathJax 写法，并补充可运行纯 Python 行为分布重定向 demo，展示 SFT bias、preference reward 和 safety penalty 如何把概率质量从续写式/危险回答转向 helpful assistant answer；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险和广告关键词检查通过，`git diff --check` 通过。
89. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 52 讲 SFT 数据设计进阶已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 Hugging Face chat template、TRL SFTTrainer assistant-only loss 和常规 SFT 数据格式资料边界，新增 assistant-only SFT loss 公式和 `labels[t] = token_id[t] / -100` 的 mask 规则，补充可运行纯 Python assistant-only label mask 与 masked cross entropy demo，并修正 demo 中重复 token 建词表导致 token id 空洞的问题；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险和广告关键词检查通过，`git diff --check` 通过。
90. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 53 讲 SFT 的泛化与灾难性遗忘已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 catastrophic forgetting、SFT regression eval 和 checkpoint selection 资料边界，补充能力变化量 `Delta_m`、遗忘量 `F_m`、风险增量 `R_m` 和 checkpoint gate 的稳定 MathJax 写法，新增可运行纯 Python regression gate demo，展示 assistant win-rate 提升但 HumanEval 下降和 false refusal rate 上升时不应选择该 checkpoint；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险和广告关键词检查通过，`git diff --check` 通过。
91. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 54 讲 Preference Data 构造已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 InstructGPT 偏好数据/RM/PPO 资料边界，新增偏好数据偏差审计公式 `b_len`、`Delta_len`、`b_pos`，补充可运行纯 Python preference data bias audit demo，统计 chosen/rejected 长度差、chosen 位置偏差、偏好原因分布和安全类别分布，并能对长度偏差和位置偏差输出 warning；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行。
92. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 55 讲 Reward Model 训练细节已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 InstructGPT 与 TRL RewardTrainer 资料边界，将 value head、Bradley-Terry pairwise preference probability、RM loss、pairwise accuracy、reward margin 和 reward-length correlation 改为稳定 MathJax 写法，补充可运行纯 Python RM diagnostics demo，计算 pairwise loss、accuracy、平均 reward margin、长度相关性和 rejected 排名反超分桶，并能输出排序失败与长度偏差 warning；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行。
93. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 56 讲 PPO 在 RLHF 中的目标函数已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 PPO 原论文、InstructGPT RLHF 流程和 TRL PPOTrainer 资料边界，将 RLHF reward、token-level KL penalty、advantage、policy ratio、PPO clipped objective、total loss 和 value loss 改为稳定 MathJax 写法，补充可运行纯 Python PPO clip/KL reward demo，逐 token 展示 ratio、clipped ratio、advantage、objective、clip fraction，并计算 RM score 扣除 reference KL 后的 final reward；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行。
94. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 57 讲 RLHF 的不稳定性已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 InstructGPT、PPO 和 TRL PPOTrainer 资料边界，新增 reward 增量、人工胜率增量、长度漂移、误拒率增量、基础能力回退和 checkpoint gate 的稳定 MathJax 写法，补充可运行纯 Python RLHF checkpoint diagnostics demo，展示 reward 更高的 `ppo_400` 因 KL、长度、false refusal、基础能力、clip fraction 和 value loss 风险被 gate 拦截；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行。
95. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 58 讲 DPO 数学推导已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 DPO 原论文和 TRL DPOTrainer 资料边界，将 KL 正则化 RLHF 目标、DPO loss、log-ratio 定义、最优策略、隐式 reward、Bradley-Terry 偏好概率和 response log probability 求和改为稳定 MathJax 写法，清理 `$$`、`pi*` 和比例符号风险写法，补充可运行纯 Python DPO loss demo，展示 chosen/rejected log-ratio、DPO logit、implicit reward gap 和 batch loss；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行。
96. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 59 讲 IPO、KTO、ORPO 与偏好优化变体已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 IPO 相关偏好学习理论论文、KTO 论文、ORPO 论文和 TRL 文档资料边界，将 DPO margin、DPO loss、IPO target margin、ORPO SFT+preference loss 和 odds 公式改为稳定 MathJax 写法，补充可运行纯 Python preference-variant objectives demo，对比 DPO logistic loss、IPO margin regression、KTO 单样本 desirable/undesirable 反馈和 ORPO-like SFT+preference 组合目标；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行。
97. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 60 讲 RLAIF 与 Constitutional AI 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 Constitutional AI 论文与 Anthropic 官方资料边界，补充 AI judge 与人工抽检一致率、AI 位置偏差率和类别覆盖率的稳定 MathJax 写法，新增可运行纯 Python RLAIF feedback audit demo，统计 AI-human agreement、first-position choice rate 和 category coverage，并能对人工校准不足和位置偏差输出 warning；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行。
98. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 61 讲安全对齐数据与拒答策略已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 Constitutional AI 资料边界，并结合行为规范章节要求补充 unsafe compliance rate 与 false refusal rate 的稳定 MathJax 写法，新增可运行纯 Python safety eval metrics demo，按 `disallowed`/`benign` 请求统计危险服从率、误拒率和分风险类别错误率，并能同时提示 unsafe help 与 benign over-refusal 风险；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行。
99. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 62 讲 Truthfulness、Honesty 与 Calibration 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 TruthfulQA 与 On Calibration of Modern Neural Networks 资料边界，补充 factual accuracy、hallucination rate、unsupported claim rate、分桶 accuracy/confidence、ECE、Brier Score 和平均置信差距的稳定 MathJax 写法，新增可运行纯 Python calibration bin audit demo，计算每个置信度桶的实际正确率、平均置信度、ECE、Brier、类别正确率和高置信错误 warning；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险无命中，广告关键词命中仅为历史记录和第 63 讲安全语境中的 `base64`，`git diff --check` 通过。
100. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 63 讲 Jailbreak、Prompt Injection 与鲁棒性已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 OWASP Top 10 for LLM Applications、Indirect Prompt Injection 和 Prompt Injection attack against LLM-integrated Applications 资料边界，补充 ASR、instruction hijack rate、sensitive information leak rate、unsafe tool call rate、task completion under injection、benign false refusal rate 和联合 release gate 的稳定 MathJax 写法，新增可运行纯 Python prompt injection eval audit demo，用抽象标签统计攻击成功、任务劫持、信息泄露、越权工具调用、注入场景任务完成率和正常请求误拒率；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险无命中，广告关键词命中仅为历史记录和本讲安全语境中的 `base64`，`git diff --check` 通过。
101. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 64 讲 Model Spec 与行为规范已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 OpenAI Model Spec 官方资料边界，补充 spec compliance rate、violation rate、weighted violation risk、chain-of-command error rate、tool use error rate、honesty error rate 和 spec release gate 的稳定 MathJax 写法，新增可运行纯 Python Model Spec eval audit demo，把 `rule`、`expected`、`severity`、chain/tool/honesty 错误标注转成分项指标和 failed-by-rule 诊断；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，最新 Model Spec 站点请求出现 SSL EOF 但历史官方版可用，常见公式风险无命中，广告关键词命中仅为历史记录和第 63 讲安全语境中的 `base64`，`git diff --check` 通过。
102. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：第 65 讲后训练评估体系已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 IFEval、MT-Bench/Chatbot Arena 与 LLM-as-a-judge 资料边界，补充 checkpoint 相对 baseline 指标变化、hard gate、soft score 和 release gate 的稳定 MathJax 写法，新增可运行纯 Python checkpoint selection demo，对比 `sft_1200`、`dpo_1800`、`dpo_2100` 的基础能力、安全、误拒、幻觉、长度漂移和软收益，展示软分最高但不过 hard gate 的 checkpoint 不应发布；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险无命中，广告关键词命中仅为历史记录和第 63 讲安全语境中的 `base64`，`git diff --check` 通过。
103. 第二册 `book-02-advanced-100/chapters/05-后训练与对齐进阶.md`：完成第 51-65 讲第二轮阶段性收尾；本章已覆盖 Base 到 Assistant、SFT 数据设计、灾难性遗忘、偏好数据、Reward Model、PPO、DPO、IPO/KTO/ORPO、RLAIF/Constitutional AI、安全拒答、truthfulness/calibration、jailbreak/prompt injection、Model Spec 和后训练评估体系的公式修正、资料边界核对与可运行 demo 补充；整章常见公式风险无命中，广告关键词命中仅为历史记录和第 63 讲安全语境中的 `base64`，`git diff --check` 通过。
104. 第二册 `book-02-advanced-100/chapters/06-推理部署与服务进阶.md`：第 66 讲 Prefill 与 Decode 阶段性能模型已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 PagedAttention 论文和 vLLM bench serve 文档资料边界，补充 TTFT、TPOT、端到端延迟、prefill/decode throughput、线性层 FLOPs、attention 粗略复杂度和 KV cache 显存估算的稳定 MathJax 写法，并新增可运行纯 Python inference performance toy model demo，对比 `short_chat`、`long_prompt`、`long_output` 的 TTFT、TPOT、总延迟和 KV cache 显存，说明长 prompt 与长 output 的不同瓶颈；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，SGLang/旧 vLLM metrics 路径返回 404 且未作为正文依据，另一次 Orca 论文 ID 检索命中无关论文未纳入正文；常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
105. 第二册 `book-02-advanced-100/chapters/06-推理部署与服务进阶.md`：第 67 讲 KV Cache 管理与分页注意力已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 延续核对 PagedAttention 论文资料边界，补充单请求/多请求 KV cache 显存、单 token KV bytes、4096 token 与 16 并发估算、block 数、单请求浪费 token 上界和整体碎片率的稳定 MathJax 写法，并新增可运行纯 Python paged KV allocation demo，对比连续预留与 paged allocation 的 token 空间浪费、演示 block table 定位 `token_37_location`，以及 prefix sharing 可节省的物理 block 数；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
106. 第二册 `book-02-advanced-100/chapters/06-推理部署与服务进阶.md`：第 68 讲 Continuous Batching 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 延续核对 PagedAttention/vLLM serving 资料边界，补充每轮 token budget、KV block budget 和调度资源解释的稳定 MathJax 写法，并新增可运行纯 Python mini scheduler demo，模拟 waiting/running/ready 队列、chunked prefill、decode step、KV block 分配释放和 running batch 动态变化，展示 `D_long` 被分块 prefill、A/C/E 等短请求完成后释放 KV blocks；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
107. 第二册 `book-02-advanced-100/chapters/06-推理部署与服务进阶.md`：第 69 讲 Speculative Decoding 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 Fast Inference from Transformers via Speculative Decoding、Accelerating Large Language Model Decoding with Speculative Sampling 与 vLLM speculative decoding 文档边界，补充 target/draft 分布、接受概率、拒绝后的修正分布、多 token 条件分布、平均推进 token 数和粗略加速比的稳定 MathJax 写法，并新增可运行纯 Python speculative sampling demo，对比 `good_draft` 与 `bad_draft` 的接受率、经验输出分布和加速比估算，说明严格接受-拒绝与 residual 修正分布能保持 target 分布；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，vLLM 旧 `spec_decode` 路径重定向到新文档且未作为正文直接依据，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
108. 第二册 `book-02-advanced-100/chapters/06-推理部署与服务进阶.md`：第 70 讲 Medusa、EAGLE 与多 token 预测已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 Medusa 与 EAGLE 论文资料边界，补充 Medusa head proposal 分布、候选路径数、Medusa 多 head 训练损失、EAGLE 特征预测骨架、平均接受长度和粗略端到端加速比的稳定 MathJax 写法，并新增可运行纯 Python candidate tree verification demo，模拟 3 个 Medusa heads 的候选组合、逐条验证 token 检查数、共享前缀树节点数、最佳可接受前缀和粗略 speedup；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，FlashAttention-2 检索命中为相邻系统优化资料且未作为本讲正文依据，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
109. 第二册 `book-02-advanced-100/chapters/06-推理部署与服务进阶.md`：第 71 讲量化感知训练与后训练量化已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 GPTQ、AWQ 与 PyTorch quantization 文档资料边界，补充 affine/symmetric quantization、scale/zero-point、clamp、MAE/RMSE、group-wise scale、fake quantization/STE、GPTQ 输出重构目标和 AWQ activation-aware 重要通道分数的稳定 MathJax 写法，并新增可运行纯 Python INT4 quantization demo，对比含 outlier 权重下 per-tensor 与 group-wise 的 weight/output RMSE、payload bytes 和 scale 元数据开销；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，PyTorch quantization 页面重定向到旧文档且仅作接口背景线索，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
110. 第二册 `book-02-advanced-100/chapters/06-推理部署与服务进阶.md`：第 72 讲 KV Cache 量化已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 KIVI、KVQuant 与 vLLM quantized KV cache 文档资料边界，补充多请求 KV cache 显存、K/V attention 误差路径、线性量化、per-tensor/per-token/per-channel/per-head/group-wise scale、payload/scale 元数据、量化后总 KV 显存和粗略最大并发估算的稳定 MathJax 写法，并新增可运行纯 Python KV quantization attention demo，展示 key channel outlier 下 per-channel K 更好保持 score、value token outlier 下 per-token V 更好保持输出，同时对比 FP16、INT4 per-tensor 与 INT4 channel/token 的 toy bytes；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，用户提到的 arXiv `2402.01306` 实为 KTO 且未作为本讲正文依据，GitHub KIVI 页面请求卡住后未采用，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
111. 第二册 `book-02-advanced-100/chapters/06-推理部署与服务进阶.md`：第 73 讲 Serving 系统设计已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 vLLM online serving 路径、Hugging Face TGI launcher 资源控制文档和 SGLang serving 文档边界，补充 token-aware routing score、prefill/decode pressure、KV block 预算、TTFT/TPOT 拆解、资源账户限流、容量规划和最大并发估算的稳定 MathJax 写法，并新增可运行纯 Python routing+scheduling demo，对比 naive request-count routing 与 token/KV-aware routing 在长 RAG、VIP 短请求和代码 Agent 请求下的副本选择、KV 可行性、估计 TTFT 和最终队列状态；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，vLLM 旧 OpenAI server 路径重定向到 online serving，SGLang 相关路径返回 404 且未作为正文依据，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
112. 第二册 `book-02-advanced-100/chapters/06-推理部署与服务进阶.md`：第 74 讲 成本模型：训练成本与推理成本已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 Scaling Laws、Chinchilla、Hugging Face TGI launcher 资源控制文档和 vLLM metrics 文档边界，补充训练 FLOPs、有效算力、训练小时、final run GPU 成本、真实训练总成本、端到端推理延迟、单请求成本、prefill/decode 容量 GPU 数和单位成功任务成本的稳定 MathJax 写法，并新增可运行纯 Python cost model demo，同时估算 7B/1T token final run 成本、30% 实验开销、serving 所需 GPU 数、单次请求成本和 cost per successful answer，对比小模型单次请求便宜但成功率/重试/人工修正使有效成本更高的场景；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，示例价格均标明为假设数字，vLLM metrics 路径返回 404 且未作为正文依据，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
113. 第二册 `book-02-advanced-100/chapters/06-推理部署与服务进阶.md`：第 75 讲 端侧与小模型部署已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 llama.cpp/GGUF、Core ML compression、Google LiteRT、ONNX Runtime mobile 资料边界，补充端侧权重内存、单 token KV Cache、端侧峰值内存、包体大小、量化压缩比、蒸馏目标、结构化剪枝、端侧延迟、能耗预算和端云协同路由打分的稳定 MathJax 写法，并新增可运行纯 Python edge/cloud routing demo，模拟 1.5B INT4 端侧模型在隐私笔记、代码 debug、离线意图识别和长合同请求下的权重/KV/峰值内存、端侧延迟、能耗、质量估计、云端延迟和路由决策；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，ONNX Runtime 原始 markdown 路径返回 404 但官网 mobile 页面可用，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
114. 第二册 `book-02-advanced-100/chapters/07-长上下文rag与agent.md`：第 76 讲 长上下文训练方法已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 Position Interpolation、Lost in the Middle、YaRN 和 LongBench 论文摘要资料边界，补充 RoPE 相对位置 score、Position Interpolation 映射、RoPE scaling 抽象相位、长短数据混合 LM loss、attention FLOPs 和 KV Cache 显存估算的稳定 MathJax 写法，并新增可运行纯 Python long-context budget demo，展示 4k 扩到 128k 时位置映射、RoPE 高频相位压缩、attention 成本平方增长、KV Cache 线性增长、continued pretraining 长短数据配比和 lost-in-the-middle drop；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
115. 第二册 `book-02-advanced-100/chapters/07-长上下文rag与agent.md`：第 77 讲 长上下文评估陷阱已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 Lost in the Middle、LongBench、RULER 和 Ragas 论文摘要资料边界，补充总体准确率、分桶准确率、lost-in-the-middle drop、位置鲁棒性比例、多证据证据召回、多证据严格成功率、引用准确率、unsupported claim rate、质量成本 release score、RAG 检索召回和 answer faithfulness 的稳定 MathJax 写法，并新增可运行纯 Python long-context eval audit demo，展示按长度/证据位置/任务类型分桶、严格多证据成功率、引用与 unsupported claim、质量成本分数和 RAG 失败归因；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
116. 第二册 `book-02-advanced-100/chapters/07-长上下文rag与agent.md`：第 78 讲 RAG 基础架构已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 RAG、DPR、REALM 和 Ragas 论文摘要资料边界，补充 RAG 的参数记忆与非参数检索记忆结合脉络、dense retrieval top-k、RAG 生成边缘化概率、hybrid retrieval 融合分数、reranker 交互打分、context token budget、recall/precision/MRR/nDCG、unsupported claim rate、permission violation rate、端到端延迟和单请求成本的稳定 MathJax 写法，并新增可运行纯 Python mini RAG pipeline demo，覆盖权限过滤、BM25-like 稀疏检索、toy dense 检索、hybrid 融合、rerank、context 预算和错误归因；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行并校准典型输出，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
117. 第二册 `book-02-advanced-100/chapters/07-长上下文rag与agent.md`：第 79 讲 Embedding Model 与向量检索已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 DPR、Sentence-BERT、HNSW、FAISS/PQ 和 MTEB 论文摘要资料边界，补充 embedding retrieval 的表示学习/相似度计算/索引系统三层定位、双塔编码与 top-k 检索、softmax contrastive loss、in-batch negative 分数矩阵、false negative 条件、cosine/dot/L2 等价关系、exact search 计算与存储成本、IVF 扫描候选估算、PQ payload bytes、Recall/Precision/MRR/nDCG 的稳定 MathJax 写法，并新增可运行纯 Python toy embedding retrieval demo，覆盖 cosine/dot/L2 排序差异、contrastive loss、false negative 风险、检索指标、toy IVF 召回损失和 PQ 压缩直觉；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
118. 第二册 `book-02-advanced-100/chapters/07-长上下文rag与agent.md`：第 80 讲 Reranker 与检索质量优化已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 MS MARCO、MonoT5、ColBERT、Sentence-BERT 和 BEIR 论文摘要资料边界，补充 reranker 在两阶段检索中的历史脉络、retriever 候选集合与 rerank top-M、bi-encoder/cross-encoder/late interaction 分数、context precision 与 evidence coverage、metadata-aware final score、权限硬过滤、pointwise/pairwise/listwise loss、Precision/MRR/nDCG、上线收益分数、rerank 延迟模型和 context token budget 的稳定 MathJax 写法，并新增可运行纯 Python rerank quality demo，覆盖权限过滤、cross-encoder 风格精排、版本/答案要点识别、去重、token budget、P@3/MRR/nDCG 前后对比和未召回证据无法恢复；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
119. 第二册 `book-02-advanced-100/chapters/07-长上下文rag与agent.md`：第 81 讲 RAG 的 Hallucination 与 Attribution 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 核对 Ragas、ARES、ALCE、RAGTruth 和 SelfCheckGPT 论文摘要资料边界，补充 RAG 幻觉治理不是单点 prompt 技巧而是检索、上下文、生成、引用和拒答共同评估的问题，新增端到端可信性分解、faithfulness、unsupported claim rate、attribution score、citation existence/support、拒答召回率、误拒率、groundedness score、judge accuracy、false support rate、retrieval/context recall、answer correctness 和端到端成功率的稳定 MathJax 写法，并新增可运行纯 Python RAG attribution audit demo，覆盖端到端成功、检索失败、context construction 失败、正确拒答、拒答失败、citation failure、faithfulness/citation/abstention 指标和根因分布；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告关键词命中仅为历史记录，`git diff --check` 通过。
120. 第二册 `book-02-advanced-100/chapters/07-长上下文rag与agent.md`：第 82 讲 Tool Use 与 Function Calling 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 延续核对 Toolformer、Gorilla、API-Bank、ToolLLM 和 OpenAI function calling / structured outputs 资料边界，补充 tool use 从自由文本提示到 schema、受约束解码、工具执行器、权限系统和 trace 的演进脉络；新增工具选择 `argmax`、schema 合法参数集合、schema 校验指示函数、工具执行 observation 回填、tool selection accuracy、argument exact match、slot F1、execution success、安全违规率、端到端延迟和 tool-use SFT 组合损失的稳定 MathJax 写法；新增可运行纯 Python tool-calling loop demo，覆盖 schema 校验、只读权限检查、天气工具、计算工具、高风险 `send_email` 权限拒绝和 observation 回填最终回答；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：继续第 83 讲 Agent Planning 的公式、状态机和最小 planning demo 精修。
121. 第二册 `book-02-advanced-100/chapters/07-长上下文rag与agent.md`：第 83 讲 Agent Planning 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 ReAct、Reflexion、Plan-and-Solve 和 Voyager arXiv 摘要资料边界，补充 planning 不是静态步骤清单，而是目标、状态、动作、observation、约束、预算和停止条件组成的可执行闭环；新增 Agent 轨迹、planner 策略、状态更新、停止函数、预算消耗、计划步骤集合、目标覆盖率、步骤可执行性、依赖无环、replanning 触发分数、task success、step success、constraint satisfaction、recovery rate 和 loop rate 的稳定 MathJax 写法；新增可运行纯 Python incident-debugging planning demo，展示初始计划根据日志 observation 动态插入 runbook 步骤、根据发布 observation 动态插入 config diff 步骤，并更新 final 步骤依赖后生成根因假设和排查顺序；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：继续第 84 讲 Agent 可靠性与安全的公式、风险指标和 demo 精修。
122. 第二册 `book-02-advanced-100/chapters/07-长上下文rag与agent.md`：第 84 讲 Agent 可靠性与安全已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 OWASP LLM Top 10、NIST AI RMF 和 prompt injection benchmark 论文资料边界，补充 Agent 安全不是单个 prompt 技巧，而是风险治理、权限 gate、数据流控制、确认、沙箱、trace、回滚和安全 eval 共同组成的系统问题；新增 Agent expected risk、加权风险估计、prompt injection attack success、权限 gate、风险确认、确认准确率/漏确认率、trace completeness、undo/compensation rate、task success、constraint violation、safe recovery、attack block rate、data leak rate 和 unauthorized execution rate 的稳定 MathJax 写法；新增可运行纯 Python agent safety policy gateway demo，覆盖正常订单查询、跨租户越权拦截、外部内容 prompt injection 诱导发邮件拦截、敏感数据外发拦截、高风险动作确认和安全指标汇总；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：继续第 85 讲 Memory-Augmented LLM 的公式、记忆写入/检索/冲突处理指标和 demo 精修。
123. 第二册 `book-02-advanced-100/chapters/07-长上下文rag与agent.md`：第 85 讲 Memory-Augmented LLM 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 MemGPT、Generative Agents、LongMem 和 Reflexion arXiv 摘要资料边界，补充 memory 不是简单长上下文或向量库，而是跨会话、跨任务的长期状态系统，需要写入、检索、更新、遗忘、权限和审计共同治理；新增 memory 写入门控分数、授权指示函数、检索相似度与时间新鲜度、上下文预算选择、冲突槽位判定、权威性/最近性/置信度胜出规则、遗忘衰减、写入 precision/recall、使用正确率、过期记忆误用率、隐私违规率和 memory gain 的稳定 MathJax 写法；新增可运行纯 Python memory store demo，覆盖保守写入、敏感信息过滤、临时工具结果拒写、外部注入拒写、同槽位偏好覆盖、scope 隔离检索、时间衰减打分和删除软标记说明；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：进入 `book-02-advanced-100/chapters/08-reasoning与test-time-compute.md`，继续第 86 讲 Chain-of-Thought 的机制、争议、评估公式和 demo 精修。
124. 第二册 `book-02-advanced-100/chapters/08-reasoning与test-time-compute.md`：第 86 讲 Chain-of-Thought 的机制与争议已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 Chain-of-Thought Prompting、Zero-shot CoT 和 Measuring Faithfulness in Chain-of-Thought Reasoning 的 arXiv 摘要资料边界，补充 CoT 从 direct answer 到 self-consistency、verifier、search/tool feedback 和 reasoning system 的演进脉络，并谨慎区分中间推理文本、真实内部原因和用户可见解释；新增 direct/CoT 最终答案准确率、CoT accuracy gain、accuracy-token-latency-risk 效用、步骤有效率、答案正确但过程错误比例、反事实干预敏感度和 token/verifier 成本的稳定 MathJax 写法；新增可运行纯 Python CoT audit demo，覆盖 final answer extraction、direct vs CoT accuracy、算式步骤验证、答案正确但推理步骤错误样本、干预敏感度、平均 token 成本和 toy utility；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：继续第 87 讲 Self-Consistency 与采样增强推理的投票/加权投票/验证器选择公式和 demo 精修。
125. 第二册 `book-02-advanced-100/chapters/08-reasoning与test-time-compute.md`：第 87 讲 Self-Consistency 与采样增强推理已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 Self-Consistency Improves Chain of Thought Reasoning in Language Models arXiv 摘要资料边界，补充 self-consistency 不是重复问模型，而是采样多条 reasoning paths 并对答案分布做近似边缘化聚合；新增 `P(a|x)` 边缘化、采样估计、`argmax` 聚合、独立采样多数正确概率、有效样本数、weighted vote、verifier 权重、成本/串行延迟/并行延迟、答案熵和 majority margin 的稳定 MathJax 写法；新增可运行纯 Python self-consistency voting demo，覆盖分数/小数/百分数归一化、majority vote、weighted vote、verifier selection、pass@k、entropy、margin 和相对成本，展示 `pass@5` 有正确候选但 majority vote 仍可能选错；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：继续第 88 讲 Verifier、Process Reward Model 与 Outcome Reward Model 的 ORM/PRM 公式、训练目标、校准指标和 demo 精修。
126. 第二册 `book-02-advanced-100/chapters/08-reasoning与test-time-compute.md`：第 88 讲 Verifier、Process Reward Model 与 Outcome Reward Model 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 Training Verifiers to Solve Math Word Problems、Solving math word problems with process- and outcome-based feedback、Let's Verify Step by Step 等 arXiv 摘要资料边界，并结合子 agent 只读核对结果，补充 verifier 是生成-验证分工而非天然真理源，ORM/PRM 不应被写成 PRM 普遍优于 ORM；新增候选级 verifier selection、ORM 归一化标签、ORM BCE、pairwise ranking loss、PRM step score、mean/geometric/min/logavg 聚合、第一错步定位、混合质量/过程/规则/成本打分、classification/regression/ranking/PRM/value function 训练目标、selection accuracy、oracle@N、pairwise accuracy、first-error accuracy、ECE、rerank gain 和成本效用的稳定 MathJax 写法；修正 ORM 标签必须基于归一化答案或程序执行结果，PRM 局部步骤分数不等于整条链最终正确概率，表格中“是否奖励好过程”改为更稳妥的“过程质量监督”；新增可运行纯 Python ORM/PRM/hybrid rerank demo，覆盖答案碰巧对但过程错、hard negative、模型置信误选、PRM 第一错步、`oracle@N` 与 selection accuracy 区别、pairwise accuracy、相对串行成本/并行延迟，并补充代码 verifier public/hidden tests 例子，展示 public tests 通过不等于隐藏测试可靠；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：继续第 89 讲 Search、Tree-of-Thought 与 MCTS 的搜索状态/价值函数/剪枝公式和最小 search demo 精修。
127. 第二册 `book-02-advanced-100/chapters/08-reasoning与test-time-compute.md`：第 89 讲 Search、Tree-of-Thought 与 MCTS 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 Tree of Thoughts、Language Agent Tree Search 和 Large Language Model Guided Tree-of-Thought arXiv 摘要资料边界，补充 search 不是简单多生成几条 CoT，而是 thought 级状态空间探索、评价、剪枝、回溯和预算控制的结构化 test-time compute；新增状态转移、policy 采样、value function、轨迹最优目标、ToT frontier 展开、Top-B 选择、beam search 成本惩罚、UCB/UCT 选择、MCTS 增量回传、剪枝 gate、完整树节点数、beam 展开节点上界、search 成本/延迟预算、剪枝保真率、节点效率和预算收益的稳定 MathJax 写法；新增可运行纯 Python beam search 与 MCTS toy demo，展示 `beam_size=1` 被高分 `shortcut` 分支误导并早剪正确路径，`beam_size=2` 保留 `factor` 分支后成功，MCTS/UCB 通过探索将最佳根动作转向 `factor`；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：继续第 90 讲 Test-Time Compute Scaling 的预算分配、收益递减、动态路由公式和最小 adaptive compute demo 精修。
128. 第二册 `book-02-advanced-100/chapters/08-reasoning与test-time-compute.md`：第 90 讲 Test-Time Compute Scaling 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 Scaling LLM Test-Time Compute Optimally can be More Effective than Scaling Model Parameters、Large Language Monkeys: Scaling Inference Compute with Repeated Sampling 和 Self-Consistency Improves Chain of Thought Reasoning in Language Models 的 arXiv 摘要资料边界，补充 test-time compute 不是简单多问几遍模型，而是对 token、采样、搜索、验证、工具调用和等待时间做 compute-optimal allocation 的系统问题；新增推理成本、质量-成本-延迟-风险效用、预算约束路由、重复采样命中概率、边际收益、early stopping、多数 margin/答案熵停止条件、cost per solved task 和 adaptive gain 的稳定 MathJax 写法；新增可运行纯 Python adaptive compute routing demo，对比 `fixed_low`、`fixed_high` 与 `adaptive` 三种策略，展示重复采样提升 coverage 但仍需要选择机制，直接回答会在高风险请求上违规，固定高预算会浪费简单请求，而自适应预算在保持 `solve_rate=1.0` 的同时将总成本从 25.0 降到 18.8、平均延迟从 4.25 降到 2.9；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：继续第 91 讲数学推理模型训练的训练目标、数据配比、过程监督/结果监督公式和最小 math training data demo 精修。
129. 第二册 `book-02-advanced-100/chapters/08-reasoning与test-time-compute.md`：第 91 讲数学推理模型训练已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 MATH、GSM8K/Training Verifiers to Solve Math Word Problems、Minerva 和 Let's Verify Step by Step 的 arXiv 摘要资料边界，补充数学 reasoning 训练从高质量 step-by-step solution、技术内容继续训练、多候选 verifier selection 到 PRM/过程监督的演进脉络；新增 CoT SFT token-level loss、结果监督答案标准化标签、ORM BCE、PRM step-level BCE、rejection sampling 保留集合、综合质量分、数据混合损失、污染率和错误类型占比的稳定 MathJax 写法；新增可运行纯 Python 数学训练数据过滤 demo，对比 `final_only` 与 `process_aware` 两种过滤，展示只看最终答案会在 6 条保留样本中混入 2 条过程噪声，而过程感知过滤保留 4 条更适合 CoT SFT 的样本，并生成 26 个 PRM 步骤标签、4 个 preference pair 和 step-level 错误分桶；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：继续第 92 讲代码推理与执行反馈的 pass@k、执行反馈、测试 verifier、sandbox 风险公式和最小 code execution feedback demo 精修。
130. 第二册 `book-02-advanced-100/chapters/08-reasoning与test-time-compute.md`：第 92 讲代码推理与执行反馈已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 Codex/HumanEval、AlphaCode、CodeRL 和 Self-Debugging 的 arXiv 摘要资料边界，补充代码 reasoning 从 functional correctness、repeated sampling、大规模采样与行为过滤、unit test/critic 反馈到 self-debug repair 的演进脉络；新增 pass@k 组合估计、public/hidden test pass rate、隐藏测试泛化差距、执行反馈 reward、代码 rejection sampling 保留集合、repair success、average attempts to solve、sandbox violation rate、compile/test/security 指标的稳定 MathJax 写法；新增可运行纯 Python code execution feedback demo，模拟 `count_even(nums)` 多候选评估，覆盖公开测试全过但隐藏测试失败、hardcode public tests、unsafe `import os` 静态拦截、语法错误、public-only rerank 误选、hidden-aware reward 正确选择、pass@1/pass@2/pass@4 和 attempts to first success；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：继续第 93 讲自我改进与合成推理数据的生成-过滤-训练闭环公式、质量/多样性/退化指标和最小 synthetic reasoning data pipeline demo 精修。
131. 第二册 `book-02-advanced-100/chapters/08-reasoning与test-time-compute.md`：第 93 讲自我改进与合成推理数据已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 Self-Instruct、STaR、WizardLM/Evol-Instruct、phi-1 和 Orca 的 arXiv 摘要资料边界，补充合成推理数据从 instruction generation、rationale bootstrapping、复杂指令演化、教材式代码数据到强推理轨迹蒸馏的脉络；新增候选生成、软质量分、hard gate、保留集合、保留率、自我改进训练闭环、合成数据混合权重、干净评估集退化风险、类别分布熵、有效桶数、污染率、数据质量分和下游收益的稳定 MathJax 写法；新增可运行纯 Python synthetic reasoning data pipeline demo，模拟 9 条合成候选的去重、过程噪声过滤、污染 gate、安全 gate、generator SFT 集合、verifier negative 集合、合成数据权重、类别多样性、filtered/unfiltered eval delta 和 lineage 追踪；顺手修正第 88、91、92 讲相邻标题编号错位；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：继续第 94 讲 Reasoning Model 评估的评测维度、污染/鲁棒性/成本指标和最小 evaluation harness demo 精修。
132. 第二册 `book-02-advanced-100/chapters/08-reasoning与test-time-compute.md`：第 94 讲 Reasoning Model 评估已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 MATH、GSM8K/Training Verifiers、HumanEval/Codex 和 BBH 等评估资料边界，并结合只读 explorer 审阅结果，补充 reasoning evaluation 从单个 benchmark 到 answer/process/budget/robustness/contamination/trajectory evaluation harness 的演进脉络；新增 final accuracy、分域 accuracy、step accuracy、答案正确但过程错误比例、难度分桶准确率、公开/隐藏测试泛化差距、证据覆盖率、Agent trajectory 合法率、安全成功率、oracle@N、selection accuracy、budget gain、cost per solved、变体鲁棒准确率、变体性能下降、污染风险率、judge-human agreement 和 release gate 综合分的稳定 MathJax 写法；新增可运行纯 Python reasoning evaluation harness demo，模拟 direct 与 reasoner 两种策略在数学、代码、逻辑、Agent 和污染 holdout 样本上的 final accuracy、step accuracy、lucky-right rate、robustness、safety violation、token/latency、cost per solved、domain accuracy、error buckets 和 release gate；同步修复第 87、92、94 讲相邻标题编号；已用 `UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 可运行且输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：继续第 95 讲从 Chat Model 到 Reasoning Model 的训练/后训练/推理时增强路径、模型行为差异公式和最小对比 demo 精修。
133. 第二册 `book-02-advanced-100/chapters/08-reasoning与test-time-compute.md`：第 95 讲从 Chat Model 到 Reasoning Model 已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 InstructGPT/RLHF、Chain-of-Thought、Self-Consistency 和 Let's Verify Step by Step/PRM800K 的 arXiv 摘要资料边界，补充从 instruction following 到 step-by-step solving、multi-sample rerank、process/outcome verification、search/tool/test-time compute 和 budget-aware product system 的演进脉络；新增 chat SFT、instruction accuracy、final accuracy、step accuracy、效用函数、upgrade mixed loss、reasoning 数据配比、token 加权混合损失、样本质量分、CoT SFT 加权目标、PRM step-level BCE、verifier selection、动态推理路由、预算分配、budget-normalized gain、instruction regression、状态转移、安全成功和 release hard gate 的稳定 MathJax 写法；新增可运行纯 Python Chat-to-Reasoning 训练混合与动态路由 demo，模拟 `chat_only`、`cot_default` 和 `reasoning_router` 在普通问答、数学、逻辑、代码 hidden test、Agent 工具和高风险请求上的 solve rate、step accuracy、安全违规率、token/latency、cost per solved、模式选择和 release gate，展示默认 CoT 仍可能因工具安全违规被拦截，而动态 router 可以在成本可控下通过 gate；已用 `sed -n '9342,9541p' ... | UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，标题编号检查通过，`git diff --check` 通过。下一目标：第八部分阶段性收尾检查，随后进入下一部分论文精读与开放研究题或继续按全书顺序推进后续章节的第二轮公式和 demo 精修。
134. 第二册 `book-02-advanced-100/chapters/09-论文精读与开放研究题.md`：第 96 讲 Attention Is All You Need 精读已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 Attention Is All You Need、RoFormer/RoPE 和 ALiBi 的 arXiv 摘要资料边界，补充从 RNN seq2seq 并行瓶颈、attention 已经有效、self-attention 替代 recurrence 到 Transformer block 成为现代 LLM 可 scaling 架构起点的来龙去脉，并强调现代 LLM 在 decoder-only、causal mask、pre-norm、RoPE/ALiBi、SwiGLU、FlashAttention、KV cache 和长上下文上的后续改造；将 Scaled Dot-Product Attention、点积方差缩放、causal mask、softmax 权重、multi-head attention、head dimension、sinusoidal position encoding、绝对位置叠加、RoPE 旋转、ALiBi 线性距离偏置、FFN/GatedFFN、post-norm/pre-norm、RNN 与 attention 信息路径、attention 计算/显存复杂度和长上下文二次放大关系改为稳定 MathJax 写法；新增可运行纯 Python scaled attention demo，覆盖 `sqrt(d_k)` 缩放前后 softmax entropy、`it` 对 `animal` 的 Q/K 匹配、causal mask 未来 token 屏蔽、sin/cos position encoding 数值和 `h*n*n` attention score cell 估算；已用 `sed -n '689,767p' ... | UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，标题编号检查通过，`git diff --check` 通过。下一目标：继续第 97 讲 GPT、InstructGPT 与 ChatGPT 路线精读的公式、路线图和最小训练/对齐流程 demo 精修。
135. 第二册 `book-02-advanced-100/chapters/09-论文精读与开放研究题.md`：第 97 讲 GPT、InstructGPT 与 ChatGPT 路线精读已完成首轮公式和代码精修；按 `WRITING_PLAN.md` 用 `wget -qO-` 核对 GPT-3 / Language Models are Few-Shot Learners、InstructGPT / Training language models to follow instructions with human feedback 和 Transformer 论文的 arXiv 摘要资料边界，补充从 next-token prediction、scaling 与 in-context learning，到 instruction tuning、reward modeling、RLHF 和 ChatGPT 多轮对话产品化的路线脉络；新增语言模型负对数似然、PPL、few-shot 条件预测、用户效用目标错配、SFT loss、Bradley-Terry 偏好概率、reward model pairwise loss、KL 约束 RLHF reward、token-level KL、偏好胜率、助手综合分、危险服从率、误拒率、多轮上下文递推和 chat release gate 的稳定 MathJax 写法；新增可运行纯 Python Base LM / SFT / RLHF 选择差异 demo，模拟 base LM 按似然选择、SFT 按示范似然选择、RLHF 按 reward 减 KL 选择，以及 unsafe、hallucination、instruction following 和 release gate 指标差异；已用 `sed -n '1533,1655p' ... | UV_CACHE_DIR=/tmp/uv-cache uv run --no-sync python` 验证 demo 输出与正文一致，常见公式风险无命中，广告/无关链接关键词无命中，`git diff --check` 通过。下一目标：继续第 98 讲 Scaling Laws 与 Chinchilla 精读的 scaling 公式、compute-optimal 结论和最小预算分配 demo 精修。

## 项目完成度

待开始。

## 论文阅读状态

待开始。

## 模拟面试记录

待开始。

## 当前薄弱点

待后续学习后记录。

## 待办项

1. 第二轮全系列精修已启动，详见 `SECOND_PASS_REVIEW_PLAN.md`。
2. 第一优先级：从第一册开始逐章遍历，全量修正数学式子和数学公式，保证 GitHub Markdown 阅读效果和变量解释正确。
3. 第二优先级：从第一册开始逐章记录并补充适合小白理解的 demo 级 Python 代码，优先使用标准库、`numpy`、`pandas`，必要时使用 `matplotlib` 或 `torch`。
4. 第三优先级：审计全书是否完整覆盖大模型相关领域，标记缺失、薄弱、过时和重复主题。
5. 第四优先级：对每个重点知识点补齐“是什么、为什么、机制、代码 demo、边界、工程实现、面试追问、专家视角”。
6. 第五优先级：第二轮每册、每个重点主题精修前必须联网检索官方论文、技术报告、官方文档、GitHub 仓库、框架文档和权威工程博客作为辅助资料。
7. 第二轮正文修改后，同步完善第四册百科、`INTERVIEW_BANK.md`、`EXERCISES.md`、`GLOSSARY_EN_ZH.md`、`PROJECTS.md` 和 `KNOWLEDGE_GRAPH.md`。

## 下一步计划

24 本主书正文第一版已完成。大模型工程师面试补充章节第 60-82 章已扩写，补充篇第一版完成。第二轮全系列精修已启动，详见 `SECOND_PASS_REVIEW_PLAN.md`。下一步从第一册开始逐章执行公式全量修正，并同步记录和补充适合小白理解的 demo 级 Python 代码；所有重点主题精修前必须联网检索辅助资料。
